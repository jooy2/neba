import{_ as t,o as a,c as s,a4 as i}from"./chunks/framework.cdJ1L9ZC.js";const g=JSON.parse('{"title":"Design language","description":"A Neba surface is a sheet of cut acrylic, not a moulded plastic key. That one sentence is the reason for every rule below. When a new component leaves you…","frontmatter":{"title":"Design language","order":1},"headers":[],"relativePath":"design/design-language.md","filePath":"en/design/design-language.md","lastUpdated":1785303798000}'),o={name:"design/design-language.md"};function n(r,e,l,h,d,c){return a(),s("div",null,[...e[0]||(e[0]=[i(`<h1 id="design-language" tabindex="-1">Design language <a class="header-anchor" href="#design-language" aria-label="Permalink to &quot;Design language&quot;">​</a></h1><p>A Neba surface is <strong>a sheet of cut acrylic, not a moulded plastic key</strong>. That one sentence is the reason for every rule below. When a new component leaves you unsure, come back to it.</p><p>What that sentence takes, and what it leaves out:</p><ul><li><strong>Taken</strong>: light catching the top edge of a surface, translucency over a blurred backdrop, an elevation scale with distinct steps.</li><li><strong>Dropped</strong>: the dark bevel underneath, gloss and refraction, bulk, and shadows that are always on.</li></ul><hr><h2 id="_1-the-surface" tabindex="-1">1. The surface <a class="header-anchor" href="#_1-the-surface" aria-label="Permalink to &quot;1. The surface&quot;">​</a></h2><p>Every filled surface is four layers, in this order and at these strengths.</p><table tabindex="0"><thead><tr><th>Layer</th><th>Token</th><th>Job</th></tr></thead><tbody><tr><td>Fill</td><td><code>--neba-{color}-fill</code></td><td>The colour, opaque to <code>--neba-fill-alpha</code> (88%)</td></tr><tr><td>Backdrop</td><td><code>--neba-blur</code></td><td><code>blur(9px) saturate(1.5)</code></td></tr><tr><td>Grain</td><td><code>--neba-grain</code></td><td>A noise tile composited in <code>overlay</code></td></tr><tr><td>Edge</td><td><code>--neba-plate-solid</code></td><td>A bright top line plus a 1px white hairline all round</td></tr></tbody></table><h3 id="translucency-is-tuned-with-the-blur-not-just-the-alpha" tabindex="-1">Translucency is tuned with the blur, not just the alpha <a class="header-anchor" href="#translucency-is-tuned-with-the-blur-not-just-the-alpha" aria-label="Permalink to &quot;Translucency is tuned with the blur, not just the alpha&quot;">​</a></h3><p>Lowering the alpha does not by itself make glass. <strong>The blur radius is what decides whether the backdrop is legible.</strong> At 16px a grid line behind the control smears into flat colour and the surface reads opaque again. Held at 9px, the backdrop is present but never resolves into something you could read.</p><p>88% is the floor for 4.5:1 white-on-fill over a white page. <strong>Fill lightness was picked against that number, not against opaque.</strong> Lower the alpha and the lightness has to come down with it.</p><h3 id="the-undyed-sheet-neba-glass-bg-is-the-exception" tabindex="-1">The undyed sheet (<code>--neba-glass-bg</code>) is the exception <a class="header-anchor" href="#the-undyed-sheet-neba-glass-bg-is-the-exception" aria-label="Permalink to &quot;The undyed sheet (\`--neba-glass-bg\`) is the exception&quot;">​</a></h3><p>That rule is about a <strong>dyed</strong> fill. <code>--neba-glass-bg</code> — the base under the outline and text variants, and the default surface of Card, Box and TextField — has no colour in it, so alpha is the only axis there is. Here alpha does not decide whether the backdrop is legible; it decides <strong>whether the sheet reads as white</strong>.</p><p>In the light theme, 42% let through more of the page than of the sheet itself. On any backdrop that is not pure white, that backdrop&#39;s grey came straight up through it and the whole surface went dull. <strong>That is why it is 66%</strong> — the blur is still 9px, so the backdrop is just as unreadable as before. The dark theme went 5% → 7% for the same reason.</p><h3 id="container-surfaces-are-never-dyed" tabindex="-1">Container surfaces are never dyed <a class="header-anchor" href="#container-surfaces-are-never-dyed" aria-label="Permalink to &quot;Container surfaces are never dyed&quot;">​</a></h3><p>Box, Card and TextField draw their surface from <code>--neba-panel</code> / <code>-hover</code> / <code>-press</code> — <strong>three strengths of an undyed white sheet</strong>, not from the family&#39;s own <code>--neba-{color}-panel</code>.</p><p>What a container holds is other people&#39;s content, and it arrives with its own colours: body text, links, buttons, fields. Tinting the sheet underneath puts every one of them on a background they were not chosen against. So <strong>the family stops at the hairline, the focus ring and the caret, and the sheet stays white.</strong></p><p>Controls are the opposite case and keep the tinted <code>--neba-{color}-panel</code>, because a Button&#39;s surface <em>is</em> the thing being coloured.</p><p>It is also why the three steps rise in <strong>opacity</strong> rather than lightness: as a surface is engaged it holds more light, instead of turning grey.</p><blockquote><p><strong>One consequence.</strong> On a <code>solid</code> Box or Card, which has no border, <code>color</code> has nothing left to reach and makes no visible difference. On a container, <code>color</code> is effectively the prop that picks the edge.</p></blockquote><h3 id="the-grain-is-what-makes-it-acrylic" tabindex="-1">The grain is what makes it acrylic <a class="header-anchor" href="#the-grain-is-what-makes-it-acrylic" aria-label="Permalink to &quot;The grain is what makes it acrylic&quot;">​</a></h3><p>Translucency plus blur alone gives you polished glass. Noise is what makes it sandblasted acrylic. One 120px <code>feTurbulence</code> tile (fractalNoise, three octaves) goes in as a data URI and sits over the fill in <code>background-blend-mode: overlay</code>. The tile is 12% alpha, decoded once, and reused by every surface on the page.</p><h3 id="no-dark-bevel-on-the-edge" tabindex="-1">No dark bevel on the edge <a class="header-anchor" href="#no-dark-bevel-on-the-edge" aria-label="Permalink to &quot;No dark bevel on the edge&quot;">​</a></h3><p><code>inset 0 -1px 0 black</code> turns a surface into injection-moulded plastic immediately. Use only the bright top line (<code>--neba-plate-top</code>) and the white hairline around the whole plate (<code>--neba-plate-edge</code>).</p><h3 id="a-tick-wears-no-plate-at-all" tabindex="-1">A tick wears no plate at all <a class="header-anchor" href="#a-tick-wears-no-plate-at-all" aria-label="Permalink to &quot;A tick wears no plate at all&quot;">​</a></h3><p>The plate is a 1px line, so how much of a surface it is depends on how big that surface is. On a 32px button it is light catching a cut edge. On an 18px checkbox, an 18px radio or a 20px switch track it is a bevel drawn at a fifteenth of the object, and a bevel that heavy on something that small reads as a toolbar icon from 2008.</p><p>So <code>Checkbox</code>, <code>Radio</code> and <code>Switch</code> keep the acrylic — the grain, the sheen and the backdrop blur are what make the box a sheet of something — and drop <code>--neba-plate-glass</code> and <code>--neba-plate-solid</code> entirely. They are the only controls in the library that do.</p><h3 id="the-sheen-is-off-vertical" tabindex="-1">The sheen is off-vertical <a class="header-anchor" href="#the-sheen-is-off-vertical" aria-label="Permalink to &quot;The sheen is off-vertical&quot;">​</a></h3><p><code>linear-gradient(148deg, …)</code>. A symmetric gradient falling straight down says nothing about where the light is coming from, which leaves the surface looking painted rather than cut. Tilting it a few degrees reads as light arriving from one side.</p><hr><h2 id="_2-colour" tabindex="-1">2. Colour <a class="header-anchor" href="#_2-colour" aria-label="Permalink to &quot;2. Colour&quot;">​</a></h2><p>This section is the <em>why</em>. For what the tokens actually resolve to and how to override them, see <a href="./color">Colour</a>.</p><p>The base colour is <code>#4072cd</code>; everything else comes off its palette.</p><table tabindex="0"><thead><tr><th>Role</th><th>Where it comes from</th></tr></thead><tbody><tr><td><code>primary</code></td><td>The base colour</td></tr><tr><td><code>secondary</code></td><td>Slate that keeps the base colour&#39;s hue</td></tr><tr><td><code>success</code></td><td>A toned-down triadic green</td></tr><tr><td><code>warning</code></td><td>The complementary amber</td></tr><tr><td><code>danger</code></td><td>A split-complementary vermilion</td></tr><tr><td><code>info</code></td><td>The analogous cyan</td></tr></tbody></table><p>Every colour is defined in <code>oklch()</code>, because its lightness axis matches perception — which is what lets all six families be pinned to the same number.</p><h3 id="only-five-values-are-hand-picked-per-family" tabindex="-1">Only five values are hand-picked per family <a class="header-anchor" href="#only-five-values-are-hand-picked-per-family" aria-label="Permalink to &quot;Only five values are hand-picked per family&quot;">​</a></h3><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>--neba-{color}-solid          the fill&#39;s base colour</span></span>
<span class="line"><span>--neba-{color}-solid-hover    −4.5 lightness</span></span>
<span class="line"><span>--neba-{color}-solid-active   −12 lightness</span></span>
<span class="line"><span>--neba-{color}-on-solid       text on the fill</span></span>
<span class="line"><span>--neba-{color}-accent         readable on a surface (for the text/outline variants)</span></span></code></pre></div><p>The rest (<code>-fill</code>, <code>-panel</code>, <code>-soft</code>, <code>-line</code>, <code>-ring</code>) are computed with <code>color-mix()</code> in the derived block. <strong>Adding a colour family is two edits</strong> — one entry in the <code>NebaColor</code> union and five lines in <code>styles.css</code>.</p><h3 id="chroma-goes-to-the-gamut-edge-lightness-goes-as-far-as-contrast-allows" tabindex="-1">Chroma goes to the gamut edge; lightness goes as far as contrast allows <a class="header-anchor" href="#chroma-goes-to-the-gamut-edge-lightness-goes-as-far-as-contrast-allows" aria-label="Permalink to &quot;Chroma goes to the gamut edge; lightness goes as far as contrast allows&quot;">​</a></h3><p>When a family looks muddy, the cause is usually not its lightness but its <strong>chroma</strong>. The <code>oklch()</code> chroma ceiling in sRGB differs per hue and per lightness, and a colour sitting well under that ceiling reads as grey at the very same brightness. Neba&#39;s chroma is held at roughly <strong>90% of the maximum</strong> for each family&#39;s lightness — vivid, with enough margin that the browser never has to clip.</p><p>Lightness is not nearly as free. With a white <code>on-solid</code> and a fill at 88%, holding 4.5:1 over a white page pins the fill to the high 40s / low 50s. <strong>A brighter fill means a darker ink</strong> — which is exactly what <code>warning</code>, the one family that does it, is doing. Every step is checked against this, hover and active included.</p><blockquote><p><strong>Moving the hue a few degrees is also an option.</strong> <code>success</code> went 152 → 148 and <code>info</code> 218 → 223. Both sit where sRGB is unusually narrow at mid lightness, and a few degrees to the side buys chroma that no amount of tuning at the original hue could.</p></blockquote><blockquote><p><strong>The derived block is repeated per theme root.</strong> A custom property resolves its <code>var()</code>s <strong>on the element that declares it</strong>. Declared only on <code>:root</code>, the derived tokens would freeze to their light-theme values inside a <code>.dark</code> subtree. That is why the selector is <code>:root, .dark, .light, [data-theme=&#39;dark&#39;], [data-theme=&#39;light&#39;]</code>.</p></blockquote><h3 id="do-not-lighten-the-fill-in-dark-mode" tabindex="-1">Do not lighten the fill in dark mode <a class="header-anchor" href="#do-not-lighten-the-fill-in-dark-mode" aria-label="Permalink to &quot;Do not lighten the fill in dark mode&quot;">​</a></h3><p>Pastel fills with dark text on them are a common dark-theme approach, but on a dark screen the fill itself becomes the light source and it hurts to look at. Neba keeps fills mid-tone in dark mode and keeps the text white. The only thing that lightens is <code>accent</code>, the colour that has to read on a surface.</p><h3 id="warning-has-dark-text" tabindex="-1"><code>warning</code> has dark text <a class="header-anchor" href="#warning-has-dark-text" aria-label="Permalink to &quot;\`warning\` has dark text&quot;">​</a></h3><p>White on amber does not reach 4.5:1 at any lightness. <code>--neba-warning-on-solid</code> is the one dark brown in the set. Changing the text colour is the right answer; distorting the family to preserve contrast is not.</p><hr><h2 id="_3-size-and-density" tabindex="-1">3. Size and density <a class="header-anchor" href="#_3-size-and-density" aria-label="Permalink to &quot;3. Size and density&quot;">​</a></h2><h3 id="size-—-height-and-type-scale" tabindex="-1"><code>size</code> — height and type scale <a class="header-anchor" href="#size-—-height-and-type-scale" aria-label="Permalink to &quot;\`size\` — height and type scale&quot;">​</a></h3><table tabindex="0"><thead><tr><th></th><th>xs</th><th>sm</th><th>md</th><th>lg</th><th>xl</th></tr></thead><tbody><tr><td>Height</td><td>22px</td><td>26px</td><td><strong>32px</strong></td><td>40px</td><td>48px</td></tr><tr><td>Text</td><td>11px</td><td>12px</td><td><strong>13px</strong></td><td>15px</td><td>17px</td></tr><tr><td>Radius</td><td>10px</td><td>12px</td><td><strong>14px</strong></td><td>18px</td><td>22px</td></tr></tbody></table><p>The uneven steps are deliberate. <code>md</code> is the desktop workhorse, <code>xs</code>/<code>sm</code> are for toolbars and table rows, and <code>lg</code>/<code>xl</code> are for the one action a screen is about. Marching up in equal 4px increments would make five steps look like two.</p><p><code>xl</code> at 48px clears the 44px mobile touch target.</p><h3 id="the-radius-is-45-of-the-height" tabindex="-1">The radius is 45% of the height <a class="header-anchor" href="#the-radius-is-45-of-the-height" aria-label="Permalink to &quot;The radius is 45% of the height&quot;">​</a></h3><p>At 50% it is a pill. Stopping at 45% leaves a flat run along the top and bottom edge, and that flat run is what reads as a plate with its corners cut off.</p><h3 id="density-—-padding-and-only-padding" tabindex="-1"><code>density</code> — padding, and only padding <a class="header-anchor" href="#density-—-padding-and-only-padding" aria-label="Permalink to &quot;\`density\` — padding, and only padding&quot;">​</a></h3><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>default   10 / 12 / 16 / 20 / 24px</span></span>
<span class="line"><span>compact    6 /  8 / 10 / 12 / 16px</span></span></code></pre></div><p><strong>Density touches neither the height nor the type scale.</strong> Two controls of the same <code>size</code> are the same height whatever their density, so a row of mixed-density controls keeps its baseline. The two tracks are roughly 2:1, so the difference is legible at a glance.</p><hr><h2 id="_4-elevation" tabindex="-1">4. Elevation <a class="header-anchor" href="#_4-elevation" aria-label="Permalink to &quot;4. Elevation&quot;">​</a></h2><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">type</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> NebaElevation</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 0</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 1</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 2</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 3</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span></code></pre></div><p><strong>The default is 0, and 0 means no shadow at all.</strong> What separates a surface from the page is the acrylic edge, not a drop shadow. Raise it only for surfaces that genuinely float above the content around them.</p><p>Hovering adds a level and pressing removes one, so a control at elevation 0 still answers a press without growing a shadow. Level 4 is only reachable by hovering a level-3 surface.</p><p><strong>Never tint a shadow with the control&#39;s own colour.</strong> A coloured glow is the loudest thing a small control can do. Every <code>--neba-shadow-*</code> is neutral.</p><hr><h2 id="_5-motion" tabindex="-1">5. Motion <a class="header-anchor" href="#_5-motion" aria-label="Permalink to &quot;5. Motion&quot;">​</a></h2><h3 id="controls-do-not-move" tabindex="-1">Controls do not move <a class="header-anchor" href="#controls-do-not-move" aria-label="Permalink to &quot;Controls do not move&quot;">​</a></h3><p><strong>Do not use <code>transform</code>.</strong> Scaling a control resamples its label, and text that shimmers under the cursor undoes every other bit of restraint. State changes are expressed <strong>in colour and depth only</strong>.</p><h3 id="press-is-instant-release-is-slow" tabindex="-1">Press is instant, release is slow <a class="header-anchor" href="#press-is-instant-release-is-slow" aria-label="Permalink to &quot;Press is instant, release is slow&quot;">​</a></h3><p>This asymmetry is the house interaction signature. The same principle drives two things.</p><p><strong>The fill</strong> — per-property durations, with <code>:active</code> overriding all of them to 0ms.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>transition-property: background-color, border-color, box-shadow, color;</span></span>
<span class="line"><span>transition-duration: var(--neba-duration-fill), var(--neba-duration), …;  /* 340ms, 160ms… */</span></span></code></pre></div><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>&amp;:active { transition-duration: 0ms }</span></span></code></pre></div><p>The colour lands on the frame of the press and then takes 340ms to drain back out.</p><p><strong>The afterglow layer</strong> — the same trick applied to <code>opacity</code>. No JavaScript, no ripple element, no timers.</p><div class="language-css vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">css</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">.neba-glow::after</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  opacity</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  transition</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: opacity </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">900</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">ms</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> cubic-bezier</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0.22</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0.36</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">.neba-glow:active::after</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  opacity</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  transition-duration</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">ms</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><h3 id="the-pointer-spotlight" tabindex="-1">The pointer spotlight <a class="header-anchor" href="#the-pointer-spotlight" aria-label="Permalink to &quot;The pointer spotlight&quot;">​</a></h3><p><code>.neba-glow::before</code> is a soft bloom trailing the cursor. The component writes <code>--n-mx</code>/<code>--n-my</code> straight to the element&#39;s inline style on <code>pointermove</code>.</p><p><strong>Do not hold this in React state.</strong> The event fires at pointer rate, so a <code>setState</code> would re-render the tree on every mouse move. The coordinates are read from <code>offsetX</code>/<code>offsetY</code> rather than <code>getBoundingClientRect()</code>, so nothing forces a reflow. Icons carry <code>pointer-events: none</code>, so the offsets are always relative to the control.</p><p>Both layers respect <code>@media (hover: hover)</code> and <code>prefers-reduced-motion</code>.</p><h3 id="a-ceiling-on-the-flourish" tabindex="-1">A ceiling on the flourish <a class="header-anchor" href="#a-ceiling-on-the-flourish" aria-label="Permalink to &quot;A ceiling on the flourish&quot;">​</a></h3><p>The light effects are <strong>the highlight riding on top of the press, not the press itself</strong>. The press is the fill going dark. That is why <code>--neba-flash-on-fill</code> is only a shade brighter than the spotlight it replaces.</p><hr><h2 id="_6-states" tabindex="-1">6. States <a class="header-anchor" href="#_6-states" aria-label="Permalink to &quot;6. States&quot;">​</a></h2><p>The three states each have to speak on their own axis, and each has to be distinguishable from the default at a glance.</p><table tabindex="0"><thead><tr><th>State</th><th>How it is expressed</th><th>Why</th></tr></thead><tbody><tr><td><code>disabled</code></td><td>Drops the colour family entirely for neutral grey</td><td>A faded colour still reads as &quot;this is the primary action, only blurrier&quot;</td></tr><tr><td><code>loading</code></td><td>Unchanged, with a spinner in the <code>startIcon</code> slot</td><td>It is in progress, not unavailable</td></tr><tr><td><code>readOnly</code></td><td>Keeps the colour, goes flat, <code>saturate(0.55)</code></td><td>A label that happens to be button-shaped</td></tr></tbody></table><p>Only <code>disabled</code> uses the native <code>disabled</code> attribute. <code>loading</code> and <code>readOnly</code> are marked with <code>aria-disabled</code>, keep focus, and stop activation in the handler.</p><blockquote><p><strong>Don&#39;t express state with opacity.</strong> <code>opacity: 0.5</code> reads as &quot;blurry&quot; whatever the state is. Give each state its own axis — saturation, colour family, flatness.</p></blockquote><hr><h2 id="_7-implementation-rules" tabindex="-1">7. Implementation rules <a class="header-anchor" href="#_7-implementation-rules" aria-label="Permalink to &quot;7. Implementation rules&quot;">​</a></h2><h3 id="branch-state-in-js-not-in-css" tabindex="-1">Branch state in JS, not in CSS <a class="header-anchor" href="#branch-state-in-js-not-in-css" aria-label="Permalink to &quot;Branch state in JS, not in CSS&quot;">​</a></h3><p>Two Tailwind variants of equal specificity are resolved by <strong>their order in the generated stylesheet</strong>. That is not a property a component may depend on.</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// like this</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">disabled </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> disabledClasses[variant] </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> readOnly </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> readOnlyClasses[variant] </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> restClasses[variant];</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// not like this — the precedence of data-disabled: against data-readonly: is undefined</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;data-disabled:bg-gray-200 data-readonly:bg-blue-500&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span></code></pre></div><h3 id="colour-slots-go-in-inline-styles" tabindex="-1">Colour slots go in inline styles <a class="header-anchor" href="#colour-slots-go-in-inline-styles" aria-label="Permalink to &quot;Colour slots go in inline styles&quot;">​</a></h3><p>Tailwind only ever sees <strong>class names that appear literally in the source</strong>. Hardcoding <code>[--n-fill:var(--neba-primary-fill)]</code> per family means dozens of classes for every colour added. Generate the <code>--n-*</code> slots as inline styles instead.</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;--n-fill&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">\`var(--neba-\${</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">color</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">}-fill)\`</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span></code></pre></div><p>This is the only kind of reason to step outside Tailwind: <strong>step outside only when Tailwind cannot express it.</strong></p><h3 id="move-it-to-css-when-it-stops-being-readable" tabindex="-1">Move it to CSS when it stops being readable <a class="header-anchor" href="#move-it-to-css-when-it-stops-being-readable" aria-label="Permalink to &quot;Move it to CSS when it stops being readable&quot;">​</a></h3><p><code>.neba-glow</code> is a real class in <code>styles.css</code> rather than a set of Tailwind utilities because <code>[&amp;::before]:[background:radial-gradient(…)]</code> is technically expressible and impossible to maintain. Styling that puts a gradient on a pseudo-element belongs in CSS.</p><h3 id="never-use-outline-none" tabindex="-1">Never use <code>outline-none</code> <a class="header-anchor" href="#never-use-outline-none" aria-label="Permalink to &quot;Never use \`outline-none\`&quot;">​</a></h3><p>Tailwind v4&#39;s <code>outline-*</code> utilities route the style through <code>--tw-outline-style</code>. An <code>outline-none</code> anywhere on the element sets that variable to <code>none</code> and <strong>the focus ring disappears entirely.</strong> Use the shorthand.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>focus-visible:[outline:2px_solid_var(--n-ring)] focus-visible:outline-offset-2</span></span></code></pre></div>`,102)])])}const u=t(o,[["render",n]]);export{g as __pageData,u as default};
