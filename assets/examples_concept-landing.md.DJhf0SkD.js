import{_ as d,C as a,o as c,c as n,j as t,a as r,E as i,a4 as s}from"./chunks/framework.CKd2nDfM.js";const y=JSON.parse('{"title":"Landing page","description":"A marketing page for Kestrel, a product analytics tool that does not exist. It is the case a component library is least obviously for (mostly type, space and…","frontmatter":{"title":"Landing page","order":2,"aside":false},"headers":[],"relativePath":"examples/concept-landing.md","filePath":"en/examples/concept-landing.md","lastUpdated":1788613435000}'),l={name:"examples/concept-landing.md"};function h(p,e,u,m,g,b){const o=a("Demo");return c(),n("div",null,[e[0]||(e[0]=t("h1",{id:"landing-page",tabindex:"-1"},[r("Landing page "),t("a",{class:"header-anchor",href:"#landing-page","aria-label":'Permalink to "Landing page"'},"​")],-1)),e[1]||(e[1]=t("p",{class:"neba-lede"},"A marketing page for Kestrel, a product analytics tool that does not exist. It is the case a component library is least obviously for (mostly type, space and one call to action repeated), which makes it the best test of whether the parts compose.",-1)),i(o,{src:"concepts/landing","min-height":"640px"}),e[2]||(e[2]=s('<p>The source is one file: <code>docs/.vitepress/demos/concepts/landing.tsx</code>. Everything on the page is a Neba component; nothing is a bare <code>div</code> dressed up as one.</p><h2 id="what-it-is-made-of" tabindex="-1">What it is made of <a class="header-anchor" href="#what-it-is-made-of" aria-label="Permalink to &quot;What it is made of&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Block</th><th>Components used</th><th>Worth noticing</th></tr></thead><tbody><tr><td>Announcement bar</td><td><code>Pill</code></td><td>A <code>Pill</code> with <code>onClick</code> is a button, so the banner is reachable by keyboard without any extra markup</td></tr><tr><td>Header</td><td><code>Toolbar</code> <code>Icon</code> <code>Button</code> <code>IconButton</code> <code>Tooltip</code></td><td><code>render={&lt;header /&gt;}</code> makes the bar a real landmark; the nav links are <code>variant=&quot;text&quot;</code> buttons on one baseline</td></tr><tr><td>Hero</td><td><code>Typography</code> <code>Chip</code> <code>Button</code> <code>Avatar</code> <code>Highlight</code></td><td><code>Typography</code> sets both the type scale and the element, so <code>level=&quot;h1&quot;</code> is an actual <code>&lt;h1&gt;</code></td></tr><tr><td>Trust strip</td><td><code>Divider</code> <code>Typography</code></td><td>A <code>Divider</code> with children carries the section label, so the rule and the heading are one element</td></tr><tr><td>Numbers</td><td><code>GridContainer</code> <code>Grid</code> <code>Statistic</code></td><td><code>unit</code>, <code>prefix</code> and <code>previousValue</code> cover every shape of figure on the row</td></tr><tr><td>Features</td><td><code>Card</code> <code>Icon</code></td><td>The icon sits in <code>headerAction</code>, which keeps it on the title&#39;s baseline at every size</td></tr><tr><td>Product tour</td><td><code>Tabs</code> <code>ProgressLinear</code> <code>List</code> <code>Chip</code></td><td>Three views of the same data; the funnel is <code>ProgressLinear</code> with <code>showValue</code> rather than a chart</td></tr><tr><td>Quote</td><td><code>Blockquote</code></td><td><code>author</code> and <code>source</code> are separate slots, so the attribution wraps as two lines on a phone</td></tr><tr><td>Pricing</td><td><code>SegmentedButton</code> <code>Card</code> <code>List</code> <code>Table</code></td><td>The billing toggle is a segmented control; the comparison table is rendered from a column list</td></tr><tr><td>FAQ</td><td><code>Accordion</code></td><td>One <code>AccordionItem</code> per question, all closed on arrival</td></tr><tr><td>Closing CTA</td><td><code>Card</code> <code>TextField</code> <code>Button</code></td><td>The only field on the page, inside a real <code>&lt;form&gt;</code> with its own submit</td></tr><tr><td>Footer</td><td><code>Divider</code> <code>Grid</code> <code>List</code> <code>Button</code></td><td>Link columns are <code>List</code> with <code>href</code> on each row, which renders anchors</td></tr></tbody></table><h2 id="notes" tabindex="-1">Notes <a class="header-anchor" href="#notes" aria-label="Permalink to &quot;Notes&quot;">​</a></h2><ul><li>The page is bounded by a single <code>Container maxWidth=&quot;xl&quot;</code> on a <code>&lt;main&gt;</code>. Nothing inside it sets a page width of its own.</li><li>Colour carries meaning, not emphasis: the featured plan is <code>color=&quot;primary&quot;</code> with <code>elevation={2}</code>, and the other two stay <code>secondary</code> and flat.</li><li>The email field validates on change and shows <code>error</code> only once something has been typed, so an empty form is never red.</li></ul>',5))])}const v=d(l,[["render",h]]);export{y as __pageData,v as default};
