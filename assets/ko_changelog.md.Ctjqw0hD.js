import{_ as o,o as t,c as a,a4 as d}from"./chunks/framework.CKd2nDfM.js";const g=JSON.parse(`{"title":"변경 기록","description":"Registering a language costs 3.4 kB now against 2.8 kB. Words that were hardcoded English are in every language's module: those of a Combobox, a ToastProvider…","frontmatter":{"title":"변경 기록","order":1,"editLink":false},"headers":[],"relativePath":"ko/changelog.md","filePath":"ko/changelog.md","lastUpdated":null}`),n={name:"ko/changelog.md"};function s(i,e,r,c,h,l){return t(),a("div",null,[...e[0]||(e[0]=[d(`<h1 id="changelog" tabindex="-1">Changelog <a class="header-anchor" href="#changelog" aria-label="Permalink to &quot;Changelog&quot;">​</a></h1><h2 id="vnext-2026" tabindex="-1">vNext (2026--) <a class="header-anchor" href="#vnext-2026" aria-label="Permalink to &quot;vNext (2026--)&quot;">​</a></h2><h2 id="_1-14-0-2026-09-15" tabindex="-1">1.14.0 (2026-09-15) <a class="header-anchor" href="#_1-14-0-2026-09-15" aria-label="Permalink to &quot;1.14.0 (2026-09-15)&quot;">​</a></h2><h3 id="breaking-changes" tabindex="-1">Breaking changes <a class="header-anchor" href="#breaking-changes" aria-label="Permalink to &quot;Breaking changes&quot;">​</a></h3><ul><li><p><strong>Under a reduced-motion preference an <code>Animate*</code> lands on its last frame.</strong> <code>animation: none</code> switched every effect off, so content leaving with <code>mode=&quot;out&quot;</code> stayed on screen, an <code>AnimateRotate</code> rested at <code>0deg</code> rather than at its <code>to</code>, and <code>animationend</code> never fired, so a component unmounted on it never went. A running effect is cut to a one-millisecond run that ends on its last frame now. An effect still waiting for its trigger, or held with <code>paused</code>, shows the element as it is, and a float stops. Content that should stay visible under reduced motion while it leaves should be rendered conditionally rather than faded out.</p></li><li><p><strong>A <code>Sidebar</code> outside a <code>PageLayout</code> does not hold its place by default.</strong> <code>sticky</code> was on everywhere, and on its own a Sidebar became a column the height of the window, which inside a shorter box put its last items out of reach. It defaults to <code>true</code> inside a PageLayout and <code>false</code> outside one; a standalone sidebar that should stick passes <code>sticky</code>. A Sidebar whose own <code>collapseBelow</code> differs from its layout&#39;s now warns in development, because a SidebarTrigger follows the layout, and the page says that <code>ref</code> reaches the column and is <code>null</code> while the sidebar is collapsed.</p></li><li><p><strong>An <code>AnimateCounter</code> counts a new <code>value</code> on from the number on screen.</strong> Every change of <code>value</code> counted up from <code>from</code> again, after <code>delay</code> again, so a live figure going from 100 to 105 dropped to 0 and climbed back. A counter that has counted now runs from wherever it is to the new value; <code>from</code> and <code>delay</code> are for the first count and for one that starts over when its trigger lets go. A counter meant to start from <code>from</code> on every change should be given a new <code>key</code>.</p></li><li><p><strong>A <code>Collapsible</code>&#39;s title and subtitle wrap.</strong> Both were always cut to one line, so a title written as a question lost its end on a narrow screen. They wrap now, as an <code>AccordionItem</code>&#39;s do, and the new <code>lines</code> cuts them off after that many lines with an ellipsis. A header that has to stay on one line should pass <code>lines={1}</code>.</p></li><li><p><strong>A <code>Show</code> given <code>render</code> keeps the element&#39;s own box.</strong> <code>display: contents</code> went on the element <code>render</code> named as well, so the <code>render={&lt;td /&gt;}</code> and <code>render={&lt;li /&gt;}</code> the docs recommend lost a cell&#39;s padding, border and background and a list item&#39;s marker. Only a <code>Show</code> without <code>render</code> is <code>display: contents</code> now. A <code>Show</code> whose <code>render</code> names a block element inside a grid or a flex row becomes a box of its own there: put the grid or flex items inside it, or drop <code>render</code>.</p></li><li><p><strong>A lone <code>width</code> or <code>height</code> on an <code>Image</code> sizes its box.</strong> One dimension is not a proportion, so it used to reach the <code>&lt;img&gt;</code> and change nothing on the page. <code>height={200}</code> is now a box 200 pixels tall across the width it is given, and <code>width={320}</code> one 320 wide, capped at the container, with <code>fit</code> deciding what the picture does inside. A number is pixels and a string a CSS length, and beside a <code>ratio</code> a lone <code>height</code> takes its width from the ratio. An <code>Image</code> given only one of the two as a hint about the file draws at that size now: pass both, or remove the one. Both together still reserve their proportion, as before.</p></li><li><p><strong>A <code>Gallery</code> loads its tiles lazily.</strong> Every tile&#39;s picture is <code>loading=&quot;lazy&quot;</code> unless the Gallery says otherwise, so a wall of forty photographs asks for the few a reader can see rather than all forty at once. A Gallery that is the largest thing above the fold should pass <code>loading=&quot;eager&quot;</code>, or its first row arrives later than it did.</p></li><li><p><strong>A <code>Form</code> without <code>onSubmit</code> lets the submit go ahead.</strong> It prevented the native submit whether or not anybody was given the values, and React skips a function <code>action</code> for a prevented submit, so <code>&lt;Form action={formAction}&gt;</code> did nothing. Validation still runs first and still stops an invalid submit. A <code>Form</code> that relied on never navigating with no <code>onSubmit</code> should pass <code>onSubmit</code> or an <code>action</code>.</p></li><li><p><strong>A disabled <code>MenuItem</code> or <code>NavigationMenuItem</code> with an <code>href</code> is no longer a link.</strong> Both ignored <code>disabled</code> once they had an <code>href</code>, so the row was drawn as available and pressing it navigated. While disabled it renders as a disabled row, with no <code>&lt;a&gt;</code> and no <code>href</code>; a test that found it by the <code>link</code> role finds a <code>menuitem</code>, or plain text in a navigation menu.</p></li><li><p><strong>A <code>DataTable</code> sorted descending puts rows with no value last.</strong> A blank, <code>null</code>, <code>undefined</code> or <code>NaN</code> cell went last in an ascending sort and was reversed to the top of a descending one, so the first screen of a column sorted high-to-low could be all blanks. Blanks now go last in both directions, which was always the stated intent. A column with its own <code>compare</code> orders its blanks itself, as before.</p></li><li><p><strong>A <code>TextLink</code> with a <code>target</code> of its own is announced as opening elsewhere.</strong> The spoken &quot;(opens in a new tab)&quot; came only with <code>newTab</code>, so <code>target=&quot;_blank&quot;</code> written by hand opened a new tab without a word to a screen reader. The sentence now follows any target that leaves this tab, which changes the link&#39;s accessible name: a test that finds it by name needs the sentence added. The glyph still comes only with <code>newTab</code> or <code>icon</code>.</p></li><li><p><strong>A picker&#39;s trigger is named by its label and what it shows.</strong> <code>DatePicker</code>, <code>DateRangePicker</code>, <code>TimePicker</code>, <code>DateTimePicker</code>, <code>TreeSelect</code> and <code>ColorPicker</code> named the trigger by <code>label</code> alone, which covered the date, the time or the choice written on it, so a screen reader said &quot;Ships on, button&quot; whatever had been picked. The name is now the label followed by the trigger&#39;s own text — &quot;Ships on Jul 30, 2026&quot;. A test that finds the trigger by its exact label needs the value added, or <code>exact: false</code>.</p></li><li><p><strong>A calendar&#39;s month and year buttons are named by what they show.</strong> They were named &quot;Choose a month&quot; and &quot;Choose a year&quot; over the words &quot;July&quot; and &quot;2026&quot;, so a reader using voice control could not say what they saw to press them. They are named by their text now and described by what they do, which changes their accessible names: a test that found <code>name: &#39;Choose a month&#39;</code> finds the month on screen. This covers <code>Calendar</code> and every date picker.</p></li><li><p><strong>A <code>FilePicker</code>&#39;s button is named by its <code>label</code>.</strong> The label was drawn above the box and tied to nothing, so a form asking for a résumé and a cover letter had two buttons both called &quot;Drop files here, or click to browse&quot;. The button is now named by the label followed by what the box says. A test that finds the box by its title alone needs the label added, or <code>exact: false</code>.</p></li><li><p><strong>A <code>Fieldset</code>&#39;s <code>description</code> describes the group instead of joining its name.</strong> It was drawn inside the legend, so the group was named &quot;Billing address Where the card statement goes.&quot; and that sentence was read before every control in it. The legend alone names the group now, and the description is tied to it with <code>aria-describedby</code>, merged with any the caller passes. A test that found the group by the whole sentence finds it by the legend.</p></li><li><p><strong>A pressable <code>Chip</code> is a toggle only when it says whether it is on.</strong> <code>selected</code> defaulted to <code>false</code>, so every chip with an <code>onClick</code> carried <code>aria-pressed=&quot;false&quot;</code> and was announced as a toggle that was off, including a chip that only opened something. It is a plain button now unless <code>selected</code> is passed, and <code>selected={false}</code> still makes it a toggle. A test that expected <code>aria-pressed=&quot;false&quot;</code> on a chip with no <code>selected</code> needs the prop.</p></li><li><p><strong>An <code>AccordionItem</code>&#39;s <code>action</code> sits beside its heading instead of inside it.</strong> The action was rendered in the <code>&lt;h3&gt;</code>, so a section with a Switch in its header was a heading named &quot;Billing Enable&quot;, and every heading list read the control&#39;s label as part of the title. The heading holds only the trigger now, and the action is its sibling in the same row. Nothing moves on screen; a test that found the heading by title and action together finds it by the title.</p></li><li><p><strong>A closed <code>Accordion</code> or <code>Collapsible</code> panel stays in the document.</strong> <code>hiddenUntilFound</code> is on by default. A closed panel used to leave the DOM, so an FAQ&#39;s answers were missing from a server render and from a crawler&#39;s index, and the browser&#39;s find-on-page could not reach them. A closed panel is now <code>hidden=&quot;until-found&quot;</code>: out of sight, the tab order and the accessibility tree as before, and opened when a page search lands in it. A test that asserted a closed panel&#39;s text is absent should assert it is not visible, and a panel too expensive to build while closed can pass <code>hiddenUntilFound={false}</code>. <code>Tabs</code> keeps its default; its docs now say to set <code>keepMounted</code> on a panel whose content should be indexed.</p></li><li><p><strong>A <code>NavigationMenu</code> panel&#39;s links are in the page before it opens.</strong> A panel was created only when a pointer or a key first opened it, so its links were missing from a server render and a crawler never followed them, although the component is the one meant to put a site&#39;s destinations in a crawler&#39;s index. Each <code>NavigationMenuItem</code> now renders its panel hidden from the start, and <code>keepMounted={false}</code> restores the old behaviour for a panel that is expensive to build. A test that asserted a panel link is absent before opening should assert it is not visible.</p></li><li><p><strong>A <code>Pagination</code> stepper keeps the focus when the press runs it out.</strong> Pressing Next onto the last page made the button <code>disabled</code>, and with <code>getPageHref</code> and <code>onPageChange</code> it swapped the link for a button, so either way the focus fell to the document and a keyboard reader started again from the top. A stepper with nowhere to go, and the current page, stay where they are with <code>aria-disabled</code>; a link loses only its <code>href</code>. A test that asserted <code>toBeDisabled()</code> on such a stepper should assert <code>aria-disabled=&quot;true&quot;</code>, and with <code>getPageHref</code> the current page and an exhausted stepper are found by the <code>link</code> role.</p></li><li><p><strong>A <code>Carousel</code> arrow keeps the focus when the press runs it out.</strong> With <code>loop={false}</code>, pressing Next onto the last slide made the arrow <code>disabled</code>, which dropped the focus onto the document, and the blur that caused restarted an auto-playing carousel the focus had paused. The arrow stays in place with <code>aria-disabled</code>; a test that asserted <code>toBeDisabled()</code> on it should assert <code>aria-disabled=&quot;true&quot;</code>.</p></li><li><p><strong>A <code>ScrollZone</code> button keeps the focus when the press reaches the end.</strong> The button with nowhere to go became <code>disabled</code>, or under <code>buttons=&quot;auto&quot;</code> with overlaid buttons was removed outright, and either way the focus fell to the document. It is now <code>aria-disabled</code>, and an overlaid one stays until the focus leaves it. A test that asserted <code>toBeDisabled()</code> on it should assert <code>aria-disabled=&quot;true&quot;</code>.</p></li><li><p><strong><code>HowToSteps</code>&#39; Previous and Next keep the focus at the ends.</strong> Pressing Previous onto the first step, or Next onto the last with <code>completion={false}</code>, made the button <code>disabled</code> and dropped the focus onto the document. Both stay in place with <code>aria-disabled</code>; a test that asserted <code>toBeDisabled()</code> on them should assert <code>aria-disabled=&quot;true&quot;</code>.</p></li><li><p><strong>A confirm is an alert dialog that opens a destructive question on Cancel.</strong> <code>useConfirm</code> rendered a plain <code>role=&quot;dialog&quot;</code> and put the focus on the confirming button whatever was asked, so for <code>color: &#39;danger&#39;</code> the Enter that raised the question was one more Enter from the loss. The sheet is <code>role=&quot;alertdialog&quot;</code> now, a <code>danger</code> question opens on the cancelling button, and the focus goes back to what asked once the last queued question is answered; it could be lost there before, because the confirming button took the focus as the sheet mounted. A test that found the sheet by the <code>dialog</code> role finds it by <code>alertdialog</code>.</p></li><li><p><strong>A <code>CodeBlock</code> scrolls as a region only when there is something to scroll.</strong> Every block was a named <code>role=&quot;region&quot;</code> and a tab stop, so a page of thirty short snippets had thirty landmarks and thirty stops on the way through it. The box is measured now, and only code wider or taller than it is a focusable, named region. A test that found a short block by the <code>region</code> role finds it by its text or its toolbar.</p></li><li><p><strong>A captioned <code>Gallery</code> tile is named by its caption, and a hover caption shows on a touch screen.</strong> A tile&#39;s button was named &quot;alt — Image 1 of 12&quot; whatever was written on it, so a voice-control user saying the caption they could see pressed nothing, and <code>caption=&quot;hover&quot;</code> never appeared on a device that cannot hover. A tile with a caption drawn is now named by its title and its place in the set, described by its description, and a hover caption is always up where there is no hover. A tile with no caption keeps the <code>alt</code>; a test that found a captioned tile by its <code>alt</code> finds it by its title.</p></li><li><p><strong>A <code>DataTable</code> without a selection can be worked from the keyboard.</strong> With <code>selectionMode=&quot;none&quot;</code> the table took no focus, so <code>onRowActivate</code> answered only a double-click and an editable cell opened only on one. A table whose rows open something or whose cells edit is now a <code>grid</code> with a tab stop: the arrows move an active row without choosing it, Enter calls <code>onRowActivate</code>, and F2 edits the first editable cell of the row, in every selection mode. Enter and Escape in an editor hand the focus back to the table. A test that found such a table by the <code>table</code> role finds it by <code>grid</code>.</p></li><li><p><strong><code>AnimateCounter</code> no longer takes <code>paused</code>.</strong> It came with the props every <code>Animate*</code> shares, and a count ignored it: nothing held the number, so <code>paused</code> type-checked and did nothing. It is gone from the type; hold a count with <code>trigger=&quot;manual&quot;</code> and <code>play</code>. A counter that passed <code>paused</code> fails to compile and can drop it with no change in behaviour.</p></li><li><p><strong><code>AnimateLighting</code> no longer takes <code>easing</code>.</strong> The arc travels on a pseudo-element at a constant rate, so <code>easing</code> type-checked and changed nothing. It is gone from the type; a lighting that passed it fails to compile and can drop it with no change in behaviour. The props tables of <code>AnimateScramble</code>, <code>AnimateTyping</code> and <code>AnimateHeadline</code> also stop listing <code>easing</code> and <code>alternate</code> where their types never took them, and <code>repeat</code> on a Scramble and a Headline is described as what it actually decides.</p></li><li><p><strong>A <code>Skeleton</code> given a <code>label</code> says it.</strong> A labelled skeleton was a <code>role=&quot;status&quot;</code> with <code>aria-busy=&quot;true&quot;</code> and the label as its <code>aria-label</code>. <code>aria-busy</code> tells a screen reader to hold a region&#39;s changes back until it clears, and a live region is announced by what it holds rather than by its name, so neither half reached the reader. The status now holds the label as visually hidden text and carries no <code>aria-busy</code>. A test that found it with <code>getByRole(&#39;status&#39;, { name })</code> should look for the text inside it.</p></li><li><p><strong>An <code>Overlay</code> with no <code>label</code> is named &quot;Loading&quot;.</strong> The default name was the word &quot;Overlay&quot;, so the common case — a sheet holding only a spinner — was announced as &quot;Overlay, dialog&quot;, which says nothing about why the page went out of reach. The default is the locale&#39;s word for &quot;Loading&quot; now, in all eighteen registered languages. A test that found an unlabelled overlay by the name &quot;Overlay&quot; should use &quot;Loading&quot;, and an overlay that means something else should pass <code>label</code>.</p></li><li><p><strong>A labelled <code>GaugeChart</code> is a <code>meter</code>.</strong> It was a <code>role=&quot;img&quot;</code> named &quot;label: value / max&quot;, so a dial from <code>-50</code> to <code>50</code> reading <code>0</code> was announced as &quot;0 / 50&quot; and its <code>caption</code> was never read. It is now <code>role=&quot;meter&quot;</code> named by <code>label</code>, with <code>aria-valuenow</code>, <code>aria-valuemin</code> and <code>aria-valuemax</code>, and an <code>aria-valuetext</code> of the reading followed by a text caption. A dial with no reading stays a <code>role=&quot;img&quot;</code> named by its label. A test that found the dial with <code>getByRole(&#39;img&#39;, { name: &#39;CPU: 64 / 100&#39; })</code> finds it with <code>getByRole(&#39;meter&#39;, { name: &#39;CPU&#39; })</code>.</p></li><li><p><strong>A link whose address uses a scheme other than <code>http</code>, <code>https</code>, <code>mailto</code> or <code>tel</code> is written with no <code>href</code>.</strong> A ChatBubble&#39;s <code>preview.url</code> and every other <code>href</code> a component takes went into the DOM as they were, and React 18, which the peer range includes, writes a <code>javascript:</code> URL unchanged, so a preview built from another user&#39;s message could run a script when pressed. <code>TextLink</code>, <code>MenuItem</code>, <code>NavigationMenuLink</code>, <code>NavigationMenuItem</code>, <code>ListItem</code>, <code>TreeItem</code>, <code>BreadcrumbItem</code>, <code>BottomNavigationItem</code>, <code>AppLogo</code>, <code>Anchor</code>, <code>Pagination</code>&#39;s <code>getPageHref</code> and <code>ChatBubble</code>&#39;s preview now pass it through one check, which reads the scheme the way a browser does. A relative address, a <code>#fragment</code> and a <code>//host</code> are kept. An address with another scheme is dropped: a component that switches between a link and a button draws the button, and one that is always a link draws an <code>&lt;a&gt;</code> with no <code>href</code>. A link to a custom scheme such as <code>sms:</code> or an app&#39;s own should be written as a plain <code>&lt;a&gt;</code> through <code>render</code>.</p></li><li><p><strong>A <code>FloatingActionButton</code>&#39;s <code>className</code> and <code>style</code> go on the button.</strong> <code>className</code> landed on the box pinned to the corner, so a class meant for the button styled the invisible frame around it, and the button&#39;s own inline radius had nothing a caller could merge with. Both now go on the button, with <code>style</code> merged over the round radius instead of replacing it, and the box in the corner takes its classes through the new <code>classNames.frame</code>. A caller who positioned or spaced the box with <code>className</code> should move those classes to <code>classNames.frame</code>; every other <code>&lt;div&gt;</code> attribute still goes to the box.</p></li><li><p><strong>A chart axis&#39;s <code>tickFormat</code> returns a string or a number.</strong> It was typed to return any <code>ReactNode</code>, but a tick is SVG text and the result went through <code>String()</code>, so a callback that handed back an element type-checked and drew <code>[object Object]</code> on the axis. The type now says what the axis can write. A <code>tickFormat</code> returning JSX stops compiling, and should return the text instead.</p></li><li><p><strong>An <code>Image</code> and a <code>Stack</code> draw only <code>&lt;span&gt;</code> elements, so they can sit inside a <code>&lt;p&gt;</code>.</strong> An Image drew its proportion box and its loading placeholder as <code>&lt;div&gt;</code>s, and a Stack&#39;s root was a <code>&lt;div&gt;</code>, although a Markdown renderer puts an Image inside a paragraph and a pile of avatars in a line of text is what a Stack is for. A <code>&lt;div&gt;</code> inside a <code>&lt;p&gt;</code> is invalid markup, which the browser rewrites and React reports as a hydration error. Both now draw spans with the same display they had. A <code>Stack</code>&#39;s <code>ref</code> is an <code>HTMLSpanElement</code> and its props are a <code>&lt;span&gt;</code>&#39;s, so code that typed the ref as <code>HTMLDivElement</code> needs the new type.</p></li><li><p><strong><code>AnimateTyping</code>, <code>AnimateHeadline</code> and <code>AnimateMarquee</code> render a <code>&lt;span&gt;</code> and take <code>render</code>.</strong> Each rendered a <code>&lt;div&gt;</code>, so none could sit inside a <code>&lt;p&gt;</code> without invalid markup and a hydration error, and a headline being typed or turned could not be a heading. Each now renders a <code>&lt;span&gt;</code> with the display it had, and <code>render</code> puts it on another element: <code>&lt;AnimateHeadline render={&lt;h2 /&gt;}&gt;</code>. The <code>ref</code> of all three is an <code>HTMLElement</code> and their props are a <code>&lt;span&gt;</code>&#39;s, so code that typed the ref as <code>HTMLDivElement</code> needs the new type.</p></li><li><p><strong>A <code>DateRangePicker</code> reports a range only once it has both ends, and keeps the old one until then.</strong> The first press called <code>onValueChange</code> with <code>{ start, end: null }</code>, so the range that had been chosen was gone as soon as a new start was pressed, and closing the popup before the second press left the half range as the value, although the docs said it was thrown away. The first press now only marks the start, the value changes once when the second end is chosen, and closing early keeps the range that was there. A caller that acted on the half-range call gets no call until the range is complete.</p></li><li><p><strong>A <code>Badge</code> with no <code>content</code> draws its dot.</strong> Leaving <code>content</code> out was documented to draw a dot, and the dot was drawn with <code>invisible</code> and <code>aria-hidden</code>, so a status marker such as <code>&lt;Badge color=&quot;success&quot;&gt;&lt;Avatar /&gt;&lt;/Badge&gt;</code> never appeared. It is now a visible dot, hidden from a screen reader only when it has no <code>label</code> to read. A <code>content</code> of <code>0</code> is still hidden unless <code>showZero</code> is passed, and a caller who relied on an empty Badge drawing nothing should pass <code>invisible</code>.</p></li><li><p><strong>An <code>Image</code> with <code>preview</code> puts <code>className</code> and <code>style</code> on the button.</strong> They landed on the picture inside it, so the button spanned the whole line beside a smaller picture and pressing the empty space opened the preview. A caller who styled that inner element with <code>preview</code> on now styles the button; a frame&#39;s own look stays on <code>classNames.frame</code>.</p></li></ul><h3 id="where-the-bytes-went" tabindex="-1">Where the bytes went <a class="header-anchor" href="#where-the-bytes-went" aria-label="Permalink to &quot;Where the bytes went&quot;">​</a></h3><table tabindex="0"><thead><tr><th>What you import</th><th>1.13.0</th><th>1.14.0</th></tr></thead><tbody><tr><td><code>Button</code></td><td>5.2 kB</td><td>5.2 kB</td></tr><tr><td><code>Chip</code></td><td>3.4 kB</td><td>3.5 kB</td></tr><tr><td><code>LineChart</code></td><td>11.8 kB</td><td>12.7 kB</td></tr><tr><td><code>CodeBlock</code></td><td>5.1 kB</td><td>5.6 kB</td></tr><tr><td><code>Image</code></td><td>7.1 kB</td><td>8.9 kB</td></tr><tr><td><code>Gallery</code></td><td>10.4 kB</td><td>11.8 kB</td></tr><tr><td>a whole page shell</td><td>29.2 kB</td><td>30.0 kB</td></tr><tr><td>12 components — a typical app</td><td>71.0 kB</td><td>71.6 kB</td></tr><tr><td>12 components, with Korean</td><td>73.8 kB</td><td>75.0 kB</td></tr><tr><td>25 components — a large one</td><td>116.9 kB</td><td>118.5 kB</td></tr><tr><td>all exports</td><td>265.8 kB</td><td>279.8 kB</td></tr></tbody></table><p><code>Image</code> and <code>Gallery</code> are the rows that moved most. 1.5 kB of <code>Image</code>&#39;s is the props above, all of it in <code>Image</code> itself: the quarter-turn layout and its preview box, the <code>position</code> reader that follows a turn and a mirror, the blurred letterbox, the picture stand-in and its object URL. <code>Gallery</code> carries 1.0 kB of that because it draws an <code>Image</code>, and most of the rest of its growth is a tile named by its caption and answering the keyboard focus the way it answers the pointer.</p><p><code>LineChart</code> is the chart frame and <code>internal/chart.ts</code> doing more, and every chart carries both: a tap pins the tooltip, the left axis is named above the plot, the plot is described in one sentence, <code>height</code> takes any CSS length, a hidden series is remembered by name, and a full-stacked chart writes its numbers through <code>format</code>.</p><p><code>CodeBlock</code> checks whether it scrolls before it becomes a region, tries a highlighter again after a failed load, and fetches the grammars a language embeds.</p><p>Registering a language costs 3.4 kB now against 2.8 kB. Words that were hardcoded English are in every language&#39;s module: those of a <code>Combobox</code>, a <code>ToastProvider</code> and the chart tables, a <code>FilePicker</code>, each <code>OtpField</code> slot, a <code>Panes</code> handle, a <code>Timeline</code> step, a <code>WindowPane</code>&#39;s resize corner, an unlabelled <code>Overlay</code>, a failed <code>Image</code> and a <code>DataTable</code> row with no group.</p><p>All exports grew 14.0 kB, and no dependency was added. Measured one module at a time, the largest shares are <code>Image</code> at 1.7 kB, the chart frame and its arithmetic at 1.4 kB, <code>DataTable</code> at 0.8 kB, and the calendar, <code>WindowPane</code> and <code>Sidebar</code> at 0.4 kB each. The rest is a few hundred bytes on each component the fixes below touch.</p><p><code>neba/styles.css</code> is 1.8 kB larger, 24.1 kB gzipped. It carries the classes the new props and fixes use, <code>CodeBlock</code>&#39;s <code>auto</code> tokens written out for each theme root, the forced-colour rules and the larger hit area on small controls.</p><h3 id="added" tabindex="-1">Added <a class="header-anchor" href="#added" aria-label="Permalink to &quot;Added&quot;">​</a></h3><ul><li><p><strong><code>ProgressLinear</code>, <code>ProgressCircular</code> and <code>Meter</code> take <code>locale</code>, and fall back to a provider&#39;s.</strong> The value was written in the runtime&#39;s language, which a server and a browser do not always share, and there was no typed way to say otherwise. <code>locale</code> is passed to Base UI&#39;s formatter now, and a <code>NebaProvider</code>&#39;s <code>locale</code> is used when it is left out. A <code>Meter</code> also announces the reading it draws: its <code>aria-valuetext</code> came from Base UI&#39;s own formatting, which in some languages put a space before the <code>%</code> the text beside it does not have.</p></li><li><p><strong><code>play</code> takes a number on every <code>Animate*</code>.</strong> A number that changes starts the effect over, the way <code>false</code> → <code>true</code> does, and <code>0</code> is stopped, so a count of failed attempts replays an <code>AnimateShake</code> on every failure without remounting what is inside it. The docs used a fresh <code>key</code> for that, which took the focus away from the field that had just failed.</p></li><li><p><strong><code>play</code> takes a number on every <code>Animate*</code>.</strong> A number that changes starts the effect over, the way <code>false</code> → <code>true</code> does, and <code>0</code> is stopped, so a count of failed attempts replays an <code>AnimateShake</code> on every failure without remounting what is inside it. The docs used a fresh <code>key</code> for that, which took the focus away from the field that had just failed.</p></li><li><p><strong><code>ScrollZone</code> takes <code>scrollerRef</code>.</strong> It is a ref to the box that scrolls, which is what a caller restoring a scroll position needs. <code>ref</code> stays on the root, which also holds the buttons.</p></li><li><p><strong><code>Image</code> turns and mirrors a picture with <code>rotate</code> and <code>flip</code>.</strong> <code>rotate</code> takes quarter turns, clockwise, and <code>flip</code> mirrors along the axes the picture is shown on, so <code>flip=&quot;horizontal&quot;</code> swaps left and right whether or not the picture was turned. A picture on its side reserves a box on its side: <code>width</code> and <code>height</code> still describe the file, and with neither the box takes the turned shape once the file arrives. The preview opens turned and mirrored the same way.</p></li><li><p><strong><code>fit</code> takes <code>scale-down</code> on <code>AspectRatio</code> and <code>Image</code>.</strong> It is <code>contain</code> that never enlarges, for a file that may be smaller than the box it is given.</p></li><li><p><strong><code>Image</code> takes <code>position</code>.</strong> It is <code>object-position</code>: which part of the picture a <code>cover</code> crop keeps, and where <code>contain</code>, <code>none</code> and <code>scale-down</code> leave their empty space. It takes a side, a corner, <code>center</code> or two percentages, and is read on the picture as it is shown, so <code>position=&quot;top&quot;</code> keeps the top the reader sees through <code>rotate</code> and <code>flip</code>.</p></li><li><p><strong><code>Image</code> takes <code>letterbox</code>.</strong> It fills the part of the box <code>contain</code>, <code>none</code> or <code>scale-down</code> leave empty: <code>blur</code> lays the same picture behind it, covering the box and blurred, and any other string is a CSS <code>background</code>. The blurred copy is the same file rather than a second download, follows <code>position</code>, <code>rotate</code>, <code>flip</code> and <code>filter</code>, and is only drawn under a <code>fit</code> that can leave space.</p></li><li><p><strong>An <code>Image</code>&#39;s <code>placeholder</code> can be a picture.</strong> <code>placeholder={{ src }}</code> stands a URL, a data URI or a <code>Blob</code> in for the file while it arrives, drawn with the same <code>fit</code>, <code>position</code>, <code>rotate</code> and <code>flip</code>, and <code>blur</code> softens a copy stretched up from a few pixels. A Blob is shown through an object URL that is released when the stand-in goes. The picture fades in over the stand-in, which is taken away once that fade has run.</p></li><li><p><strong><code>Image</code> takes <code>priority</code>.</strong> It is for the picture a page is judged by, usually its Largest Contentful Paint: <code>loading=&quot;eager&quot;</code> and a high fetch priority together, with an attribute written out still winning. <code>fetchpriority</code> is spelled the way the installed React accepts it, so neither React 18 nor React 19 warns.</p></li><li><p><strong><code>Gallery</code> passes the new <code>Image</code> props on.</strong> An item takes <code>rotate</code>, <code>flip</code>, <code>position</code> and a <code>placeholder</code>, and the Gallery takes <code>fit</code>, <code>letterbox</code> and <code>loading</code> for every tile. An item&#39;s <code>ratio</code> stays the file&#39;s own: a turned item is laid out on its side by <code>masonry</code> and <code>justified</code>, and opens turned in the viewer.</p></li><li><p><strong><code>TreeItem</code> takes <code>selectable</code>.</strong> A row with <code>selectable={false}</code> is never chosen, and pressing it only opens and shuts its branch — the folder in a tree whose answers are the files.</p></li><li><p><strong><code>ToastProvider</code> takes <code>label</code>, <code>Tour</code> takes <code>closeLabel</code> and <code>Combobox</code> takes <code>openLabel</code>.</strong> Each overrides a word the component otherwise takes from its <code>locale</code>: the name of the region the toast stack lives in, the × that ends a tour, and the chevron of a combobox with no string label.</p></li><li><p><strong><code>Button</code> and <code>IconButton</code> take <code>focusableWhenDisabled</code>.</strong> A disabled button stays in the tab order, marked <code>aria-disabled</code> rather than with the <code>disabled</code> attribute, and still cannot be pressed. It is for a button that is disabled by its own press, which would otherwise hand the focus to the document.</p></li><li><p><strong><code>Slider</code> takes <code>getAriaLabel</code> and <code>getAriaValueText</code>.</strong> The first names each thumb by its index, so a range slider&#39;s two thumbs are no longer both read by the label; the second words a thumb&#39;s value, for a unit or in place of the English Base UI reads for a range.</p></li><li><p><strong><code>Dialog</code> takes <code>initialFocus</code> and <code>finalFocus</code>.</strong> Base UI&#39;s own options, passed to the popup: what takes the focus when the dialog opens, and where it goes when it closes.</p></li><li><p><strong><code>FilePicker</code> takes <code>locale</code>.</strong> The sentence inside the box and the name of each remove button were English whatever the page spoke, with only a prop per string as the way out. Both come from the new <code>file</code> namespace now, which every registered language has; <code>title</code> and <code>removeLabel</code> still override them.</p></li><li><p><strong><code>Toolbar</code>, <code>Pill</code>, <code>Footer</code>, <code>FloatingActionButton</code> and <code>Drawer</code> take <code>safeArea</code>.</strong> On a page laid out under a phone&#39;s notch and home indicator with <code>viewport-fit=cover</code>, a bar or button pinned to the edge of the screen sat underneath them. With <code>safeArea</code> on, the default, a <code>fixed</code> or <code>sticky</code> one adds the screen&#39;s <code>env(safe-area-inset-*)</code> on the edge it is held against, and an overlay <code>Drawer</code> keeps its contents inside the insets on the edges it runs to; the sheets still reach the edge of the screen. A <code>static</code> one is left alone.</p></li><li><p><strong><code>PageLayout</code> takes <code>main</code>.</strong> With <code>main={false}</code> the content goes in a plain <code>&lt;div&gt;</code> rather than a <code>&lt;main&gt;</code>, with no <code>id</code> and no skip link, for a layout that is not the page — an app shell previewed inside the page&#39;s own layout. A <code>PageLayout</code> on a <code>Mockup</code>&#39;s screen is off unless it passes <code>main</code>, so a device preview no longer gives the document a second <code>&lt;main&gt;</code>, a second <code>id=&quot;main&quot;</code> and a second skip link.</p></li><li><p><strong>A <code>Panes</code> handle is named, and says which panes it resizes.</strong> A handle was a <code>role=&quot;separator&quot;</code> with a value and nothing else, so a screen reader announced &quot;separator, 50&quot; with no word for what the number was the share of. Each handle is now named by the new <code>panes</code> namespace&#39;s &quot;Resize panes&quot;, in all eighteen registered languages, and its <code>aria-controls</code> names the pane on either side; a pane without an <code>id</code> is given one. <code>handleLabel</code> takes a name of its own, as a string or as a function of the handle&#39;s index, and <code>locale</code> picks the language.</p></li><li><p><strong><code>Table</code> takes <code>maxHeight</code>, and <code>stickyHeader</code> sticks under it.</strong> A Table&#39;s sheet scrolls sideways, which makes it the box its sticky header sticks to, so limiting the height on a box around the table, as the docs said to, scrolled that outer box instead and the header scrolled away with the rows. <code>maxHeight</code> limits the sheet itself, as a number of pixels or any CSS length, so the rows scroll inside it and a <code>stickyHeader</code> stays at its top.</p></li><li><p><strong><code>BreadcrumbItem</code> takes <code>render</code> and <code>target</code>.</strong> A step with an <code>href</code> could only be an <code>&lt;a&gt;</code>, so every step in an app with a router reloaded the page. <code>render={&lt;Link to=&quot;/projects&quot; /&gt;}</code> draws the router&#39;s link with <code>href</code> passed through, and a <code>target</code> other than this tab gets <code>rel=&quot;noopener noreferrer&quot;</code>.</p></li></ul><h3 id="changed" tabindex="-1">Changed <a class="header-anchor" href="#changed" aria-label="Permalink to &quot;Changed&quot;">​</a></h3><ul><li><p><strong>A chart no longer renders again when the pointer moves inside the mark or column it is already on.</strong> The index under the pointer was set on every <code>pointermove</code> and left to React to discard, and the first repeat after a real change still rendered the whole chart once.</p></li><li><p><strong>An <code>Image</code> reports <code>loading</code> when it asks for a file.</strong> <code>onLoadingStatusChange</code> was typed with <code>loading</code> and only ever called with <code>loaded</code> or <code>failed</code>, so a caller could not tell a file on its way from one that had not been asked for. It is called with <code>loading</code> on mount and whenever <code>src</code> changes, then with how the file settled. A handler written for the two endings is now also called once before them.</p></li><li><p><strong>A <code>NebaProvider</code> with inline <code>defaults</code> no longer re-renders everything under it.</strong> <code>defaults={{ size: &#39;sm&#39; }}</code> is a new object on every render of the component around the provider, and it was handed to the context as it came, so every Neba component below re-rendered with the page even when nothing had changed. The four values are kept by value now.</p></li><li><p><strong>Dragging a run of <code>DataTable</code> rows reports the selection once per row it reaches.</strong> Every <code>pointermove</code> committed the run again, so a drag held still inside one row called <code>onSelectedChange</code> and re-rendered the table on every frame the pointer shook.</p></li><li><p><strong>A grouped <code>DataTable</code> finds each row&#39;s place on the page in constant time.</strong> Every render searched the page once per row of every group, which in a five-thousand-row grouped table without virtualisation made a single selection tens of millions of comparisons.</p></li><li><p><strong>A controlled <code>TreeView</code> re-renders its rows only when something in the tree changed.</strong> Its toggle and select handlers were rebuilt whenever the <code>expanded</code> or <code>selected</code> array or an inline callback was, which is every render in the usual way of writing a controlled tree, and each rebuild re-rendered every row.</p></li><li><p><strong><code>ScatterChart</code> and <code>TimelineChart</code> stop re-rendering for every pixel the pointer moves.</strong> Both stored the pointer&#39;s offset on each move for a tooltip mode that only a chart of columns reads, so hovering over one mark laid the whole chart out again for every pixel of travel. They re-render when the mark under the pointer changes.</p></li><li><p><strong>A chart formats, cuts and measures its category labels once rather than on every hover.</strong> Moving the pointer across a <code>LineChart</code>, <code>AreaChart</code> or <code>BarChart</code> re-renders it for each column crossed, and every one of those re-renders ran every label through <code>tickFormat</code>, the truncation and the width estimate again, which was most of the time spent on a plot of ten thousand dates. The widest label is also found without spreading the list into <code>Math.max</code>, which threw a <code>RangeError</code> past about a hundred thousand of them.</p></li><li><p><strong><code>BarChart</code> finds a series&#39; high and low once for <code>valueLabels=&quot;extremes&quot;</code>.</strong> It walked the whole series again for every bar, so a five-hundred-bar series did a quarter of a million comparisons on each render, and a hovered chart renders on every column the pointer crosses. It now asks the same question the line charts already asked once.</p></li><li><p><strong><code>CodeBlock</code> trims the end of its code in linear time.</strong> The trailing whitespace was found with a pattern that retried every run of spaces from each position in it, so a pasted file with a long run of spaces in the middle held the page for seconds — forty thousand of them took 2.6 seconds — every time <code>code</code> changed.</p></li><li><p><strong>An open <code>Tour</code> re-renders only when its target moves.</strong> It listens to scrolling anywhere in the page, and every scroll frame handed it a new measurement even when the target had not moved, so scrolling a side panel re-rendered the tour on every frame.</p></li><li><p><strong><code>AnimateTyping</code> and <code>AnimateHeadline</code> attach a callback <code>ref</code> once.</strong> Both wrote their ref inline, so a caller&#39;s callback ref was called with <code>null</code> and then with the node again on every render — once per character typed, and once per line turned. <code>AnimateMarquee</code> already held its ref, and the other two do the same now.</p></li><li><p><strong>An endless animation holds while it is off the screen.</strong> An <code>Animate*</code> component with <code>repeat=&quot;infinite&quot;</code> — <code>AnimateLighting</code>, <code>AnimateMarquee</code>, <code>AnimateFloat</code>, <code>AnimateBlink</code> and the rest, and a <code>transition=&quot;blink&quot;</code> — kept its animation, timer or paint running for as long as it was mounted, however far below the fold it sat. It now pauses when it leaves the viewport and carries on from where it was when it comes back, through one shared observer. A finite animation is untouched and still finishes on schedule.</p></li><li><p><strong>A <code>Pill</code> with <code>details</code> no longer forces a layout on every render of its parent.</strong> The details were written inline in the usual case, a new object each time, and each one re-subscribed the height observer and read the panel&#39;s height again; the observer already follows the details as they change.</p></li><li><p><strong><code>AnimateHeadline</code> holds its line under a reduced-motion preference.</strong> It already dropped the slide, but the reel went on changing every few seconds for as long as it was on the page, which is still motion and never ends. It now stays on the line it is showing; a controlled <code>index</code> still changes it. The docs of every <code>Animate*</code> that keeps moving past five seconds now say to pair it with a pause button bound to <code>paused</code>, and <code>transition=&quot;blink&quot;</code> is documented as the one <code>transition</code> that repeats.</p></li><li><p><strong>On a <code>HeatmapChart</code> grid, <code>↑</code> and <code>↓</code> keep the column and change the row.</strong> Both vertical arrows did what the horizontal ones did and walked the cells row by row, so following one hour down a week of rows took a key press per cell in between. They now move to the same column in the row above or below, skipping a row whose cell there is a gap. A treemap has no columns, so every arrow still walks its tiles from the largest to the smallest, which the docs now say.</p></li><li><p><strong>A chart&#39;s plot is described by one sentence rather than by its whole table.</strong> The plot&#39;s <code>aria-describedby</code> pointed at the visually hidden data table, so a screen reader read every number in the chart each time the plot took the focus — three hundred and sixty-five of them on a year of daily points. It now points at a hidden sentence with the number of values and the lowest and highest of them (&quot;Data points: 7. Range: 5 to 40.&quot;), and a <code>TimelineChart</code> counts its spans and names when the first starts and the last ends. The table is still there to be walked. The words are the <code>chart</code> namespace&#39;s new <code>summary</code>, in all eighteen registered languages.</p></li><li><p><strong>Under a reduced-motion preference an <code>AnimateMarquee</code> lays its content down once and lets it wrap.</strong> The strip stopped, but its <code>overflow: hidden</code> and its copies stayed, so long content was cut off at the edge for good and short content sat on the page two or four times over. It now renders a single copy, stops clipping and wraps the content onto more lines, and the stylesheet does the same in the first paint, before the component has read the preference.</p></li><li><p><strong>A <code>MenuSubmenu</code> opens towards the row&#39;s inline end.</strong> Its <code>side</code> defaulted to <code>&#39;right&#39;</code> and its chevron was always turned to point right, so under RTL the submenu opened over the menu it came from and the chevron pointed away from it. Left out, <code>side</code> is now the inline end, to the left under RTL, and the chevron follows the same direction Base UI places the popup by. A <code>side</code> passed explicitly is unchanged.</p></li><li><p><strong>A <code>DataTable</code> export writes a text cell a spreadsheet would run with a <code>&#39;</code> in front.</strong> A cell starting with <code>=</code>, <code>+</code>, <code>-</code>, <code>@</code>, a tab or a carriage return went into the CSV as it was, so a name a user had typed as <code>=HYPERLINK(...)</code> ran as a formula in whatever spreadsheet opened the file. Such text is now written as <code>&#39;=HYPERLINK(...)</code>, which a spreadsheet shows as text. Numbers are untouched. <code>exportEscapeFormulas={false}</code> writes the cells as they are, for a file no spreadsheet opens.</p></li><li><p><strong>A vertical <code>Menubar</code> or <code>NavigationMenu</code> opens its popups beside the bar.</strong> With <code>orientation=&quot;vertical&quot;</code> a <code>Menubar</code>&#39;s menus still hung below the word that opened them, and a <code>NavigationMenu</code>&#39;s panels below their item, so each popup covered the next word or item down the rail. Both now open at the bar&#39;s inline end: to the right, or to the left under RTL. A horizontal bar is unchanged.</p></li><li><p><strong>An empty <code>TimePicker</code> writes a picked hour on the hour.</strong> With no <code>value</code> and no <code>referenceDate</code>, the time was written onto the moment the picker mounted, so at 15:42:17 pressing the hour <code>9</code> gave 09:42:17, and the seconds stayed even with <code>showSeconds</code> off. The default <code>referenceDate</code> is now the start of today, so the same press gives 09:00:00. A <code>referenceDate</code> passed explicitly is unchanged.</p></li><li><p><strong>A nested <code>NebaProvider</code> builds on the one around it.</strong> An inner provider replaced the outer <code>defaults</code> whole, so <code>defaults={{ density: &#39;compact&#39; }}</code> inside <code>defaults={{ size: &#39;sm&#39; }}</code> lost the small size; one with no <code>direction</code> reset Base UI&#39;s direction to left-to-right inside a right-to-left tree; and every provider wrote its scheme onto <code>&lt;html&gt;</code>, so a nested preview fought the page&#39;s own toggle, although the guide recommends nesting. An inner provider&#39;s <code>defaults</code> are now merged over the outer ones, a missing <code>direction</code> is inherited, and only the outermost provider writes the scheme to <code>&lt;html&gt;</code>. A nested provider still writes it to the element its <code>colorSchemeElement</code> names.</p></li><li><p><strong>A chart with more than eight series warns that its colours repeat.</strong> The palette&#39;s eight slots are handed out by index and start again at the ninth series, while the docs said they never cycled, and nothing on screen told a reader that the ninth line was not the first one again. A development build now says so once in the console, naming how many series the chart was given, and the docs say the slots repeat. A production build says nothing.</p></li><li><p><strong>A <code>BottomNavigation</code> with <code>labels=&quot;selected&quot;</code> keeps its glyphs still as the selection moves.</strong> An unselected name was clipped to a pixel and took no line, so an item was a line shorter while it was not selected, and each glyph moved down as the selection left its item and up as it came back. Every item now keeps the line its name takes, with the name drawn only on the current one and still read out on the rest. A <code>labels=&quot;none&quot;</code> bar is unchanged.</p></li><li><p><strong>A <code>DateRangePicker</code> starts a new range at a day before the start, as a <code>Calendar</code> does.</strong> With a start chosen, pressing an earlier day sorted the two and made the first press the end of the range, while a <code>Calendar</code> in <code>range</code> mode took the same press as a new start. The picker now starts over at the earlier day and waits for the end, so the one gesture means the same thing in both.</p></li><li><p><strong>A <code>DateTimePicker</code> or <code>TimePicker</code> holds a chosen value to its bounds.</strong> A row stays pressable when its span only overlaps the bounds, so pressing it could commit a value outside them: with a <code>minTime</code> of 09:30 and a value of 10:15, the hour <code>9</code> gave 09:15, and choosing the day a <code>minDate</code> falls on, with no time yet, gave midnight. A value outside the bounds is now moved to the nearest bound before it is committed, so those presses give 09:30.</p></li><li><p><strong>A controlled <code>Anchor</code> still reports the heading the reader is in, and a page with nothing to scroll starts on its first heading.</strong> Passing <code>activeHref</code> switched the scroll tracking off, so <code>onActiveChange</code>, which the docs pair with it, never fired and a controlled caller had nothing to set <code>activeHref</code> from. On a page too short to scroll, the rule that marks the last heading at the bottom of the page already held on the first frame, so the last heading was marked on a page that had just opened at the first. Tracking now runs in both modes, and when nothing scrolls the first heading is marked once it is in the document.</p></li><li><p><strong><code>Highlight</code> ignores accents in a string <code>query</code>.</strong> <code>jose</code> marks <code>José</code>, as a DataTable&#39;s search finds it; before, a DataTable found the row and a Highlight in it marked nothing. The match is marked in the original text, so a decomposed accent stays inside the mark. A RegExp is still matched as written.</p></li></ul><h3 id="fixed" tabindex="-1">Fixed <a class="header-anchor" href="#fixed" aria-label="Permalink to &quot;Fixed&quot;">​</a></h3><ul><li><p><strong>A bounded <code>DataTable</code> brings a row fully on screen when its edge falls between pixels.</strong> The arrow keys measured the row with a rounded <code>offsetHeight</code> and set a fractional scroll offset, which WebKit truncates, so a row of 32.2px could stay more than a pixel under the edge of the box.</p></li><li><p><strong>A chart&#39;s axis name above the plot stays inside the chart&#39;s box in every font.</strong> Its baseline sat one em below the top edge, and a font with a taller ascent, such as the Noto Sans many Linux systems use, reached past the edge the SVG clips at.</p></li><li><p><strong><code>ScatterChart</code> grows the mark under the crosshair on React 18.</strong> The pixel it grows by was a unitless number in a <code>style</code>, and React 18 wrote it as a length, which the browser discarded, so the mark stayed the same size.</p></li><li><p><strong><code>AnimateTyping</code> and <code>AnimateScramble</code> hold the size of their final text from the first frame.</strong> A typewriter&#39;s box was only as big as the characters that had arrived, so it grew a line at a time as it wrapped and pushed the page down, although its docs said nothing reflows, and a scramble kept its size only on one line or in a monospace font. Both lay the final string out underneath, invisible and unfindable, and draw the animation over it.</p></li><li><p><strong>An effect inside another <code>Animate*</code> keeps its own settings.</strong> The <code>--n-anim-*</code> slots inherit and only the ones an effect uses are written, so a <code>&lt;Box transition=&quot;fade&quot;&gt;</code> inside an <code>AnimateFade</code> waiting for its trigger stayed on its first frame until the wrapper ran, and a fade inside an <code>AnimateReveal</code> ran from full opacity to full opacity. Each animated element now starts from its own shape, and a <code>transition</code> writes its own running state.</p></li><li><p><strong>A <code>CodeBlock</code> with <code>theme=&quot;auto&quot;</code> follows the nearest theme root.</strong> It turned dark through a selector that asked whether any ancestor was dark, so a light panel inside a dark page drew a dark block, and an <code>html</code> forced to light under a dark system setting drew one too. The two palettes are tokens on every theme root now, as the colour tokens are, and <code>auto</code> reads whichever the nearest root declares.</p></li><li><p><strong>Only the <code>header</code> and the <code>footer</code> a <code>PageLayout</code> was handed are its bars.</strong> Every Header and Footer inside a layout registered as its bar, so an article&#39;s own header overwrote the site header&#39;s height with its own and, once it unmounted, left the layout with none. A bar switched between <code>static</code> and <code>fixed</code> was not measured again either. The two the layout places are the only ones measured now, and a change of <code>position</code> measures again.</p></li><li><p><strong>A <code>WindowPane</code> can always be grabbed again, and takes a new size from its props.</strong> A fixed or absolute window could be dragged until its title bar, its only handle, left the screen or its container. It is held now so that the bar stays inside. A size given by a resize also outranked <code>width</code> and <code>height</code> for good; a <code>width</code> or <code>height</code> passed afterwards replaces it.</p></li><li><p><strong>An <code>AnimateHeadline</code> holds each line for the whole <code>interval</code> once it has arrived.</strong> The interval was counted from when a line started arriving, so it rested for <code>interval</code> less <code>duration</code>, and not at all once <code>duration</code> reached <code>interval</code>, although the docs said it counts from arrival. A cycle is <code>duration</code> longer now. The line on its way out also no longer disappears for a frame before it leaves.</p></li><li><p><strong>A <code>CodeBlock</code> colours the script and style inside HTML whatever else is on the page.</strong> A grammar that hands part of its source to another only coloured that part when the other grammar had already been fetched by some other block, so an HTML block&#39;s <code>&lt;script&gt;</code> was plain on a page with no JavaScript block. HTML now fetches JavaScript and CSS with it, Markdown fetches HTML, JavaScript and TypeScript fetch HTML for JSX, and shell and Dockerfile fetch Bash, each as its own chunk after the first paint.</p></li><li><p><strong>A <code>PieChart</code> draws no slice for a negative value.</strong> The share was worked out from the absolute value, so −20 among 50 and 30 was drawn as a fifth of the pie, beside a tooltip reading <code>−20 · 20%</code>. A negative now draws nothing and adds nothing to the total, as a treemap already did; it stays in the table and the legend.</p></li><li><p><strong>A paused <code>AnimateBlink</code> rests at full opacity.</strong> Holding it with <code>paused</code>, or with <code>play={false}</code>, froze the blink wherever the pulse had got to, so the docs&#39; own <code>paused={!recording}</code> left a recording light translucent, or invisible at <code>min={0}</code>. It stops at full opacity now and starts over when it runs again.</p></li><li><p><strong>A <code>Sidebar</code> closes its drawer when the window widens back into a column.</strong> The drawer&#39;s open state outlived the column, so narrowing the window again, which is what turning a tablet to landscape and back does, reopened a modal drawer nobody asked for and trapped the focus in it. The drawer is closed as the column returns, and a controlled sidebar hears <code>onOpenChange(false)</code>.</p></li><li><p><strong>A <code>TabPanel</code> inside a Fragment goes to the panel area, and an outline tab bar capped with <code>lines</code> stops scrolling by a pixel.</strong> Panels were told apart from tabs by type, so one grouped in a Fragment was put into the tab list, inside <code>role=&quot;tablist&quot;</code>. A Fragment is looked through now; a component of your own that renders a <code>TabPanel</code> is still not recognised, as the page says. The <code>lines</code> cap also left out the 1px rule under an outline bar, which scrolled the bar it was meant to fit.</p></li><li><p><strong><code>AnimateTyping</code>, <code>AnimateSplit</code> and <code>AnimateScramble</code> read the text inside element children.</strong> Only strings and numbers were read, so <code>&lt;AnimateTyping&gt;Hello &lt;b&gt;world&lt;/b&gt;&lt;/AnimateTyping&gt;</code> typed <code>Hello </code> and a screen reader heard the same, although the docs said an element&#39;s text counts. The text inside an element is read now, and its markup is dropped as before.</p></li><li><p><strong>A <code>HeatmapChart</code> whose values are all the same no longer looks like peak activity.</strong> A flat sequential range took the darkest step, so a week of zeros painted every cell at full strength. A flat range is read against zero now: zero or less takes the lightest step and anything above zero the darkest.</p></li><li><p><strong>A <code>ProgressCircular</code> keeps a hole in the middle whatever its <code>thickness</code>.</strong> The stroke was held to the whole radius, so a thick <code>thickness</code> on a <code>md</code> ring filled it in to a disc, although the JSDoc and the page said it stopped at half the radius. It stops there now.</p></li><li><p><strong>A disabled <code>ColorPicker</code> takes its text field out of the tab order.</strong> The field was only ever read-only, so a disabled picker kept a stop in the tab order that could do nothing. It is disabled with the picker now; a <code>readOnly</code> picker still leaves it focusable, so its value can be selected and copied.</p></li><li><p><strong>A <code>TimelineChart</code> writes the time of its spans once one of them needs it.</strong> On an axis that ticks in days or weeks every date was written to the day, so a two-hour meeting read <code>3 Mar 2026 – 3 Mar 2026</code> in the tooltip and the hidden table. When any span starts or ends away from midnight, every start and end in the chart is written with its time; a chart whose spans all fall on midnight is written as before.</p></li><li><p><strong>A <code>ScrollZone</code>&#39;s <code>onScroll</code> fires when the strip scrolls.</strong> It was passed through to the root with every other attribute, and the box that scrolls is inside the root, where a scroll does not bubble out of, so the handler was never called. It is the scrolling box&#39;s handler now.</p></li><li><p><strong>Initials are upper-cased the same way on every machine.</strong> An <code>Avatar</code> or an <code>AppLogo</code> with no picture upper-cased its initials in the language of whatever was running it, so a server in English and a browser in Turkish drew <code>I</code> and <code>İ</code> for the same name, and React reported the difference as a hydration mismatch. The letters are upper-cased without a language now, which costs the rules of languages such as Turkish on the one letter where they differ.</p></li><li><p><strong>A <code>FloatingAction</code>&#39;s <code>style</code> is merged over its round radius.</strong> The radius is an inline declaration and the action spread its remaining props after it, so a <code>style</code> written on an action replaced the radius and left the action square. It is merged over now, as the button the dial came out of merges its own.</p></li><li><p><strong>A scroll-driven <code>AnimateAppear</code> or <code>AnimateSplit</code> runs whatever its trigger.</strong> Under <code>timeline=&quot;view&quot;</code> the scroll position is what plays the effect, and the other <code>Animate*</code> components already ignored <code>trigger</code> there. These two still waited for theirs, so with <code>trigger=&quot;visible&quot;</code> or <code>trigger=&quot;manual&quot;</code> the effect was held paused and the children stayed on their first frame until the trigger came.</p></li><li><p><strong>An <code>AnimateSplit</code> cut by character breaks a line only between words, and its blink keeps blinking.</strong> Every character piece is an inline block, and a line may break between any two of them, so a word cut into characters could end one line and start the next. The characters of each word are held together now, with the space after the word left as text where the line can break. <code>effect=&quot;blink&quot;</code> also took the split&#39;s own default of one repeat, so it blinked once and stopped; like a blink anywhere else in the library, it now blinks until it is given a <code>repeat</code>.</p></li><li><p><strong>Replaying an <code>Animate*</code> rewinds only what it animates.</strong> A replay cleared the animation of every descendant that had one, so an <code>&lt;Alert transition=&quot;fade&quot;&gt;</code> inside <code>&lt;AnimateShake&gt;</code> faded in again on every shake, and an <code>Animate*</code> nested in another started over with it. Each component now names its own parts: its root, the children it hands an effect to, a split&#39;s pieces and a marquee&#39;s tracks.</p></li><li><p><strong>An <code>AnimateCounter</code> counts in the decimal places of its two ends, in figures of one width.</strong> The default <code>format</code> writes up to three decimals, so a count to <code>37251</code> read <code>29,851.407</code> on the way there, and the line changed width on every frame. The count is rounded to the decimal places of <code>value</code> or <code>from</code>, whichever has more, and the root sets <code>tabular-nums</code>; <code>className=&quot;proportional-nums!&quot;</code> takes the figures back.</p></li><li><p><strong>An <code>AnimateScramble</code> picks up where it was paused.</strong> Pausing tore the settling down, and resuming started it again from the first letter, so a heading that had nearly landed went back to noise. The letters that had settled are kept across a pause now.</p></li><li><p><strong>An <code>AnimateTyping</code> keeps its place across a pause, and types a new string of the same length.</strong> Pausing tore the loop down, and the one that resumed started on its first pass and never deleting, so every pause gave <code>repeat={2} erase</code> another pass, and a pause in the middle of a delete typed forward instead. A new string was also noticed only by its length, so one of the same length replaced the last at once without being typed. The pass and the direction are kept with the count now, and any new string starts a new performance.</p></li><li><p><strong>A hover-triggered animation does not start again as the focus moves inside it.</strong> React&#39;s focus events bubble, so a Tab from one field of a form inside <code>&lt;AnimateFade trigger=&quot;hover&quot;&gt;</code> to the next reached the trigger as a new focus and rewound the effect, and an endless effect stopped and started again on the way. A focus or a blur that stays inside the element is ignored now, on every <code>Animate*</code> that takes a hover trigger.</p></li><li><p><strong>An <code>AnimateMarquee</code> with <code>trigger=&quot;hover&quot;</code> moves while the pointer is on it.</strong> <code>pauseOnHover</code>, which is on by default, stops the strip under the pointer and while the focus is inside it, and those are exactly what start a hover trigger, so the strip never moved at all. It no longer applies under a hover trigger.</p></li><li><p><strong><code>useElementSize</code> and <code>useOnScreen</code> watch an element that arrives after the first render.</strong> Both read the ref once, in an effect that ran on mount, so a component that drew a placeholder first and the element after it, as <code>if (!data) return &lt;Spinner /&gt;</code> does, was measured at <code>0 × 0</code> and reported as off screen for good. The hook now hears an element being put on the ref or taken off it, whichever component renders it.</p></li><li><p><strong>Dragging a <code>ScrollZone</code> with <code>snap</code> follows the pointer.</strong> A mandatory snap answers every scroll offset a drag writes by jumping to the nearest child, so a mouse or a pen moved the strip in steps rather than along with the pointer. The snap is held off while the strip is dragged and handed back when it is let go, and the strip then settles on the nearest child.</p></li><li><p><strong>A <code>ScrollArea</code> with only <code>maxHeight</code> scrolls.</strong> Its viewport is <code>100%</code> of the root&#39;s height, and a root with a ceiling and no height has no height for that to resolve against, so the viewport grew as tall as its content and the root cut it off at the ceiling with nothing to scroll. The viewport takes the root&#39;s ceiling now, whether it came from <code>maxHeight</code>, a class or a style.</p></li><li><p><strong>An <code>AspectRatio</code> fits a <code>canvas</code> inside it.</strong> <code>fit</code> reached an <code>img</code>, a <code>video</code> and the <code>img</code> in a <code>picture</code>, so a <code>canvas</code> was stretched to the box and squashed out of its own proportion, although the docs listed it among the media <code>fit</code> applies to. A <code>canvas</code> is fitted like the others now. The docs also stop saying that <code>fit</code> reaches an <code>svg</code> or an <code>iframe</code>: both are stretched to the box and lay their own content out, as they always did.</p></li><li><p><strong>A responsive map whose <code>xs</code> is <code>undefined</code> keeps the default there.</strong> <code>withBaseline</code> spread the caller&#39;s map over the default, so <code>spacing={{ xs: undefined, md: 4 }}</code>, the shape a map built from optional values takes, copied the <code>undefined</code> over it. A <code>GridContainer</code> then had no gutter at all below <code>md</code> instead of its default of 2. An <code>xs</code> of <code>undefined</code> now means the same as leaving it out.</p></li><li><p><strong>A component shows its own name in React DevTools and in React&#39;s warnings.</strong> Every component is a named function handed to <code>forwardRef</code>, and the published build dropped that name, because terser removes a function expression&#39;s name when nothing inside the function calls it. A consumer&#39;s DevTools showed a tree of <code>ForwardRef</code>, and a warning&#39;s component stack named nothing. Names that start with a capital letter now survive the minifier. That adds 0.2 kB gzipped across the package, and a production bundle, which its own build minifies again, moves by a few dozen bytes either way.</p></li><li><p><strong>A <code>Rating</code> shows where the focus is in Firefox 113 to 120.</strong> Its only focus indicator is a ring keyed to <code>:has(:focus-visible)</code>, which those versions do not have, so a keyboard user there saw no focus at all. In a browser without <code>:has()</code> the star holding the focus now rings through <code>:focus-within</code>, which also rings a star that was clicked. Every browser with <code>:has()</code> draws exactly what it drew before.</p></li><li><p><strong>A secondary line inside a tinted or filled surface is the same ink, one step smaller.</strong> <code>Pill</code>&#39;s description was <code>currentColor</code> at 72% and a selected <code>List</code> row&#39;s was <code>--neba-muted-fg</code>, and neither could hold 4.5:1 — the description on a <code>solid</code> pill read 3.2:1 and the row&#39;s 3.1:1. The reason is the same in both: the ink on those beds was already solved to the minimum, <code>--n-on-solid</code> on <code>--n-fill</code> being 4.6:1 at full strength, so there is nothing to take away. Size and weight carry the step now, which they do on every variant and need no number. A row that is <em>not</em> selected keeps the neutral grey, because on the bare sheet that is what quiet means.</p></li><li><p><strong>A <code>Chip</code>&#39;s count plate carries its own bed instead of another wash on top of the chip&#39;s.</strong> <code>--n-on-tint</code> is solved for one wash, and the plate was <code>--n-soft-press</code> painted over whatever wash the chip already had: a count on a <code>text</code> chip read 4.2:1 and on a selected one 3.5:1. It is a fill now on every variant that is not itself filled, which holds 4.6:1 or better wherever the chip is sitting; a filled chip keeps the hole punched in its own fill.</p><p><code>Badge</code>, <code>Avatar</code> and <code>AppLogo</code> take the idea the rest of the way, because they are marks a caller drops <em>inside</em> other things and cannot know what: their <code>text</code> plate is the wash laid on the page&#39;s own surface rather than on the caller&#39;s, which makes it opaque and leaves it no bed to depend on. It had none to spare — a <code>text</code> Badge read 3.9:1 on a selected row and <strong>1.45:1 on a filled surface</strong>, which is not faint, it is gone. It reads 5.5:1 in light and 7.6:1 in dark now, on every bed, and unchanged on the page: there the opaque colour <em>is</em> what the wash resolved to.</p></li><li><p><strong>Text on a surface tinted with its own colour has an ink of its own.</strong> <code>panel</code> and <code>soft</code> are both washes of the family&#39;s <code>accent</code>, so writing the label in that accent put a colour on a pale copy of itself — the two move together, and past a wash of about 9% no adjustment to either one can reach 4.5:1. <code>soft-press</code> is 25%. An <code>on</code> Toggle, a <code>text</code> Chip, a highlighted menu row and an <code>outline</code> Button under the pointer were all between 3.4:1 and 4.5:1, in <strong>both</strong> themes; this one was never a dark-mode fault. <code>--neba-{color}-on-tint</code> is the ink for those beds, the way <code>on-solid</code> is the ink for a fill: <code>accent</code> pulled 28% toward <code>--neba-fg</code>, so it turns toward near-black on a white page and near-white on a dark one without a second value to keep in step. Every family now clears 4.5:1 on every step of both ladders, worst case 4.8:1 in light and 5.6:1 in dark. <code>accent</code> is untouched, so a TextLink, a Statistic&#39;s delta and an Alert&#39;s title read exactly as they did, and a project that overrides the five hand-picked values of a family gets the new ink derived from them for free.</p></li><li><p><strong>Dark mode&#39;s panel ladder is 5/7/9%, down from 7/10/13%.</strong> The ladder is opacity, which is why it is free on a white page — white over white is white — and expensive on a near-black one, where every step multiplies the bed&#39;s luminance and nested sheets compound. At the old numbers a Card was 2.6x the page and a table header inside that Card was 7.5x, which squeezed the whole ink hierarchy into the top of its range: <code>--neba-fg</code> itself fell from 16:1 to 9:1 just by being inside two sheets. The light theme&#39;s own ladder moves a sheet&#39;s lightness by nothing at all, and it is the hairline and the plate edge that say where a Card begins there, so the dark sheet keeps more separation than its light counterpart even after the change.</p></li><li><p><strong>Dark mode&#39;s secondary ink, disabled ink and hairline are measured on that raised sheet, not the bare one.</strong> All three were picked against <code>--neba-surface</code>, which is the one bed almost nothing is drawn on. So a DataTable&#39;s column names came out at 3.3:1, a SegmentedButton&#39;s unchosen labels at 3.9:1, a Combobox&#39;s placeholder at 4.5:1, and a disabled Select option or segment at 1.8:1 — a fifth of what its own label reads at.</p><p><code>--neba-muted-fg</code> now reads 5.05:1 on a table header inside a Card and 4.34:1 with a page shell around that, against the 4.33-4.64:1 the light theme reads everywhere — parity at every depth the library composes. <code>--neba-disabled-fg</code> lands inside the 2.0-2.4:1 light reads at, still clearly unavailable and no longer gone. Nothing in the light theme moved.</p></li><li><p><strong>A hairline in dark mode is lighter than the sheet it is drawn on.</strong> <code>--neba-border</code> was 0.11 above the bare surface in perceptual lightness but 0.02 to 0.05 <em>below</em> a raised one, so on anything that is not the bare page the edge that is supposed to catch the light was a black scratch. A TreeView&#39;s rails and elbows were the clearest case, at 1.03:1 against the sheet behind them; a chart&#39;s grid and axis, a Panes divider, a Slider&#39;s ticks, an Anchor&#39;s rail, a Timeline&#39;s connectors and a DataList&#39;s rules are derived from the same token and were all doing it. The dark value now sits above the whole panel ladder. It is more present on a plain page than the light hairline is, which is the trade — one value cannot be a whisper across a ladder that moves.</p></li><li><p><strong>A heading a component renders keeps the sheet&#39;s type, not the article&#39;s.</strong> The same specificity problem, one level out: an Accordion&#39;s header, an inline <code>Drawer</code>&#39;s title and a <code>HowToSteps</code> step are real <code>h1</code>-<code>h6</code> because they belong in the document outline, and that is exactly the tag <code>.prose</code> and <code>.vp-doc</code> have already styled. Inside rendered Markdown an Accordion header came out at 20px where the sheet asked for 15px — taking every <code>em</code>-sized glyph in the trigger with it — and a step heading at 18px and one weight heavier. All three write their type through <code>[&amp;.neba-heading]</code> now, and <code>headingTitleClasses</code> is <code>sheetTitleClasses</code> in that form; the two tables cannot drift, because <code>test/package/resolution.test.ts</code> asserts one is the other with the prefix on each token.</p><p>The <code>.neba-title</code> rule has the same fix. It is what lets a caller pass a real heading — <code>&lt;Card title={&lt;h2&gt;…&lt;/h2&gt;}&gt;</code> — and keep the card&#39;s type, and it was written with one class plus a <code>:where()</code>, which is one class: inside an article the browser&#39;s own bold was being replaced by the article&#39;s rather than by the card&#39;s. Card, Alert, Empty, Collapsible, HoverCard, Toast, Timeline, Gallery, FilePicker and Fieldset all read it.</p><p><code>Dialog</code>, <code>Popover</code>, <code>Tour</code> and an overlay <code>Drawer</code> were measured and need none of this: they render into a portal at the end of <code>&lt;body&gt;</code>, where a container&#39;s <code>h2</code> rule cannot reach them.</p></li><li><p><strong><code>Typography</code> states its type scale at a specificity a host stylesheet cannot beat.</strong> <code>h1</code>-<code>h6</code> and <code>p</code> are the tags a CSS framework is most certain to have styled by name, and <code>.prose h2</code> and VitePress&#39;s <code>.vp-doc h2</code> both write <code>font-size</code>, <code>line-height</code>, <code>letter-spacing</code> and <code>font-weight</code> on them — at one class plus one tag, which a single utility cannot outrank. Measured inside a rendered-Markdown article, every level of the scale lost its leading, <code>h1</code> and <code>h4</code> their size, the headings their tracking, and <code>weight</code> was ignored outright: <code>&lt;Typography level=&quot;h2&quot; weight=&quot;regular&quot;&gt;</code> rendered at 600. The scale, the weight, the ink and the <code>gutter</code> margin are now written through <code>[&amp;.neba-typography]</code>, the same doubling <code>TextLink</code> uses, and <code>align</code> and <code>lines</code> stay plain because nothing styles them by tag name.</p><p><strong>This changes how an override is written.</strong> A plain <code>className=&quot;text-[2.75rem]&quot;</code> no longer wins; use Tailwind&#39;s <code>!</code> — <code>className=&quot;text-[2.75rem]!&quot;</code> — which is what the component&#39;s own docs already recommended, for the weaker reason that the two classes used to tie. There is no specificity above a host&#39;s <code>h2</code> rule and below a caller&#39;s plain utility, so the only choice is which of the two wins.</p></li><li><p><strong><code>Table</code> and <code>DataTable</code> write their header ink inline.</strong> Everything else a cell is painted with already was, for the reason that <code>&lt;th&gt;</code> and <code>&lt;td&gt;</code> are among the last tags a host stylesheet still styles by name, at a specificity a one-class utility cannot reach. <code>color</code> was the declaration left out, and both <code>.vp-doc th</code> and <code>.prose th</code> write it — so under Tailwind Typography a column heading took the <em>heading</em> colour, the one ink on the page chosen to stand out from the text around it. The ink reads a <code>--n-cell-ink</code> slot, which is also the way a caller changes it: a custom property is invisible to a host stylesheet, where a <code>text-*</code> class through <code>classNames.headCell</code> never worked.</p></li><li><p><strong>A <code>Chip</code> keeps the tails of its letters.</strong> The label is truncated, and truncation clips at the line box, which the chip&#39;s one-em leading made shorter than the glyphs inside it — so every g, j, p, q and y lost its descender, at every size. The label&#39;s line box is now the font&#39;s own height. The chip is the same height and the words sit where they did.</p></li><li><p><strong>Ticking a <code>DataTable</code> row with the pointer adds it to the selection.</strong> In <code>multiple</code> mode with <code>checkboxes</code>, the press on a tick reached the row first and replaced the selection with that row, and the tick&#39;s own click then took it away again, so ticking a second row left at most one chosen. A press on a checkbox, switch, radio or label inside a cell now belongs to that control, as a press on a button or a link already did.</p></li><li><p><strong>An <code>Image</code> given its own <code>onLoad</code> or <code>onError</code> still shows the picture.</strong> The caller&#39;s handler replaced the one that marks the picture loaded, so it stayed at <code>opacity: 0</code> behind its placeholder for good, and a caller&#39;s <code>onContextMenu</code>, <code>onDragStart</code> or <code>draggable</code> switched <code>protect</code> off in the same way. Both handlers run now, the component&#39;s first.</p></li><li><p><strong>A <code>Tour</code>&#39;s card follows the step to its target.</strong> The card was anchored to the first step&#39;s target for as long as the tour stayed open, so from the second step on the cut-out moved and the card did not. It is anchored again on every step.</p></li><li><p><strong><code>Highlight</code> no longer remounts its children when a query arrives or clears.</strong> Marking re-keyed every child in a list, so typing the first character of a search, or deleting the last one, threw away the state inside every row it wrapped: what had been typed into a field, what was open and which pictures had loaded. Keys are left as they were, and a part of the tree with no match in it is not rebuilt at all.</p></li><li><p><strong>Turning a <code>Carousel</code> slide scrolls the strip and nothing else.</strong> The slide was scrolled into view, which moves every scroll container above it as well, so a reader who had scrolled past an autoplaying carousel was pulled back up to it on every turn, and a <code>value</code> changed from outside did the same. Only the carousel&#39;s own track moves now.</p></li><li><p><strong><code>registerMessages</code> cannot be used to write onto <code>Object.prototype</code>.</strong> A locale parsed from JSON can carry an own <code>__proto__</code> key, and registering one put the tag on every object in the page. Namespaces are now matched only against the ones the library has, and a tag of <code>__proto__</code> is ignored.</p></li><li><p><strong>A <code>TextLink</code> given <code>target</code> directly gets the same <code>rel</code> protection as <code>newTab</code>.</strong> Only the target <code>newTab</code> implies went through the merge, so <code>&lt;TextLink target=&quot;_blank&quot;&gt;</code> left out <code>noopener noreferrer</code> and the new page kept a <code>window.opener</code> and a <code>Referer</code> pointing back.</p></li><li><p><strong>A <code>NavigationMenuLink</code> that opens elsewhere carries <code>noopener noreferrer</code>.</strong> A link inside a panel passed <code>target</code> straight through, unlike a <code>NavigationMenuItem</code> link beside it, so <code>target=&quot;_blank&quot;</code> in a panel left the new page a <code>window.opener</code> and a <code>Referer</code>. A <code>rel</code> of the caller&#39;s own is kept and merged.</p></li><li><p><strong><code>neba/hooks</code> has types under <code>moduleResolution: node10</code>.</strong> <code>typesVersions</code>, which that resolver reads instead of <code>exports</code>, had no entry for it, so its wildcard sent the import to a component folder that does not exist and TypeScript reported the module as missing.</p></li><li><p><strong>A <code>Tour</code> step with a selector the browser cannot parse no longer takes the app down.</strong> <code>querySelector</code> throws on a <code>target</code> such as <code>#1-intro</code> or an id made by React 18&#39;s <code>useId</code>, and it threw inside an effect, where it unmounted the whole tree. Such a step is drawn as a step with no target.</p></li><li><p><strong>A <code>TreeSelect</code> branch that cannot be chosen opens again.</strong> It was drawn as a disabled row, and a disabled row answers neither a click nor its arrow, so with the default <code>selectableBranches={false}</code> no branch could be opened at all and only <code>defaultExpanded</code> or a search reached the leaves. Such a branch is now an ordinary row that opens and shuts and is never chosen; a branch marked <code>disabled</code> still looks and acts disabled.</p></li><li><p><strong><code>useToast</code>&#39;s methods keep one identity.</strong> <code>add</code>, <code>close</code>, <code>update</code> and <code>promise</code> were rebuilt every time the list of toasts changed, so an effect that raised a toast and listed <code>add</code> among its dependencies raised one, received a new <code>add</code>, and ran again without end. The object the hook returns still changes with <code>toasts</code>; list the method you call, not the object.</p></li><li><p><strong>A grouped <code>DataTable</code> in pages heads each page with the groups that are on it.</strong> Every group&#39;s heading was drawn on every page, so the second page of a long table opened with a column of headings for groups whose rows were all somewhere else. An open group is headed only on the pages that hold its rows; a folded one has no rows anywhere and stays on every page, so it can be opened again.</p></li><li><p><strong><code>CodeBlock</code> keeps nothing for a <code>language</code> it cannot load.</strong> Every unknown name a page passed was remembered for the page&#39;s lifetime, so a block whose language came from a reader&#39;s choice grew the cache without bound; such a name is now answered as plain text on the spot. A <code>language</code> named after a member of <code>Object</code>&#39;s prototype, such as <code>constructor</code>, is plain text too rather than a failed attempt to call <code>Object</code> as a grammar.</p></li><li><p><strong><code>HowToSteps</code> keeps the keyboard focus through Done and Start over.</strong> Both remove the button that was pressed, so the focus fell to the page and a keyboard reader started again from the top. Done hands it to Start over, and Start over hands it back to the forward button.</p></li><li><p><strong>A <code>Spoiler</code> keeps the keyboard focus when it is revealed or covered again.</strong> The pressed button went inert with the half it was in, which drops the focus to the page. Revealing now hands the focus to the content, and covering again hands it to the cover&#39;s button.</p></li><li><p><strong>Unfolding a <code>Breadcrumb</code> keeps the keyboard focus in the trail.</strong> The <code>…</code> button left the trail as soon as it was pressed, which dropped the focus to the page. It now moves to the first step the fold was hiding.</p></li><li><p><strong>Removing a file from a <code>FilePicker</code> keeps the keyboard focus in the list.</strong> The remove button left with its row and the focus fell to the page. It now moves to the remove button that takes that place, the one before it when the last file went, and the drop zone when the list is empty.</p></li><li><p><strong>Pressing a <code>FloatingAction</code> puts the keyboard focus back on the button it came out of.</strong> The dial closed with the pressed action inside it, so the focus fell to the page, where Escape already refused to leave it. A dial kept open with <code>closeOnAction={false}</code> leaves the focus on the action.</p></li><li><p><strong>Picking a month from a calendar&#39;s month grid keeps the focus in the calendar.</strong> The day grid drew its first frame with the tab stop still in the previous month, found no day to focus and dropped the focus to the page; this happened in <code>Calendar</code> and in every date picker. The first day of the picked month takes the focus.</p></li><li><p><strong><code>Panes</code>, <code>Sidebar</code> and a draggable <code>WindowPane</code> can be dragged with a finger.</strong> Their handles and the window&#39;s title bar let the browser treat a moving finger as a scroll, so it cancelled the pointer a few pixels into every drag. They take <code>touch-action: none</code> now, as the window&#39;s resize handles already did.</p></li><li><p><strong>The preview button of an <code>Image</code> and the tiles of a <code>Gallery</code> draw a focus ring.</strong> Both wrote their outline through a ring colour that nothing above them declares, and a custom property with no value drops the whole declaration, so a keyboard reader saw no focus at all. They fall back to the primary ring.</p></li><li><p><strong>Focus rings inside a clipped sheet are drawn inside the edge.</strong> A ruled <code>Accordion</code>, a ruled <code>List</code> and a <code>ScrollArea</code> clip their overflow, and the ring drawn outside a full-width header, row or viewport was cut off on both sides — entirely, for a lone accordion section or the viewport. Those three rings sit inside the edge now, as <code>Collapsible</code>&#39;s already did.</p></li><li><p><strong>The words <code>Combobox</code>, <code>ToastProvider</code>, <code>ScatterChart</code> and <code>TimelineChart</code> said in English now follow <code>locale</code>.</strong> The row that adds what was typed, the name of a combobox&#39;s chevron, the name of the toast region, and the column headings of the scatter and timeline data tables were written in English whatever language the product spoke. All eighteen languages under <code>neba/locales</code> translate them, and a test now fails when a language is missing a word English has. The hidden tables&#39; English headings change as well: a scatter&#39;s size column is <code>Size</code> rather than <code>z</code>, and a timeline&#39;s columns are <code>Label</code>, <code>Start</code> and <code>End</code>.</p></li><li><p><strong><code>colorSchemeScript()</code> sets <code>color-scheme</code> as well as <code>data-theme</code>.</strong> A remembered dark page kept light scrollbars and native controls until the app hydrated, because only the provider wrote <code>color-scheme</code>. The pre-paint script writes both now, as the provider does.</p></li><li><p><strong>Reduced motion holds inside a forced-light box.</strong> An element with <code>.light</code> or <code>data-theme=&quot;light&quot;</code> declares the motion durations again, so a reader who asked for less motion got the hover transitions and popup fades back everywhere inside one. The override now names those roots too.</p></li><li><p><strong>A <code>Gallery</code> tile answers the keyboard focus the way it answers the pointer.</strong> The lift, the dim, the zoom and a <code>caption=&quot;hover&quot;</code> were keyed to the tile being focused, and the tile is a list item that never is, so none of them appeared for a keyboard reader. They follow a focused button inside the tile now, and <code>focus-within</code> in a browser without <code>:has()</code>.</p></li><li><p><strong>A <code>Transfer</code>&#39;s two lists are named groups.</strong> Each list has its own &quot;Select all&quot; box and search field, identical to the other list&#39;s, and nothing tied them to a heading, so a screen reader could not tell which list a control belonged to. Each list is now a group named by its heading.</p></li><li><p><strong>An inline <code>ColorPicker</code> is a group named by its <code>label</code> and described by its <code>description</code> and <code>error</code>.</strong> The three were drawn beside the panel and connected to nothing, so the square, the rails and the field were heard without any of them, and two inline pickers on a page could not be told apart.</p></li><li><p><strong>A read-only <code>Rating</code> reads an average out to one decimal.</strong> The score was spoken whole, so <code>value={13 / 3}</code> was announced as &quot;4.333333333333333 out of 5&quot;. It is rounded to one decimal before it is written into the sentence.</p></li><li><p><strong>A virtualised <code>DataTable</code> with column groups counts its rows correctly.</strong> The row numbers a screen reader reads out assumed a head of one row, so under a two-row head every body row was numbered one too low and the total one too few. Both head rows are numbered now and the body starts after them.</p></li><li><p><strong><code>TreeView</code>&#39;s keyboard follows the tree pattern more closely.</strong> Enter or Space on a button in a row&#39;s <code>action</code> chose the row instead of pressing the button; the tree now leaves keys pressed inside a row&#39;s controls alone. Enter on a row with an <code>href</code> follows the link, where it only chose the row before. In a <code>multiple</code> tree every row that can be chosen says whether it is, and a reader tabbing into a tree lands on the chosen row rather than the first.</p></li><li><p><strong>A selected <code>ListItem</code> that is not pressable is announced as the current one.</strong> Only a row that was a button or a link said so, through <code>aria-current</code>; a plain row showed it with its tint alone. It carries <code>aria-current</code> as well now.</p></li><li><p><strong>An autoplaying <code>Carousel</code> stays held while the keyboard focus is inside it.</strong> The pointer and the focus shared one pause, so a mouse passing over and out of the strip started it again under a reader who had tabbed into a slide, and the slide changes made there by the arrows were not announced because the live region stayed silent. The two pauses are kept apart, the live region speaks whenever the strip is held, and a caller&#39;s <code>onFocus</code>, <code>onBlur</code>, <code>onPointerEnter</code> and <code>onPointerLeave</code> run beside the carousel&#39;s own instead of switching the pause off.</p></li><li><p><strong>A failed <code>Image</code> is named once, and a previewable one with <code>alt=&quot;&quot;</code> has a name.</strong> When the file did not arrive, the <code>&lt;img&gt;</code> kept its <code>alt</code> in the accessibility tree and the fallback wrote the same words over it, so a screen reader read the name twice; the drawn words are hidden from it now. With <code>preview</code> and an empty <code>alt</code>, the button and the preview had no name at all, and both are called &quot;Enlarge image&quot;, translated in every language under <code>neba/locales</code>.</p></li><li><p><strong>An <code>Avatar</code> says its <code>alt</code> whatever stands in for the picture, and says nothing when <code>alt</code> is empty.</strong> A silhouette given an <code>alt</code> and no <code>name</code> had nothing to read while its picture loaded or after it failed, and <code>alt=&quot;&quot;</code> beside a <code>name</code> still read the initials out as two letters. The name is spoken from the label alone now, and an empty <code>alt</code> hides the stand-in as it hides the picture.</p></li><li><p><strong>A <code>Popconfirm</code> bubble is named by its title and described by its description.</strong> Both were plain paragraphs connected to nothing, so the focus moved into a bubble announced only as &quot;dialog&quot;. They are the popover&#39;s own title and description now, drawn as the same paragraphs.</p></li><li><p><strong>A focus ring on the light sheet clears 3:1.</strong> A ring is drawn 2px off its control, so it is read against the page, and on white 55% of a family&#39;s accent was 2.2:1 to 2.6:1 — under what a focus indicator needs, on a Button or a Chip&#39;s × where the ring is the only sign of focus. The light theme draws the ring at 80% now, 3.4:1 or better for every family; the dark theme keeps 55%, which already cleared 3.2:1. <code>--neba-ring-alpha</code> holds the number for each theme.</p></li><li><p><strong>A calendar says which month it moved to.</strong> Stepping a month, or arrowing off the edge of one, changed every cell at once and a screen reader heard only the newly focused day. The grid is named by the month on screen, in a polite live region that says it again when it changes. A month cell is named the way the locale writes a month of a year (&quot;2026년 11월&quot;, not &quot;11월 2026&quot;), and a <code>multiple</code> or <code>range</code> calendar marks its grid <code>aria-multiselectable</code>.</p></li><li><p><strong>The tick on a chosen <code>ColorPicker</code> swatch is the ink that reads.</strong> It turned black only past a luminance of 0.42, so a white tick was drawn on a mid grey, an orange or a green at 2.3:1 to 2.8:1, where black reads 7:1 or better. The tick now takes whichever of the two contrasts more, so a few light swatches that had a white tick have a black one.</p></li><li><p><strong>A <code>HowToSteps</code> with a <code>title</code> is a named group.</strong> Its root pointed <code>aria-labelledby</code> at the title, but a <code>&lt;div&gt;</code> with no role cannot carry a name, so the reference was ignored and the guide had no name at all. With a <code>title</code> the root is <code>role=&quot;group&quot;</code> named by it.</p></li><li><p><strong>A blink never runs faster than three times a second.</strong> <code>AnimateBlink</code>&#39;s <code>duration</code> had no floor, so <code>duration={200}</code> flashed five times a second, past the rate at which a flash can bring on a seizure. A blink shorter than 334ms is raised to it, on <code>AnimateBlink</code>, on <code>transition=&quot;blink&quot;</code> and on an <code>AnimateSplit</code> whose effect is <code>blink</code>.</p></li><li><p><strong>Hidden panels are out of reach under React 18 too.</strong> <code>Spoiler</code>, <code>Pill</code>, <code>WindowPane</code> and <code>HowToSteps</code> take what they hide out of the tab order and the accessibility tree with <code>inert</code>, written as a boolean. React 18 does not know the attribute and drops a boolean on it, so under 18 a covered spoiler was read aloud, a collapsed pill&#39;s details and a minimised window took the focus, and a guide&#39;s other steps could be tabbed into. The attribute is now spelled for the React that is running.</p></li><li><p><strong>An <code>aria-label</code> on a <code>Select</code>, <code>Combobox</code> or <code>NumberField</code> names the control.</strong> <code>aria-label</code> and <code>aria-labelledby</code> went to the root <code>&lt;div&gt;</code> with every other attribute, so a field with no visible label, the usual case in a table cell or a toolbar, had a trigger or an input with no name at all. Both now go to the control itself, as they already did on <code>TextField</code>.</p></li><li><p><strong>A <code>Slider</code> is named and described on its thumbs.</strong> <code>aria-label</code> and <code>aria-labelledby</code> went to the root, so a slider with no visible label had thumbs with no name, and <code>description</code> was tied to nothing. Both names, and <code>aria-describedby</code> merged with the description, now go to every thumb.</p></li><li><p><strong><code>HowToSteps</code> says which step it moved to.</strong> Next, Previous and a row in the list changed the panel in place and left the focus on the button pressed, so a screen reader heard nothing change. A polite live region now reads the new step, as &quot;Step 2: Configure&quot;, or its position when the title is not a string.</p></li><li><p><strong>A <code>Tour</code> says which step it moved to.</strong> Next rewrote the title, the words and the counter in place while the focus stayed on Next, so a screen reader heard nothing change. A polite live region in the card now reads the new step, as &quot;Step 2: Deploy&quot;, or its position when the title is not a string.</p></li><li><p><strong>A <code>Pill</code> that opens its <code>details</code> says so.</strong> The button a pill with <code>onClick</code> draws had no <code>aria-expanded</code> and no way to be given one, since attributes passed to the pill land on its root, so a screen reader was never told that pressing it revealed anything. With <code>details</code> it now carries <code>aria-expanded</code> from <code>expanded</code> and <code>aria-controls</code> pointing at the details.</p></li><li><p><strong>A <code>priority</code> <code>Image</code> is visible before hydration.</strong> Every picture started at <code>opacity: 0</code> under a Skeleton and faded in once React had seen it load, so on a server-rendered page the picture Largest Contentful Paint measures stayed hidden until hydration, however early its file arrived. A <code>priority</code> picture is now drawn from the first paint, with no fade and no Skeleton over it; a <code>placeholder</code> picture still stands beneath it.</p></li><li><p><strong>A <code>CodeBlock</code>&#39;s line numbers and prompts are not read out.</strong> They are generated content, which a screen reader reads with the code, so a block was heard as &quot;1 const a equals 1, 2 const b…&quot; although the docs said otherwise. Both now carry empty alternative text; a browser without that syntax, Firefox before 128 and Safari before 17.4, keeps reading them as before.</p></li><li><p><strong>A modal <code>Popover</code> always has a way out.</strong> With <code>modal</code> on and <code>showClose</code> left off, the popup trapped the focus with no close button in it, so on iOS VoiceOver, which has no Escape key, a reader could not leave; Base UI requires a close for a modal popover. One is now added last in the popup, visually hidden until it takes the keyboard focus, when it shows as the × in the corner.</p></li><li><p><strong>A <code>Mockup</code> with a width in pixels is drawn in a server render, and never widens the page.</strong> The device stayed <code>visibility: hidden</code> until its box was measured, so on a server-rendered page the device and everything on its screen were invisible until hydration, and meanwhile a desktop drawn at 1440 pixels could give a phone-width page a horizontal scrollbar. A numeric <code>width</code> or <code>height</code> now works out the scale while rendering, and the root clips what it holds; a mockup sized in a CSS length is still measured first.</p></li><li><p><strong><code>OtpField</code> names each slot by its place, and takes <code>locale</code> and <code>slotLabel</code>.</strong> Every slot was named by the field&#39;s label, so a six-digit code was &quot;Verification code&quot; six times with no word about which box held the caret. The first slot keeps the label and every other is &quot;Character 2 of 6&quot;, in the <code>locale</code>&#39;s words or through <code>slotLabel</code>. Every registered language has the new <code>otp</code> namespace.</p></li><li><p><strong>A <code>Timeline</code> step says in words whether it is done.</strong> Only the current step was read, through <code>aria-current</code>; a finished and a waiting step differed only in the shape of their bullets. Each now carries visually hidden text, &quot;Completed&quot; or &quot;Upcoming&quot;, in the words of the new <code>locale</code> prop or of <code>labels</code>, and every registered language has the new <code>timeline</code> namespace.</p></li><li><p><strong>A read <code>ChatBubble</code> is told from a delivered one by shape.</strong> <code>read</code> was the <code>delivered</code> double tick in the accent colour, so whether a message had been read was said only to readers who can tell the two colours apart. It is now the double tick cut out of a filled disc.</p></li><li><p><strong>A <code>CommandPalette</code> group names the rows in it.</strong> A group heading was a <code>presentation</code> element between rows of the listbox, tied to none of them, so arrowing from &quot;Navigate&quot; into &quot;Actions&quot; never said the group had changed. Each run of rows that share a <code>group</code> is now a <code>role=&quot;group&quot;</code> named by its heading.</p></li><li><p><strong>A time column is one tab stop.</strong> Every hour, minute and second in a <code>TimePicker</code> or <code>DateTimePicker</code> was a tab stop of its own and no key moved within a column, so getting past a 24-hour clock with seconds took a hundred and forty-four presses of Tab. Each column is now one stop, on its chosen row or its first; ↑ and ↓ walk it, Home and End jump to its ends, and Enter, Space or a press still chooses.</p></li><li><p><strong>Scrolling a <code>DataTable</code> with a finger no longer changes the selection.</strong> A row was chosen on <code>pointerdown</code>, so every touch that began a scroll replaced the selection with the row under the finger. A touch now chooses on <code>click</code>, which the browser does not send when the finger went on to scroll; a mouse and a pen still choose on the press and drag a run as before.</p></li><li><p><strong>A macOS <code>WindowPane</code> shows its traffic-light glyphs to the keyboard and to touch, and its resize corner says how it works.</strong> The glyph inside each dot appeared only under a hovering pointer, so a keyboard or a touch reader told close, minimise and maximise apart by colour alone; it now also shows while one of the three has the focus, and always where there is no hover. The keyboard-reachable corner, a button that does nothing when pressed, is described as resizing with the arrow keys, in a new <code>window</code> message every registered language has.</p></li><li><p><strong>Tapping a field on an iPhone no longer zooms the page.</strong> Safari on iOS and iPadOS zooms in on any field under 16px when it takes the focus, and the default <code>md</code> field is 13px, so the ordinary field on the ordinary phone moved the whole page and left it zoomed. On those systems alone, the text of <code>TextField</code>, <code>NumberField</code>, <code>Combobox</code>, <code>CommandPalette</code>, <code>TreeSelect</code>&#39;s search, <code>ColorPicker</code>&#39;s value, a <code>DataTable</code> cell editor and an <code>xs</code> or <code>sm</code> <code>OtpField</code> is now at least 16px; every other browser keeps the size <code>size</code> sets.</p></li><li><p><strong>Controls and indicators stay visible in a forced-colour theme.</strong> Windows&#39; contrast themes draw no <code>box-shadow</code> and replace background colours, and the stylesheet had nothing for them: a filled <code>Button</code>, <code>Toggle</code>, <code>Chip</code> or <code>Pill</code> lost the plate that was its edge, and a <code>Switch</code>, a <code>ProgressLinear</code> and a <code>Slider</code> lost the fill and the thumb that say their value. Under <code>forced-colors: active</code> those controls now carry a system-coloured outline, and the indicators are drawn in the system&#39;s own <code>Highlight</code> and <code>ButtonText</code>. Nothing changes in any other mode.</p></li><li><p><strong>Small controls are pressed at the size of a finger.</strong> An <code>Alert</code> or <code>Toast</code> ×, a <code>NumberField</code> stepper at <code>xs</code> and <code>sm</code>, a <code>Carousel</code> dot, a <code>Panes</code> or <code>Sidebar</code> resize handle, a <code>WindowPane</code>&#39;s resize corner and its macOS traffic lights were all drawn, and pressed, well under the 24px WCAG 2.5.8 asks for. Each now carries an invisible target of at least 24px on the short axis; a control in a tight row grows along the row only into the gaps beside it, so it never takes a neighbour&#39;s press. Nothing drawn moves, and the <code>xs</code> step of the control height ladder is left as it was.</p></li><li><p><strong>A <code>static</code> <code>BottomNavigation</code> no longer leaves room for a home indicator.</strong> <code>safeArea</code> added the bottom inset whatever the position, so a bar sitting in the flow inside a card had 34px of empty sheet under it on an iPhone. It applies only to a <code>fixed</code> or <code>sticky</code> bar now.</p></li><li><p><strong>An <code>Empty</code> rendered as a table cell stays a cell.</strong> The props table suggests <code>render={&lt;td colSpan={5} /&gt;}</code>, which put <code>role=&quot;status&quot;</code> and <code>display: flex</code> on the <code>&lt;td&gt;</code> itself: the cell lost its role, and a flex <code>&lt;td&gt;</code> is no longer laid out as a cell, so its <code>colSpan</code> spanned nothing. Given a <code>&lt;td&gt;</code> or a <code>&lt;th&gt;</code>, the state is now drawn inside it, and the cell keeps its role, its span and the class names it was given.</p></li><li><p><strong>A <code>GaugeChart</code>&#39;s reading is in a server render.</strong> The number in the middle was drawn only once the box had been measured, and an unmeasured box counted as an empty gauge, so the HTML a server sent, and a crawler read, said &quot;Nothing here&quot; where the reading belonged. Until it is measured the gauge now shows its reading and caption in the middle of the box.</p></li><li><p><strong>An <code>AnimateMarquee</code>&#39;s copies are out of the tab order.</strong> The strip is laid down more than once so it can loop, and the copies were only <code>aria-hidden</code>, so every link or button in it was a tab stop once per copy, each landing somewhere a screen reader had been told was not there. The copies are <code>inert</code> now.</p></li><li><p><strong><code>AnimateScramble</code> and <code>AnimateCounter</code> show the answer to a reader who asked for less motion, triggered or not.</strong> Under a reduced-motion preference both skipped the animation once it started, but until then a Scramble showed its noise and a Counter its <code>from</code> value, and with <code>trigger=&quot;hover&quot;</code> or <code>&quot;manual&quot;</code> that could be all the reader ever saw. Both show the final text or number from the first frame now.</p></li><li><p><strong>A <code>Sidebar</code>&#39;s resize handle says how wide the sidebar is.</strong> It is a focusable <code>separator</code>, which has to carry a value and its range, and it carried neither, so a screen reader announced a separator and nothing about where it was or how far it could go. It now carries <code>aria-valuenow</code>, <code>aria-valuemin</code> and <code>aria-valuemax</code> in pixels, kept up to date as the width is dragged or stepped.</p></li><li><p><strong>A <code>CodeBlock</code> range that runs past the code no longer freezes the page.</strong> <code>highlightLines=&quot;10-100000000&quot;</code> counted every number in the range into a set before drawing anything, which held the page for seconds and then threw a <code>RangeError</code> that took the render down. A range is held to the lines the block has.</p></li><li><p><strong>A <code>threshold</code> outside 0–1 no longer takes the page down.</strong> Every <code>Animate*</code> component and <code>useOnScreen</code> hand <code>threshold</code> to an <code>IntersectionObserver</code>, which throws a <code>RangeError</code> for <code>1.2</code> or <code>-0.1</code>, from an effect, so nothing caught it. A threshold is held to the range now, and a <code>NaN</code> is treated as <code>0</code>.</p></li><li><p><strong>A translation&#39;s placeholders are filled the same way everywhere.</strong> <code>Tour</code>, <code>HowToSteps</code> and <code>Gallery</code> filled theirs with an older helper that looked a <code>{name}</code> up with a plain index, so a registered translation containing <code>{constructor}</code> printed <code>function Object() { [native code] }</code> into the counter. They use the one helper the rest of the library does, which leaves a placeholder it has no value for as it was written.</p></li><li><p><strong>A disabled <code>TextField</code>, <code>NumberField</code> or <code>Toggle</code> shows the not-allowed cursor.</strong> Each carried its ordinary cursor on every render and <code>cursor-not-allowed</code> beside it when disabled, and two cursor utilities on one element are decided by the order Tailwind emits them in: a disabled field showed an I-beam and a disabled toggle a pointer. The cursor is chosen with the state now, so each element carries one.</p></li><li><p><strong>A <code>fullWidth</code> <code>ButtonGroup</code>, <code>ToggleGroup</code> or <code>SegmentedButton</code> is a block.</strong> Each kept <code>inline-flex</code> in its base classes and added <code>flex</code> for <code>fullWidth</code>, and two display utilities on one element are decided by stylesheet order, so the group stayed <code>inline-flex w-full</code> and sat on the text baseline like a word. The display is chosen with <code>fullWidth</code> now.</p></li><li><p><strong>A child put in front of the others no longer remounts the ones after it in <code>Stack</code>, <code>Timeline</code> and <code>Carousel</code>.</strong> Each drew a wrapper around every child and keyed it by position, which threw the caller&#39;s own keys away: a new first avatar, step or slide handed every later wrapper a different child, so React mounted all of them again, reloading their images, dropping their state and replaying a <code>Stack</code>&#39;s entrance. The wrappers take the child&#39;s key now.</p></li><li><p><strong>An <code>Animate*</code> with <code>trigger=&quot;hover&quot;</code> runs the caller&#39;s <code>onPointerEnter</code>, <code>onPointerLeave</code>, <code>onFocus</code> and <code>onBlur</code> beside its own.</strong> Fourteen of them spread the trigger&#39;s handlers over the caller&#39;s and threw those away, and <code>AnimateTyping</code>, <code>AnimateMarquee</code> and <code>AnimateHeadline</code> spread them the other way round, so a caller&#39;s handler took the trigger off and the effect never started.</p></li><li><p><strong>A <code>Tooltip</code> given an <code>id</code> still describes its trigger.</strong> The <code>id</code> replaced the generated one on the popup while the trigger&#39;s <code>aria-describedby</code> kept the generated one, so a screen reader was pointed at an element that did not exist and read nothing. The popup takes the caller&#39;s <code>id</code> and the trigger points at it.</p></li><li><p><strong>A <code>style</code> on a <code>TimelineItem</code> is merged with the item&#39;s own slots.</strong> It went through the props spread and replaced the whole inline style, so any <code>style</code> at all took away the bullet size and the colour slots and drew the step with a bullet of no size. The caller&#39;s declarations are laid over the slots now.</p></li><li><p><strong>A <code>className</code> in the <code>imageProps</code> of an <code>Avatar</code> or an <code>AppLogo</code> is added to the picture&#39;s own classes.</strong> It went through the spread and replaced them, so <code>imageProps={{ className: &#39;grayscale&#39; }}</code> took away <code>size-full object-cover</code> on an Avatar and <code>object-contain</code> with its bounds on an AppLogo, and the picture spilled out of its circle or tile.</p></li><li><p><strong>A chart whose values all lie past a pinned axis end still draws its axis.</strong> A <code>LineChart</code> or <code>ScatterChart</code> given <code>yAxis={{ min: 0 }}</code> over values that are all below zero, or any chart with a <code>max</code> below all of its values, worked out a value range that ran backwards, so the step and the top of the scale were <code>NaN</code>, no tick was written and the marks were drawn far outside the plot. The pinned end now stays where it was put and the scale opens past it. A flat series under one pinned end keeps that end too, where it used to be moved to open the band.</p></li><li><p><strong>A chart writes the name of its left axis above the plot, and on a horizontal chart writes it at all.</strong> A vertical chart&#39;s <code>yAxis.label</code> was drawn eight pixels above the plot, which on most charts is above the top of the box, while the room for it was taken out of the band beside the ticks, where nothing was drawn. A horizontal chart&#39;s <code>xAxis.label</code> took that band too and was never drawn. The name of the axis along the left edge now takes a band along the top of the box and is drawn in it, so the plot is a little shorter and a little wider than before. A <code>hidden</code> axis draws no name, as its props say.</p></li><li><p><strong>A <code>CommandPalette</code> opens with an empty search after a command closed it.</strong> The query was cleared only when Escape or the scrim closed the sheet, so running a command, or turning a controlled <code>open</code> off, left the last search in the field and the next open showed a filtered list. It is cleared whenever the palette closes, however that happens.</p></li><li><p><strong>The highlight of a <code>SegmentedButton</code> or a <code>FloatingBottomNavigation</code> goes away when nothing matches <code>value</code>.</strong> A <code>value</code> no segment or destination carried left the tile where the last choice had been, which drew a choice that was no longer made. The tile is hidden until something matches again, and then appears in place.</p></li><li><p><strong>A <code>FloatingBottomNavigation</code> highlight lands on the whole destination when two names are the same width.</strong> Under <code>labels=&quot;selected&quot;</code> the tile was measured as the pressed destination&#39;s name set off, before it had any width, and only a change in the bar&#39;s own width sent it on from there. Two names of the same width leave the bar as wide as it was, so the tile stopped short and narrow beside the destination. It is measured again when the names finish their transition.</p></li><li><p><strong>A read-only <code>Checkbox</code> in the mixed state is filled.</strong> The read-only tick filled for <code>checked</code> and not for <code>indeterminate</code>, so <code>readOnly</code> with <code>indeterminate</code> drew a light dash on an empty box, which did not read as mixed. It is filled the way an editable mixed tick is, and desaturated like the rest of the read-only state.</p></li><li><p><strong>A <code>ColorPicker</code> reads an <code>rgb()</code> channel written as a percentage.</strong> <code>rgb(100% 0% 0%)</code> was read as a red channel of 100 out of 255 and became <code>#640000</code>, a dark red. A percentage channel is a share of 255 now, so it is <code>#ff0000</code>; a percentage alpha was already read correctly.</p></li><li><p><strong><code>marks</code> on a <code>Slider</code> stepped in fractions keeps its last mark.</strong> The marks for <code>marks={true}</code> were counted by flooring the range over the step, and <code>0.6 / 0.1</code> is <code>5.999999999999999</code>, so a slider from 0 to 0.6 in tenths drew no mark at 0.6. The count now allows for the rounding.</p></li><li><p><strong>A <code>DateTimePicker</code> blocks the right hours on a day the clocks change.</strong> A row was compared with <code>minDate</code> and <code>maxDate</code> as midnight plus a fixed number of hours, and a day the clocks go back is 25 hours long, so in New York on 1 November 2026 a minimum of 09:30 blocked the whole of hour 9 and 09:30 to 09:59 could not be chosen. A spring-forward day was an hour out the other way. Each row&#39;s first and last instant is now set on the day&#39;s wall clock.</p></li><li><p><strong>A date picker whose format writes a weekday is sized to its widest date.</strong> A trigger is held at the width of the longest string its format can produce, measured over twenty-four sample dates, and those dates fell on no Friday and paired each month with only two weekdays. A <code>dateStyle: &#39;full&#39;</code> picker in Greek was sized for 27 characters of a real 29, so choosing a Friday in February widened the field. A format with a weekday is measured over every month against every weekday now, and the samples are worked out once per locale and format rather than on every render.</p></li><li><p><strong>A virtual <code>DataTable</code> scrolled far down still draws rows after a search narrows it.</strong> Scrolled to the bottom of five thousand rows and then searched down to ten, the table kept its old scroll offset, started its window of rendered rows past the last one and drew nothing, while the spacer standing in for the rows above held the scroll where it was, so the body stayed empty. An offset past the end now draws the last screen of rows, and the table shrinks back to the rows it has.</p></li><li><p><strong>The arrow keys keep a <code>DataTable</code>&#39;s active row on screen.</strong> A row&#39;s place was counted from the top of the scrolling box, so a table with a caption, a head or group headings scrolled too little and left the row the arrows had reached below the edge — by the height of the head and caption, and by one more heading for every group above it. A table with no <code>height</code> did not scroll at all, so arrowing past the bottom of the window lost the row. The row is measured where it is now, and a table without a height scrolls the page.</p></li><li><p><strong>Dragging a run of rows in a grouped <code>DataTable</code> takes the row under the pointer.</strong> The drag read the body as rows of one height from its top, and each group heading is one more row than that, so a drag onto a row took the one after it, or more. The row is now found by where the rows are drawn.</p></li><li><p><strong>A mouse click reaches the rows of a <code>DataTable</code> that selects many, and the sort buttons of one whose columns can be dragged.</strong> Pressing a row with <code>selectionMode=&quot;multiple&quot;</code> captured the pointer to the table in case the press became a drag, and a captured pointer&#39;s <code>click</code> and <code>dblclick</code> go to the capturing element, so <code>onRowClick</code>, <code>onRowActivate</code> on a double click and a cell&#39;s editor never ran for a mouse in Chromium, Firefox or WebKit. A header with <code>reorderable</code> did the same to its own sort button. Both drags now follow the pointer from the document instead, and still carry on past the edge of the table.</p></li><li><p><strong>An emptied number cell in a <code>DataTable</code> writes no zero.</strong> <code>Number(&#39;&#39;)</code> is <code>0</code>, so clearing a cell with <code>editType: &#39;number&#39;</code> called <code>onCellEdit</code> with <code>0</code>, and so did opening an empty number cell and leaving it. An empty number field is treated like one holding no number, and the edit is dropped.</p></li><li><p><strong>A <code>DataTable</code> with ticks and a column pinned to the start keeps both in place as it scrolls sideways.</strong> The pinned columns were offset by the width of the tick column, but the tick column itself scrolled away, which left a gap that width in front of them with the scrolling cells showing through. The tick column is frozen with them now. A pinned heading was also stacked under the sticky headings beside it, so under <code>stickyHeader</code> the headings scrolling past were drawn over the frozen one; it is stacked above them.</p></li><li><p><strong>Dragging a <code>DataTable</code> column reports the new order once.</strong> The order was worked out and committed inside two nested state updaters, and React&#39;s StrictMode runs every updater twice, so <code>onColumnOrderChange</code> was called twice for one drag in development. The drag reads what it needs when it ends and commits outside them.</p></li><li><p><strong>A <code>DataTable</code> rings its sheet only when the table has the focus.</strong> The ring around a selectable table answered any focus inside it, so the search field drew its own ring inside a second ring around the whole table, which said the table had the focus when it did not; the footer&#39;s page-size Select did the same. It answers the table itself and the cell editor now, which is the one control in it with no ring of its own.</p></li><li><p><strong>A <code>DataTable</code> heading being dragged is marked with a wash instead of being faded.</strong> The heading carried along the row was drawn at 60% opacity, which faded the label saying which column it was, and state is not carried on opacity anywhere else in the library. It takes the soft wash a dragged <code>Sidebar</code> or <code>Panes</code> handle takes, over its own opaque ground, and the grabbing cursor while it moves.</p></li><li><p><strong><code>wholeWord</code> on a <code>Highlight</code> is ignored for a RegExp <code>query</code>, as documented.</strong> A regular expression says its own boundaries, and the props said <code>wholeWord</code> was ignored for one, but it went on dropping every match that was not a whole word. <code>query={/cat/} wholeWord</code> now marks what the expression matches.</p></li><li><p><strong>An <code>outline</code> <code>Highlight</code> no longer moves the text after it.</strong> Each mark takes a hair of padding and gives it back as a negative margin, but the outline&#39;s 1px border was not given back, so every mark was two pixels wider than its word and the rest of the line shifted each time a search found one more. The margin now takes the border back too.</p></li><li><p><strong>A shortcut on a letter no longer fires for the letter in the same place on another layout.</strong> When the key typed was not the one named, a shortcut fell back to the physical key, even when the layout had typed a different Latin letter there. On AZERTY the Z sits where QWERTY has W, so Ctrl+Z fired both <code>Ctrl+Z</code> and <code>Ctrl+W</code> in <code>useShortcut</code>, a <code>CommandPalette</code> and a field&#39;s <code>shortcuts</code>. The fallback is kept for a key that types something other than a Latin letter or a digit, such as <code>Alt+K</code> on a Mac or a Cyrillic layout.</p></li><li><p><strong>A shortcut does not fire in the middle of a word typed through an input method.</strong> Korean, Japanese and Chinese build a character over several keydowns, and every one of them reached the page, so a <code>Mod+Enter</code> bound on a field, or bound with <code>useShortcut</code> and <code>ignoreWhileTyping: false</code>, could fire while a syllable was still being composed and send it half written. A keystroke an input method is holding, marked by <code>isComposing</code> or a <code>keyCode</code> of 229, matches no shortcut now. This covers <code>useShortcut</code>, a <code>CommandPalette</code>&#39;s opener and a field&#39;s <code>shortcuts</code>.</p></li><li><p><strong><code>lines</code> on a <code>Typography</code> or an <code>AccordionItem</code> clamps to any count.</strong> Each count from two to six had a class of its own, and anything larger fell back to the class for six, so <code>lines={8}</code> cut the text off at six lines without a word. The count is written into an <code>--n-lines</code> slot that one clamp class reads, the way the library passes every other number to its styles.</p></li><li><p><strong>An <code>Image</code> preview opens the picture from the same sources.</strong> The enlarged picture in the preview took only <code>src</code>, so an <code>Image</code> given a <code>srcSet</code> and no <code>src</code> opened an empty dialog, and a picture served behind a CORS or referrer rule was requested again without its <code>crossOrigin</code> and <code>referrerPolicy</code>. The preview takes all three now. It does not take <code>sizes</code>, which describes the thumbnail, so the browser picks a candidate for the size of the dialog.</p></li><li><p><strong>A <code>Gallery</code> tile zooms on a transition under <code>hover=&quot;zoom&quot;</code>.</strong> The Gallery wrote a <code>transition</code> of its own onto each picture beside the one <code>Image</code> already carries, and two shorthands on one element are decided by stylesheet order: the Image&#39;s won, it did not name <code>transform</code>, and the zoom jumped to full size. The picture&#39;s own transition names <code>transform</code> now, and the Gallery no longer writes a second one.</p></li><li><p><strong>A <code>CodeBlock</code> shows new <code>code</code> as soon as it is given.</strong> The colouring is worked out after the code changes, and until it arrived the block went on drawing the colouring of the previous code, while the copy button already copied the new one; with a language whose grammar still had to load, the old code stayed on screen for the length of the download. A colouring is kept with the code and the language it was made from, and new code is drawn plain until its own arrives.</p></li><li><p><strong>A <code>ButtonGroup</code> or <code>ToggleGroup</code> beats a <code>NebaProvider</code> for the buttons inside it.</strong> The provider&#39;s defaults were filled in before a <code>Button</code> or a <code>Toggle</code> read its group, so a group&#39;s <code>size</code>, <code>density</code> or <code>variant</code> lost to the provider&#39;s: <code>&lt;ButtonGroup size=&quot;lg&quot;&gt;</code> under <code>defaults={{ size: &#39;sm&#39; }}</code> drew small buttons. The order is the button&#39;s own prop, then its group, then the provider, then the default.</p></li><li><p><strong>A <code>NebaProvider</code>&#39;s defaults reach every component that takes the axis.</strong> Some components never asked for them and others asked for part: <code>SegmentedButton</code>, <code>RadioGroup</code>, <code>Tabs</code>, <code>Timeline</code>, <code>Form</code>, <code>Fieldset</code>, <code>Menu</code>, <code>ContextMenu</code>, <code>NavigationMenu</code>, <code>LineChart</code>, <code>AreaChart</code> and <code>useConfirm</code>&#39;s sheet ignored the provider, the date pickers took their <code>size</code> and not their <code>density</code> or <code>variant</code>, the Cartesian charts took no <code>variant</code> or <code>locale</code>, and <code>AnimateCounter</code>, <code>AnimateScramble</code> and <code>AnimateSplit</code> took no <code>locale</code>. Each fills in every axis it declares now.</p></li><li><p><strong>A <code>TextLink</code> takes no <code>size</code> from a <code>NebaProvider</code>.</strong> A link in a sentence is the size of the sentence, and a provider with <code>size: &#39;lg&#39;</code> turned every link in running text into 15px type. A link given <code>size</code> itself still takes it.</p></li><li><p><strong>A disabled <code>Accordion</code> section or <code>Collapsible</code> looks disabled.</strong> Base UI keeps a disabled trigger focusable, so it never carries the <code>disabled</code> attribute, and the <code>disabled:</code> classes that grey it out never matched: a disabled section kept its ink, its hover wash and its pointer. The state is decided in the component now, for a section, for a whole disabled Accordion and for a Collapsible.</p></li><li><p><strong>The radios of a disabled <code>RadioGroup</code> look disabled.</strong> The group&#39;s <code>disabled</code> reached Base UI, which stopped the radios answering, and never reached the <code>Radio</code>s&#39; own drawing, which read only their own prop, so a disabled group looked available. A Radio takes the group&#39;s <code>disabled</code> when it has none of its own.</p></li><li><p><strong>A disabled <code>SegmentedButton</code> drops its colour family.</strong> It had no disabled drawing of its own: the groove and the tile kept the family&#39;s colours, and the chosen segment&#39;s grey label sat on the solid fill, where the checked ink and the disabled ink were decided by stylesheet order. The groove and the tile take the disabled ground a Button does, every label takes the disabled ink, and the tile under one disabled segment in an enabled set is greyed as well.</p></li><li><p><strong>A disabled <code>Slider</code> is drawn in the disabled colours rather than faded.</strong> It was the whole slider desaturated and at 70% opacity, which took its label down with the control and put the state on the one axis no other state in the library uses. The rail, the fill and the thumb take the disabled ground, ink and hairline a Button uses, and the label keeps the disabled ink it already had.</p></li><li><p><strong>A field inside a disabled <code>Fieldset</code> looks disabled.</strong> Base UI&#39;s Fieldset stopped every field inside it answering, but each Neba field draws itself from its own <code>disabled</code> prop and cannot read Base UI&#39;s fieldset state, so a disabled group of fields looked available. <code>TextField</code>, <code>NumberField</code>, <code>OtpField</code>, <code>Select</code>, <code>Combobox</code>, <code>Checkbox</code>, <code>RadioGroup</code>, <code>Switch</code> and the date and time pickers now take the Fieldset&#39;s <code>disabled</code> as well as their own.</p></li><li><p><strong>A <code>Dialog</code>, <code>Overlay</code> or <code>Drawer</code> that is not fully modal leaves the page clickable.</strong> With <code>modal={false}</code> or <code>modal=&quot;trap-focus&quot;</code> the page is meant to stay usable, but the scrim and the full-screen viewport the popup is centred in both covered it, so nothing behind them could be clicked. They let the pointer through now, and the popup takes it back. A fully modal one covers the page as before.</p></li><li><p><strong>A <code>PieChart</code> share is written in the ink its slice reads best under, and the plot answers <code>Home</code>, <code>End</code> and <code>Escape</code>.</strong> Every label written by <code>valueLabels=&quot;all&quot;</code> wore the surface colour, which is about 4:1 on the light theme&#39;s slots and disappears on a pale slice a caller coloured. Each of the eight slots now has its ink in <code>--neba-chart-on-1</code> to <code>--neba-chart-on-8</code>, per theme, and a slice given a literal colour gets black or white by contrast. The accessibility notes promised <code>Escape</code> to clear the selection, and only the arrow keys existed; <code>Home</code> and <code>End</code> now go to the first and the last slice, and <code>Escape</code> lets go of a selection.</p></li><li><p><strong>A treemap <code>HeatmapChart</code>&#39;s hidden table puts every value under its own name.</strong> Its column headings were taken from the first group&#39;s tile names, so a second group&#39;s tiles were written under the first group&#39;s headings and a screen reader read the wrong figure against each name. The columns are now every tile name the groups use, in the order they first appear, and a group&#39;s row is blank under a name it does not have.</p></li><li><p><strong>A tap on a chart pins its tooltip until a press lands outside the plot.</strong> On a touch screen a <code>LineChart</code>, <code>AreaChart</code>, <code>BarChart</code>, <code>ScatterChart</code>, <code>TimelineChart</code>, <code>PieChart</code> or <code>HeatmapChart</code> cleared its tooltip on <code>pointerleave</code>, which a finger sends the moment it lifts, so a tap showed the tooltip for a frame at most; a tap that did not move never read a point at all, because only <code>pointermove</code> did. A tap now reads the nearest point and keeps its tooltip up, and a press anywhere outside the plot puts it down. A mouse still clears it by leaving.</p></li><li><p><strong>A <code>Switch</code> under RTL puts &quot;on&quot; at the end of the line.</strong> The thumb travelled on a physical <code>left</code>, so in a right-to-left page it still sat on the left when off and moved right when on, the reverse of what the reader expects. It now travels on <code>inset-inline-start</code>, and on and off swap ends with the direction. A caller who moved the thumb through <code>classNames.thumb</code> with a <code>left-*</code> utility should use <code>start-*</code>.</p></li><li><p><strong>A <code>Transfer</code>&#39;s move arrows point the way the items go under RTL.</strong> Both glyphs were turned for a left-to-right layout only, so on a right-to-left page, where the chosen list sits on the left, each arrow pointed away from the list it moves items into. Both now turn the other way under RTL.</p></li><li><p><strong>A <code>Panes</code> boundary moves towards the arrow pressed under RTL.</strong> A drag already followed the direction, but ArrowLeft and ArrowRight on an upright bar moved the boundary as if the page ran left to right, so on a right-to-left page each key moved it the other way from the arrow. The keys now follow the direction too.</p></li><li><p><strong>A calendar&#39;s left and right arrows follow RTL.</strong> The day, month and year grids are laid out in the reading direction, but ArrowLeft always went back and ArrowRight always went forward, so on a right-to-left page each arrow moved the focus away from the cell it pointed at. Under RTL ArrowLeft is now the next day, month or year. This covers <code>Calendar</code> and every date picker.</p></li><li><p><strong>The <code>Gallery</code> viewer&#39;s arrow keys follow RTL.</strong> Its Previous and Next buttons already swapped sides on a right-to-left page, but ArrowRight still went to the next picture, so the key pointing at the Next button went back. ArrowLeft is now the next picture under RTL.</p></li><li><p><strong>A striped <code>Table</code> shows its stripes on a white page.</strong> Every other row took the panel hover token, which is 82% white, so on a white page the stripes were not there at all. They now take the 4% mix of the ink that a striped <code>DataTable</code> already uses, which shows on any surface and in both themes.</p></li><li><p><strong>A vertical <code>DataList</code> keeps each label with its own value.</strong> With <code>orientation=&quot;vertical&quot;</code> the gap between every child fell between a label and its value as well as between pairs, and the label&#39;s own margin made the first of those larger, so a label read as belonging to the value above it. <code>dividers</code> drew its rule above every <code>&lt;dd&gt;</code> too, between a label and its value. The space now goes above each label after the first, and the rule runs only between pairs. A horizontal list is unchanged.</p></li><li><p><strong>A nested <code>Anchor</code> row marks its place on the rail.</strong> On the <code>rail</code>, a row with a <code>depth</code> was indented with a margin, which moved its whole box and the <code>border-s</code> highlight with it, so the active nested row was marked a step away from the rail rather than on it. Nested rows on the rail are now indented with <code>padding-inline-start</code> inside a box that starts at the rail. Without the rail the indent is unchanged.</p></li><li><p><strong>A server-rendered <code>Calendar</code> or <code>NebaProvider</code> hydrates without a mismatch.</strong> A calendar marked today while it rendered, so a server in UTC and a reader in Seoul disagreed about which day to mark for nine hours of every day, and React reported the hydration failing; the month and year grids did the same with the current month and year. <code>NebaProvider</code> read the stored colour scheme in its first render, which a server has no <code>localStorage</code> for, so the server rendered the default and a returning reader&#39;s browser rendered their choice. Today is now marked, and the stored scheme read, once the page has hydrated; <code>colorSchemeScript()</code> still puts the stored scheme on <code>&lt;html&gt;</code> for the first paint, and the provider does not write the attribute until it has read the same value. The chart docs now tell a server-rendered page to pass <code>locale</code>, since a chart without one writes dates in the language and time zone of wherever it renders.</p></li><li><p><strong>A <code>stacked=&quot;full&quot;</code> chart writes the caller&#39;s numbers through <code>format</code> and shares the hundred between the series still shown.</strong> An <code>AreaChart</code> or <code>BarChart</code> stacked to full kept each original value as <code>String(value)</code>, so its tooltip and its table ignored <code>format</code> and the locale and read <code>1234.5678</code> where the rest of the chart said <code>1,235</code>. Hiding a series in the legend left it counted in each category&#39;s total, so the bars and bands that remained stopped short of 100%. The originals are now written through the chart&#39;s own <code>format</code> and <code>locale</code>, and the total counts only the series still shown. The normalisation, which both charts had a copy of, is done once in the shared chart frame.</p></li><li><p><strong>A chart takes any CSS length as its <code>height</code>.</strong> The type and the docs said a string was any CSS length, but a <code>LineChart</code>, <code>AreaChart</code>, <code>BarChart</code>, <code>ScatterChart</code> or <code>TimelineChart</code> given <code>height=&quot;16rem&quot;</code> drew into a plot 0 pixels tall and showed nothing, and a <code>PieChart</code>, <code>HeatmapChart</code> or <code>GaugeChart</code> ignored the string and took its <code>size</code>. A string height is now written on the chart&#39;s box and the drawing is laid out against the height that box comes to, following it when it changes. A number is still used as it is, with nothing measured.</p></li><li><p><strong>A stacked <code>LineChart</code> or <code>AreaChart</code> stacks negative values down from zero.</strong> The value axis summed each category&#39;s positive and negative values apart, but the lines and bands added every value together, so a series below zero pulled every band above it down: the top of the stack no longer met the axis, and bands drew over each other. Positive values now stack up from the zero line and negative ones down from it, as the axis and a stacked <code>BarChart</code> already did.</p></li><li><p><strong>A chart keeps the same series hidden when new data reorders them.</strong> The legend remembered hidden series by their position, so a refresh that brought the same series back in another order hid whichever series now sat where the hidden one had been, and a series added later with <code>hidden</code> was drawn anyway because the prop was only read on the first render. What the reader chose is now remembered by each series&#39; <code>name</code>, falling back to its index when it has none, and a series nobody has toggled follows its own <code>hidden</code>.</p></li><li><p><strong>A <code>DataTable</code> that takes the focus without selecting rings its sheet.</strong> A table with <code>selectionMode=&quot;none&quot;</code> that still takes a tab stop, because its rows have <code>onRowActivate</code> or a column is editable, drew no focus indicator at all when it was tabbed to, and nothing showed until an arrow key marked a row. It now rings its sheet while the table holds the focus, as a selecting table already did.</p></li><li><p><strong>A <code>Combobox</code> with a <code>limit</code> still offers to add what was typed.</strong> The row that adds a value the list does not have came last, and <code>limit</code> cut the list to that many rows, so once enough options matched the row was cut off: the typed value could not be added, and Enter chose the first option instead. <code>limit</code> now counts options only, and the row is drawn after as many of them as it allows.</p></li><li><p><strong>A <code>FilePicker</code> in a form submits the files it lists.</strong> Its hidden input held only the batch last picked in the browser dialog, so a dropped file was never submitted and a file removed from the list still was, and a <code>readOnly</code> picker disabled the input, so a form got nothing from it at all. The input now holds exactly the files in the list, written back whenever the list changes and again when the dialog is dismissed, and only <code>disabled</code> takes it out of the form. <code>required</code> still applies while the list is empty. The <code>maxFiles</code> docs said it implied <code>multiple</code>; it is counted only when <code>multiple</code> is on, and now says so.</p></li><li><p><strong>A <code>Transfer</code> keeps a value it has no row for, whichever way rows are moved.</strong> An id in <code>value</code> that <code>items</code> did not list — a row not loaded yet, or one filtered out before it reached the component — disappeared from the value the first time rows were sent to the right, and stayed when rows were sent back to the left. It now stays in both directions, after the listed rows.</p></li><li><p><strong>A <code>NumberField</code>&#39;s <code>ref</code> is its input.</strong> It was a plain function component, so under React 18 a <code>ref</code> was dropped with a warning, and under React 19 it was passed along as a prop to the root <code>&lt;div&gt;</code>, so a form library focusing the field that failed validation focused nothing a reader can type into. It now forwards the ref to the <code>&lt;input&gt;</code>, as <code>TextField</code> does. Code that read the root <code>&lt;div&gt;</code> through the ref under React 19 gets the input instead.</p></li><li><p><strong><code>IconButton</code>s in a <code>ButtonGroup</code> join like other buttons.</strong> An IconButton drew itself round with an inline <code>border-radius</code>, which beat the classes a ButtonGroup uses to square off the corners between neighbours, so a row of icon buttons overlapped as circles. It now reaches round through the radius ladder Button&#39;s own class reads, so the group&#39;s joined corners apply and the ends of the row stay round. A caller&#39;s <code>style</code> still overrides the radius.</p></li><li><p><strong>A <code>Rating</code> without <code>name</code> submits nothing.</strong> Its radios were given a generated <code>name</code> so they would act as one group, and a form then submitted that name as a field of its own, so a form&#39;s data gained an entry like <code>«r3»=4</code>. An unnamed Rating&#39;s radios still share a name, so they keep one Tab stop and the arrow keys still move between the stars, but they belong to no form. Pass <code>name</code> for a Rating a form should submit; without it <code>required</code> has nothing to hold back.</p></li><li><p><strong>Clicking a <code>FloatingActionButton</code> with a mouse no longer shuts the dial the pointer just opened.</strong> With <code>openOnHover</code>, the default, the dial opened as the pointer reached the button, and the click that followed toggled it straight back shut, so a mouse user who clicked the button saw the dial flash and close. A click on a dial the pointer opened now keeps it open, and the next click closes it. A dial opened by a press still closes on the next press.</p></li><li><p><strong>A <code>Calendar</code>&#39;s <code>elevation</code> draws a shadow, and <code>bordered</code> draws the popup&#39;s own sheet.</strong> <code>elevation</code> wrote the shadow slot every surface writes, but nothing on the calendar read it, so the prop changed nothing. <code>bordered</code> drew a sheet of its own with one padding for every size and no glass edge, although the prop says it draws the sheet a picker&#39;s popup draws. The calendar now takes its shadow from <code>elevation</code>, with or without the sheet, and a bordered calendar draws the popup&#39;s edge and its padding for each <code>size</code>.</p></li><li><p><strong>A picker in a form submits nothing while disabled, and <code>required</code> holds back an empty one.</strong> The value of a <code>DatePicker</code>, <code>DateRangePicker</code>, <code>TimePicker</code>, <code>DateTimePicker</code>, <code>TreeSelect</code> or <code>ColorPicker</code> goes out through hidden inputs, which were submitted even while the picker was <code>disabled</code>, and which a browser never validates, so <code>required</code> only reached the trigger&#39;s ARIA and an empty picker let the form submit. The hidden inputs are now disabled with the picker, and a required picker that is empty blocks the submit through the form&#39;s own validation and hands the focus to its trigger. A read-only picker is not held back, as a read-only input is not.</p></li><li><p><strong>A <code>DataTable</code> paged by its caller keeps the rows chosen on other pages.</strong> With <code>manual={[&#39;pages&#39;]}</code> the table only holds the page it was given, and every Ctrl-click, range or press of the header tick rebuilt the selection from those rows, so choosing a row on page 2 dropped everything chosen on page 1. A key already chosen is now kept when its row is not on the page. <code>onSelectedChange</code> still hands over the row behind each key the table was given; a key chosen on another page of a manual table comes without one.</p></li><li><p><strong>A grouped <code>DataTable</code> heads its ungrouped rows &quot;No group&quot;.</strong> Rows <code>groupBy</code> put in no group were headed by the empty-state text, &quot;Nothing here&quot;, above rows that were plainly there. They now take the <code>table</code> namespace&#39;s new <code>noGroup</code> word, in all eighteen registered languages and in the table&#39;s <code>locale</code>.</p></li><li><p><strong>A <code>DataTable</code> writes a <code>Date</code> in a column without <code>render</code> as a date.</strong> Sorting and the CSV export already understood a <code>Date</code>, but a cell with no <code>render</code> of its own handed the object to React, and the whole table failed with &quot;Objects are not valid as a React child&quot;. Such a cell is now written as a date in the table&#39;s <code>locale</code>. A column that needs another shape still passes <code>render</code>.</p></li><li><p><strong>A <code>Breadcrumb</code> folds again when its steps change.</strong> Once the <code>…</code> was pressed the trail stayed unfolded for as long as it was mounted, so a layout that keeps the breadcrumb across a route change showed every later trail in full.</p></li><li><p><strong><code>Shortcut</code> draws the Mac modifiers in the order macOS uses.</strong> <code>Mod+Shift+P</code> drew <code>⌘⇧P</code>; it now draws <code>⇧⌘P</code>, as every menu on a Mac and the docs do, whatever order the modifiers were written in. Windows and Linux keep the written order.</p></li><li><p><strong>A shortcut on a punctuation key fires with Shift held.</strong> <code>useShortcut(&#39;?&#39;)</code>, as the hooks guide shows it, never fired: <code>?</code> is typed with Shift and the modifiers were matched exactly. For a single key that is neither a letter nor a digit, Shift is no longer compared, so <code>?</code> fires however the layout types it. Letters and digits still need <code>Shift</code> written.</p></li><li><p><strong><code>Typography</code> with no <code>color</code> inherits.</strong> Every level was pinned to <code>--neba-fg</code>, so a Typography inside a solid Alert drew dark text on the dark fill, and a host&#39;s heading colour never reached it. It now states no colour and takes the one around it, as the docs said; <code>caption</code> and <code>overline</code> stay muted.</p></li><li><p><strong><code>align</code> and <code>gutter</code> work on a <code>caption</code> and an <code>overline</code>.</strong> Both levels are <code>&lt;span&gt;</code> elements, where <code>text-align</code> and a bottom margin do nothing. Either prop now makes them a block.</p></li><li><p><strong>A tiled <code>Image</code> watermark takes a colour token.</strong> <code>repeat: true</code> drew the mark as an SVG data URI, a separate document where <code>var(--neba-…)</code> resolves to nothing, so a mark given a token drew black. It is now an inline <code>&lt;svg&gt;</code> pattern and the colour resolves in the page.</p></li><li><p><strong><code>CodeBlock</code> answers Ctrl/Cmd+A on any keyboard layout, and draws no empty toolbar.</strong> The shortcut checked only <code>event.key</code>, so on a Russian or a Greek layout it selected the whole page; the physical A key now counts too. A block with no <code>language</code>, <code>copyable={false}</code> and no <code>title</code> drew a bar with nothing on it.</p></li><li><p><strong><code>CodeBlock</code> tries again after the highlighter failed to load, and a registered language wins over an alias.</strong> A core chunk that failed once stayed failed, so every block on the page was plain until a reload. <code>registerLanguage</code> under a name that is also an alias, such as <code>vue</code>, was shadowed by the alias.</p></li><li><p><strong>Initials keep a whole emoji.</strong> An <code>Avatar</code> or an <code>AppLogo</code> named <code>👩‍💻 Dev</code> drew <code>👩D</code>, and <code>🇰🇷 Team</code> drew half a flag, because the first letter was taken by code point. It is now taken by grapheme.</p></li><li><p><strong><code>Statistic</code> writes its percentage in the <code>locale</code>, and a delta that rounds to zero is flat.</strong> The percentage was written with <code>toFixed</code>, so <code>de-DE</code> showed <code>+71.4%</code> beside a figure written <code>1.250</code>. The arrow, the sign and the colour came from the unrounded difference, which put an up arrow next to <code>+0%</code>; they now follow what is written. A <code>NaN</code> on either side writes no delta instead of <code>NaN%</code>.</p></li><li><p><strong><code>Sparkline</code> bars grow from zero inside the strip, and a level line sits in its middle.</strong> An all-negative <code>shape=&quot;bar&quot;</code> put its baseline above the box, so the bars covered the text above it. Equal values drew one-pixel bars and a line on the floor, both of which read as zero.</p></li><li><p><strong><code>BarChart</code> with <code>valueLabels=&quot;last&quot;</code> labels the last value there is.</strong> A series ending in <code>null</code> got no label, where LineChart writes it on the last value that is not a gap.</p></li><li><p><strong><code>PieChart</code> with <code>tooltip={false}</code> ignores the arrow keys.</strong> They still moved the active slice and dimmed the others, with no readout to say which slice it was.</p></li><li><p><strong><code>HeatmapChart</code> thins its column labels on one stride.</strong> The interval was worked out per label, so a short <code>Mar 9</code> and a longer <code>Mar 10</code> were both drawn and overlapped. The widest label now sets it for all of them.</p></li><li><p><strong><code>TimelineChart</code> writes a span&#39;s dates to the day.</strong> The tooltip, the table and the summary wrote them at the axis unit, so a two-week span on a one-year range read <code>Mar 2025 – Mar 2025</code>.</p></li><li><p><strong><code>TimelineChart</code> with a pinned <code>min</code> ticks on the calendar and skips the spans it does not draw.</strong> Ticks were walked from <code>min</code> itself, so <code>min</code> on 31 January put the next tick on 3 March and February had none. A span wholly outside <code>min</code> or <code>max</code> was not drawn but was still a stop for the arrow keys.</p></li><li><p><strong><code>ScatterChart</code> writes x and z as numbers.</strong> The table and the tooltip wrote x through <code>String()</code>, so the axis said <code>24K</code> while the table said <code>24000</code>, and <code>0.1 + 0.2</code> came out as <code>0.30000000000000004</code>. <code>z</code> took y&#39;s <code>format</code>, which put a currency sign on a population. Both are now written with the locale&#39;s number format.</p></li><li><p><strong>A partial <code>useToast().update</code> keeps the rest of the toast.</strong> <code>update(id, { description })</code> replaced <code>data</code> and <code>actionProps</code> with empty values, which took off the colour, the variant, the icon and the action button. Only the fields given are changed now, and a style prop given is merged over the toast&#39;s own.</p></li><li><p><strong>Dismissing an <code>alert: true</code> confirm resolves <code>true</code>.</strong> An alert has only the confirming button, and Escape or a press on the backdrop resolved <code>false</code>, which sent the caller down a cancel branch the reader was never offered.</p></li><li><p><strong><code>Tour</code> dims the page from its portal.</strong> The dim was a <code>fixed</code> element left where the Tour sits, so inside a Card with a <code>backdrop-filter</code> it was fixed to the card: only the card was dimmed and the hole was in the wrong place.</p></li><li><p><strong><code>Carousel</code> opens on a non-zero <code>defaultValue</code>.</strong> The first effect skipped scrolling altogether, so the dots said slide 3 while the track showed slide 1. It now moves the strip there at once.</p></li><li><p><strong>An uncontrolled inline <code>Drawer</code> closes from its ×.</strong> The button only called <code>onOpenChange(false)</code>, so without <code>open</code> managed by the caller it did nothing.</p></li><li><p><strong>A <code>Pill</code> that starts <code>expanded</code> is open from the first frame.</strong> Its details were drawn at a height of 0 and animated open after mount, which pushed the content below down. The height is now <code>auto</code> until it is measured, and it is measured before paint.</p></li><li><p><strong><code>WindowPane</code> moves and resizes in its own pixels inside a scaled ancestor.</strong> Sizes were read from <code>getBoundingClientRect()</code>, which includes transforms, so inside a Mockup at scale 0.3 one key press shrank the window to 180px and a drag followed only 30% of the pointer.</p></li><li><p><strong><code>WindowPane</code> hands the front on when the front window closes, keeps a double-click on its actions, and measures a window that starts rolled up.</strong> Closing the front window left every other window marked inactive. A double-click on a control in <code>actions</code> bubbled to the title bar and maximised the window. <code>defaultMinimized</code> skipped the measurement a roll-up makes, so the border clipped the title bar.</p></li><li><p><strong>The progress indicators announce what they draw.</strong> <code>value={0} max={0}</code> was drawn indeterminate while Base UI announced <code>aria-valuenow=0</code> and marked it complete, and <code>value={Infinity}</code> was drawn full while announced as no value. Both are indeterminate on screen and to a screen reader now; a <code>Meter</code> given <code>Infinity</code> reads 0% rather than 100%. <code>ProgressBox</code> with <code>count={NaN}</code> drew no plates and <code>count={Infinity}</code> threw; both draw one.</p></li></ul><h2 id="_1-13-0-2026-09-11" tabindex="-1">1.13.0 (2026-09-11) <a class="header-anchor" href="#_1-13-0-2026-09-11" aria-label="Permalink to &quot;1.13.0 (2026-09-11)&quot;">​</a></h2><h3 id="where-the-bytes-went-1" tabindex="-1">Where the bytes went <a class="header-anchor" href="#where-the-bytes-went-1" aria-label="Permalink to &quot;Where the bytes went&quot;">​</a></h3><table tabindex="0"><thead><tr><th>What you import</th><th>1.12.0</th><th>1.13.0</th></tr></thead><tbody><tr><td><code>Button</code></td><td>5.1 kB</td><td>5.2 kB</td></tr><tr><td><code>Chip</code></td><td>3.3 kB</td><td>3.4 kB</td></tr><tr><td><code>LineChart</code></td><td>11.6 kB</td><td>11.8 kB</td></tr><tr><td><code>CodeBlock</code></td><td>5.0 kB</td><td>5.1 kB</td></tr><tr><td><code>Image</code></td><td>6.5 kB</td><td>7.1 kB</td></tr><tr><td><code>Gallery</code></td><td>10.2 kB</td><td>10.4 kB</td></tr><tr><td>a whole page shell</td><td>29.0 kB</td><td>29.2 kB</td></tr><tr><td>12 components — a typical app</td><td>70.5 kB</td><td>71.0 kB</td></tr><tr><td>12 components, with Korean</td><td>72.9 kB</td><td>73.8 kB</td></tr><tr><td>25 components — a large one</td><td>115.7 kB</td><td>116.9 kB</td></tr><tr><td>all exports</td><td>263.9 kB</td><td>265.8 kB</td></tr></tbody></table><p><code>Image</code> is the row that moved. A picture that failed with an empty <code>alt</code> printed a hardcoded English sentence, so <code>Image</code> now reaches <code>internal/i18n.ts</code> and <code>internal/defaults.ts</code>, which costs it 0.5 kB. The last 0.1 kB is <code>width</code> and <code>height</code>, which <code>Gallery</code> also carries because it draws an <code>Image</code>.</p><p>The 0.1 kB on <code>Chip</code>, <code>CodeBlock</code>, <code>LineChart</code> and the page shell is the same machinery gaining an <code>Object.hasOwn</code> on each of its two lookups, and a shared bound under the memos in <code>internal/</code>.</p><p>The twelve- and twenty-five-component rows carry <code>internal/wheel.ts</code> on top of that, which is what a <code>Tabs</code> bar now takes the wheel with.</p><p>The last 0.1 kB on nearly every row is the focus ring: the resting declaration and the transition it travels on are two more entries in the class string every control carries. <code>neba/styles.css</code> moved 22.4 kB → 22.5 kB for the same reason, plus the breakpoint slots each responsive element now clears — which is the cost of a fix that could not be made anywhere else, since only the element that reads a slot can say it does not inherit one.</p><p>Registering a language ships that language&#39;s whole module, so the twenty picker strings and the two a Carousel&#39;s stop button needs land in it whether or not the page draws either: 2.8 kB now against 2.4 kB before. Those twenty used to be hardcoded English, which a Korean product could not reach at all.</p><h3 id="added-1" tabindex="-1">Added <a class="header-anchor" href="#added-1" aria-label="Permalink to &quot;Added&quot;">​</a></h3><ul><li><p><strong>Every portalled surface reads one z-index, <code>--neba-z-portal</code>.</strong> Fourteen of them carried a hardcoded <code>z-50</code>, which is a guess about a page the library cannot see — a site whose own fixed header sits above that had its menus and dialogs opening underneath its chrome, and the way out was a selector per surface. The token defaults to 50, and a host that needs another number sets it once on <code>:root</code>.</p></li><li><p><strong>A <code>BottomNavigationItem</code> declares the <code>target</code> and <code>rel</code> its link already rendered.</strong> The props were typed against a <code>&lt;button&gt;</code> and cast to an <code>&lt;a&gt;</code>, so a destination that opened in a new tab could only be written by handing the component something TypeScript said was impossible.</p></li><li><p><strong>A <code>ProgressLinear</code>, a <code>ProgressCircular</code> and a <code>Meter</code> take a <code>thickness</code>.</strong> The ladder decides the bar&#39;s height and the ring&#39;s stroke, and the ladder is five steps — so a speed-test dial or a page that is about one number had no way to draw a heavier line without moving the diameter with it. It is a number of pixels on all three, because that is what the ring&#39;s arithmetic already works in. A ring&#39;s stroke is held inside the ring it describes: past half the radius there is no hole left in the middle of it.</p></li><li><p><strong>A <code>Combobox</code> takes a <code>filter</code>, and <code>false</code> turns it off.</strong> The list was always narrowed here, against each option&#39;s visible label — so a search answered by a server, which is the case a combobox with hundreds of options exists for, had its results filtered a second time and lost every row that had matched on a keyword, a description or a synonym rather than on the label. <code>filter={false}</code> lets a narrowed list through; a function decides per option. The row that offers to add what was typed is exempt from both, since it is the query written out.</p></li><li><p><strong>A <code>Slider</code> draws <code>marks</code>, and takes <code>classNames</code> for the parts behind its root.</strong> Labelling points on a track — <code>1 / 100 / 250 / 500</code> under a count, &quot;realistic&quot; and &quot;abstract&quot; at the ends of a style axis — was a row a caller had to lay out themselves, absolutely positioned at <code>(value − min) / (max − min)</code> of a width they had to know. An array of <code>{ value, label? }</code> does it, and a bare <code>marks</code> is a tick at every <code>step</code>. The row sits beside the control rather than on the rail, because a tick on the rail has to stay legible over both the fill and the groove, and it is <code>aria-hidden</code>: the thumb already announces the value and the range.</p><p>The slots are the other half of the same complaint. A Slider draws seven parts and offered a class name for exactly one of them, so a one-off recolour had to reach for <code>[role=&quot;slider&quot;]</code>. <code>label</code>, <code>control</code>, <code>track</code>, <code>indicator</code>, <code>thumb</code>, <code>description</code> and <code>mark</code> are named now, like every other multi-part component in the library.</p></li><li><p><strong>A <code>SelectOption</code> can name a <code>group</code>, and a run of them gets a heading.</strong> A list of time zones by region or fonts by family had nowhere to put the heading, and the stand-in was a disabled option holding a <code>&lt;strong&gt;</code> — which the keyboard and the pointer skip correctly and a screen reader still reads as an option that cannot be chosen. A group is a run of <em>adjacent</em> options naming it, so the array&#39;s order stays the list&#39;s order and nothing is moved out from under a caller; two runs of one name draw two headings, which is the array saying they are not together.</p></li><li><p><strong>A <code>Tabs</code> bar takes the wheel.</strong> A bar with more tabs than room scrolls, but a mouse has one wheel and it points down the page — and the bar draws no scroll buttons and hides its scrollbar, so the tabs past the edge were reachable only by keyboard. A wheel rolled over the bar now travels along it. It is on by default, which is what separates the prop from <code>ScrollZone</code>&#39;s <code>wheel</code>: that strip has a pair of buttons and this one has nothing. A bar that fits takes nothing at all, so a page with three tabs on it is unaffected, and <code>wheel={false}</code> turns it off.</p></li><li><p><strong>A <code>Carousel</code> with <code>autoPlay</code> draws the button that stops it.</strong> It paused on hover, on focus and in a background tab, and it never started for a reader who had asked for less motion — but a reader holding a phone hovers nothing, and one running a magnifier may never put a pointer over the strip at all. WCAG 2.2.2 asks for a mechanism, and those were not one. The control sits in the row under the frame, beside the dots and never over a slide; <code>pauseLabel</code> and <code>playLabel</code> name it, and it is translated in all nineteen languages. There is no prop to take it away — a caller who wants a control of their own can drive <code>value</code> and leave <code>autoPlay</code> off.</p></li><li><p><strong>The four date and time pickers say their twenty non-date strings in the reader&#39;s language.</strong> &quot;Previous month&quot;, &quot;Choose a year&quot;, &quot;Today&quot;, &quot;Now&quot;, &quot;Hour&quot;, &quot;AM/PM&quot; and fourteen more were hardcoded English, over dates <code>Intl</code> had already translated — so a Korean product&#39;s only way out was to write out all twenty through <code>labels</code>. There is a <code>picker</code> message namespace now, in all nineteen languages. <code>labels</code> still wins where it is given, which is what it is for: <code>locale</code> answers the language and <code>labels</code> answers the wording.</p></li><li><p><strong>An <code>Image</code> takes <code>width</code> and <code>height</code>, and reserves the box they describe.</strong> They were omitted from the props, so the one component in the library whose reason to exist is holding a picture&#39;s space could only be told what that space was as a <code>ratio</code> worked out by hand — and a default <code>Image</code> reserved nothing at all, which is the largest source of layout shift on most sites. They reach the <code>&lt;img&gt;</code> as the attributes they are, and giving both turns an <code>&#39;auto&#39;</code> ratio into their proportion. An explicit <code>ratio</code> still outranks them: that one is the layout&#39;s shape and these two are the picture&#39;s.</p></li></ul><h3 id="changed-1" tabindex="-1">Changed <a class="header-anchor" href="#changed-1" aria-label="Permalink to &quot;Changed&quot;">​</a></h3><ul><li><p><strong>A focus ring arrives rather than appears, and on a field it is flush with the edge.</strong> Two things about the same object. The ring is declared at rest with no width instead of not being declared at all, so the focus has a width to travel rather than a style to switch — it grows in beside the hairline it belongs to, at the house duration, and instantly for a reader who has asked for less motion. And a field&#39;s shell takes it flush: the hairline turns the ring&#39;s own colour the moment the focus lands, so a ring held two pixels off drew a second line with a stripe of page between the two, which is a control wearing a halo rather than an edge that has thickened. Everywhere else the ring keeps its offset, and that is contrast rather than taste — flush against a filled control it would sit on a fill of its own family.</p><p>A pointer resting on a focused field used to take the hairline a shade <em>under</em> the ring it is now flush against, because <code>hover:</code> and <code>focus-within:</code> are one class each and the winner was whichever Tailwind generated last. The pair is spelled out, so the edge is one colour.</p><p>The six field shells also stop holding every duration at 0ms while the focus is inside. That was the rule about a press applied one step too far: clicking into a field is a press, but the ring is the answer to the caret being somewhere, and it arrives from a Tab as often as from a click.</p></li><li><p><strong>An <code>AccordionItem</code>&#39;s title wraps, and the header is a heading at a level the caller chooses.</strong> Two things about the same element. The title was wrapped in <code>truncate</code>, which is right for a label and wrong for the thing an accordion is most often a list of: an FAQ&#39;s title is a sentence, and an ellipsis in the middle of it loses the question. It wraps now, and <code>lines</code> clamps it where a caller wants one — the same word, and now the same table, <code>Typography</code> uses. And the header renders an <code>&lt;h3&gt;</code> whatever sits above it, so the natural way to ask for a different one was to pass a styled heading as the <code>title</code>, which nests a heading inside a heading; the documentation said to do exactly that. <code>headingLevel</code> on the <code>Accordion</code> sets it for the whole stack, which is where it belongs — the sections are siblings, and a run of headings at different levels is an outline that lies.</p></li><li><p><strong>A field&#39;s height is a floor rather than a fixed row.</strong> A TextField, a Select, a Combobox, a NumberField and the four date and time pickers all took the size ladder as an exact height, and a field holds a caller&#39;s own text — so text set larger than the step, which is the whole point of the tool that does it, had its glyphs cut off by the row around it. <code>classNames.shell</code> could repair it only with an <code>h-auto!</code>, and needing <code>!important</code> to beat your own class is the signal that the height was the wrong shape rather than the wrong number. Nothing moves at the library&#39;s own sizes. It stays an exact height on a Button, a Chip and everything else the library draws the contents of: those are flex children of a row a caller arranges, and a minimum height there is a control that stretches to whatever is beside it.</p></li><li><p><strong>A <code>Typography</code> level&#39;s leading is a ratio rather than a length, and the root carries <code>neba-typography</code>.</strong> The two halves of one problem: the scale paired each size with the line height it worked out to, and the component emitted nothing but utilities. So a caller who set a size of their own — which is what a figure, a display number or a hero line is — got a line box built for a size nobody asked for, with no selector to repair it from a stylesheet either, since a utility string is not a contract. Every level keeps exactly the ratio it was drawn at, so nothing moves at the scale&#39;s own sizes and an overridden one gets a line box in proportion. The class is a hook and carries no styling of its own, the way <code>neba-link</code> and <code>neba-portal</code> do.</p></li><li><p><strong>A <code>ScrollZone</code> and a <code>Tabs</code> bar hold the wheel at their ends rather than handing it back.</strong> Taking the wheel and then giving it up the moment the strip runs out is what makes the page lurch mid-flick: the reader is still pushing the strip and what answers is the article behind it. Both hold it now, and the pointer leaving the strip is what gives the page its wheel back. <code>overscroll-behavior: contain</code> does the same for the gestures the browser scrolls itself — a finger, a sideways trackpad swipe, and the wheel over a vertical zone — which used to carry on into the page at either end.</p><p>This changes what <code>ScrollZone</code>&#39;s <code>wheel</code> promises. The old wording said a strip with nothing left ahead of it was something to scroll past rather than something to be caught in; a caller who wanted that behaviour wants <code>wheel</code> off.</p></li><li><p><strong>An <code>Anchor</code> finds its headings once instead of on every frame of a scroll.</strong> It ran a <code>document.getElementById</code> for each row of the trail on every scroll frame, for an answer that changes only when the document does. The elements are kept and checked against <code>isConnected</code>, so a heading that arrives after the trail is still found.</p></li><li><p><strong>An <code>AnimateMarquee</code> measures itself when its size changes, not on every render above it.</strong> The measurement listed <code>children</code> as a dependency, so every render of whatever held the strip tore both resize observers down, put them back, and ran a <code>getComputedStyle</code> and an <code>offsetWidth</code> — a forced layout — for a strip that had not moved. The track is observed, so a change in what is on it is already reported.</p></li><li><p><strong>The last two <code>Intl</code> objects a render was rebuilding are memoised.</strong> <code>localeWeekStart</code> built an <code>Intl.DateTimeFormat</code> and an <code>Intl.Locale</code> on every render of a <code>Calendar</code>, a <code>DatePicker</code>, a <code>DateRangePicker</code> and a <code>DateTimePicker</code> — every keystroke and every hover anywhere inside one — and <code>graphemesOf</code> / <code>wordsOf</code> built an <code>Intl.Segmenter</code> on every call, for three text effects that ask on every frame they animate. Both go through a cache now, as the number and date formatters already did.</p></li><li><p><strong>A <code>Transfer</code> and a <code>TreeSelect</code> fold their labels once instead of once per keystroke.</strong> Both folded every row inside the filter, which put a <code>String.prototype.normalize</code> on every item for every character typed — a <code>Transfer</code> did it on every render, search or no search. The haystacks are built once per <code>items</code> now, which is the arrangement a <code>DataTable</code> and a <code>CommandPalette</code> already used. Nothing about what matches has changed.</p></li><li><p><strong>A chart&#39;s data table is memoised, and reads the chart&#39;s own formatter.</strong> The visually hidden table is built in the same render the crosshair&#39;s state lives in, so every cell of it was reconciled again for each pixel the pointer travelled across the picture — a row per point on a <code>ScatterChart</code>. It is <code>React.memo</code> now, and the frame&#39;s <code>format</code> is keyed on what the options say rather than on the identity of the object they arrived in, so a <code>format</code> written inline in the JSX no longer defeats it.</p><p>One number moves with it: a <code>ScatterChart</code> with no <code>format</code> writes its table values the way its axis and its tooltip already write them, so <code>24000</code> reads as <code>24K</code> rather than <code>24,000</code>. It was the only chart whose table disagreed with its own picture.</p></li></ul><h3 id="fixed-1" tabindex="-1">Fixed <a class="header-anchor" href="#fixed-1" aria-label="Permalink to &quot;Fixed&quot;">​</a></h3><ul><li><p><strong>A responsive slot no longer inherits into a nested component.</strong> Every value that changes at a breakpoint resolves through a custom property, one per breakpoint, and only the breakpoints a caller names are written — so a slot an element did not write was whatever the nearest ancestor had written. A <code>GridContainer</code> inside a <code>Grid</code> read the <em>item&#39;s</em> <code>--n-span-md</code> and came out a fraction of the width it had asked for, and the same shape covered every gutter, column count, flex direction and content measure in the library. The element that reads a slot now clears it, which an inline style still outranks.</p></li><li><p><strong>A <code>NavigationMenu</code>&#39;s panel keeps its surface.</strong> The colour slots were declared on the Root, and the panel is portalled to the end of the document — so it was not a descendant of the element carrying them, <code>bg-(--n-panel-press)</code> painted nothing, and the panel opened as clear glass with the page readable straight through the links. Both ends of the component take the slots now, which is what every other portalling component in the library already did.</p></li><li><p><strong>Dragging a run of rows in a <code>DataTable</code> no longer selects the text it crosses.</strong> It was the last drag in the library written by hand, and it had the hole the column resize used to have: no text-selection suppression, over every cell the pointer passed rather than the two beside a boundary. It goes through <code>beginPointerDrag</code> now — captured to the table rather than to the row that was pressed, since a virtual body unmounts a row the moment it scrolls away. The auto-scroll at the edge stays where it is.</p></li><li><p><strong>A <code>TreeSelect</code>&#39;s clear button says &quot;Clear&quot; in every language no longer.</strong> It was named out of the pickers&#39; English defaults — a set it has nothing else to do with — rather than out of the same <code>action</code> messages the identical × on a <code>Combobox</code> reads. It takes a <code>clearLabel</code> like <code>Combobox</code> does, too.</p></li><li><p><strong>A pointer drag starts even when the pointer cannot be captured.</strong> <code>setPointerCapture</code> throws for a pointer that is no longer active — one lifted between the <code>pointerdown</code> and the handler — and the exception escaped into React&#39;s event handler, which takes the page down. Capture is an optimisation and the three listeners work without it, so it is taken where it can be and skipped where it cannot.</p></li><li><p><strong>An <code>AnimateScramble</code> redraws on its own tick and not on every render above it.</strong> The noise glyphs were picked with <code>Math.random()</code> during the render, so any re-render from anywhere in the tree reshuffled every unsettled letter at whatever moment it happened to land — an effect meant to be a clock, answering to the whole page. They are picked from the tick counter and the position now, which also settles the hydration mismatch the random source caused on a server-rendered page.</p></li><li><p><strong>A <code>CodeBlock</code>&#39;s copy button keeps the focus when the clipboard falls back.</strong> On a page without a secure context the copy goes through <code>execCommand</code> against an off-screen textarea, and selecting that textarea took the focus — so a reader who pressed the button with a keyboard was left on <code>&lt;body&gt;</code>, having lost their place in the page as the reward for copying.</p></li><li><p><strong>An <code>Anchor</code>, an <code>Image</code> and a <code>Panes</code> write their refs in an effect rather than during a render.</strong> A ref written while rendering is a ref that lies when React throws that render away, which is a live hazard under concurrent rendering and is the rule <code>useShortcut</code> had already written down. None of the three reads the value during a render, so nothing about their behaviour changes.</p></li><li><p><strong>An <code>Image</code> says its own absence in the reader&#39;s language.</strong> A picture that failed with an empty <code>alt</code> printed a hardcoded <code>Image unavailable</code>, which was the one string the library invented and could not translate. There is an <code>image</code> message namespace now, in all eighteen languages, and <code>Image</code> takes a <code>locale</code> and an <code>unavailableLabel</code> like every other component that has to invent a word.</p></li><li><p><strong>A message lookup reads a table&#39;s own keys and not its prototype&#39;s.</strong> A <code>locale</code> a caller took from a URL, or a <code>{placeholder}</code> a translation happened to name after a member of <code>Object</code>, resolved up the prototype chain — so <code>locale=&quot;constructor&quot;</code> was spread over English as though it were a table of messages, and <code>{constructor}</code> in a translation wrote <code>function Object() { [native code] }</code> into the middle of a sentence.</p></li><li><p><strong>An <code>AppLogo</code> and a <code>BottomNavigationItem</code> merge <code>noopener noreferrer</code> into a link that leaves this tab.</strong> <code>safeRel</code> reached four of the six components that let a <code>target</code> through to an <code>&lt;a&gt;</code>; these two handed the page they opened a <code>window.opener</code> pointing back at the one that opened it, and a <code>Referer</code> header naming it. A <code>rel</code> written by hand — <code>nofollow</code>, <code>sponsored</code> — is merged rather than replaced, as everywhere else.</p></li><li><p><strong><code>colorSchemeScript</code> escapes <code>&lt;</code> in the values it writes.</strong> The string is inlined inside a <code>&lt;script&gt;</code> element, and a browser stops parsing that element at the first <code>&lt;/script</code> in it however the JavaScript around it is quoted — so a <code>storageKey</code> holding one would have ended the tag and handed the rest to the HTML parser as markup. <code>Breadcrumb</code> already wrote its structured data out this way.</p></li><li><p><strong>The five memos in <code>internal/</code> cannot grow without bound.</strong> The <code>Intl</code> formatters and segmenters, the <code>MediaQueryList</code>s, the week-start table and the resolved message tables are keyed on a <code>locale</code>, on the options behind a <code>format</code> prop, or on a query string handed to the public <code>useMediaQuery</code> — a caller&#39;s to choose in every case, and two of them carried a comment claiming otherwise. They share one ceiling now, well above anything the library itself reaches; a page that never approaches it is unchanged, and one that passes it rebuilds what it would have built anyway.</p></li><li><p><strong>A shared <code>IntersectionObserver</code> is let go once nothing is watching through it.</strong> One is kept per <code>threshold</code>, and <code>threshold</code> is a public prop on all seventeen <code>Animate*</code> components — so a caller computing one left a live observer behind for every value it had ever held, each of them a registration the browser still walks on every scroll. A group now lives exactly as long as its last watcher.</p></li><li><p><strong>An <code>AnimateMarquee</code> stops for the focus as well as for the pointer.</strong> <code>pauseOnHover</code> was <code>:hover</code> alone, so a link on a moving strip could be aimed at with a mouse and not reached with a keyboard — tabbing to it left it travelling off the screen while it was being read.</p></li></ul><h2 id="_1-12-0-2026-09-05" tabindex="-1">1.12.0 (2026-09-05) <a class="header-anchor" href="#_1-12-0-2026-09-05" aria-label="Permalink to &quot;1.12.0 (2026-09-05)&quot;">​</a></h2><p>The release about the things that were already there and were not quite saying it.</p><p>Most of it came out of asking one question of every component in turn: when a state turns over, does anything <em>happen</em>, or is the second state simply drawn? Twenty-odd answers were &quot;simply drawn&quot; — a toggle whose on and off were the same colour, a checkbox whose tick was there on one frame and gone on the next, a gauge that jumped to each new reading, a spoiler that moved the page when it opened. Three were fades that had been written down and never ran.</p><p>Four components are new. <code>Stack</code> is a pile of things laid over each other and replaces <code>AvatarGroup</code>; <code>Gallery</code> is a set of pictures in one of four arrangements; <code>Show</code> and <code>Flex</code> are the two answers a breakpoint gives directly. There are seventeen <code>Animate*</code> where there were eleven, and a <code>stagger</code> that turns any of the nine keyframe effects into a set of them.</p><p>The last group is the same complaint one level up. The library had a breakpoint system — five widths, a per-breakpoint map, a cascade that resolves it without React hearing about it — and it reached exactly two props on two components. There was no way to draw something at one width and not another, no way to say &quot;a row here, a column there&quot;, no way to move the widths, and no page that wrote any of it down.</p><h3 id="where-the-bytes-went-2" tabindex="-1">Where the bytes went <a class="header-anchor" href="#where-the-bytes-went-2" aria-label="Permalink to &quot;Where the bytes went&quot;">​</a></h3><table tabindex="0"><thead><tr><th>What you import</th><th>1.11.0</th><th>1.12.0</th></tr></thead><tbody><tr><td><code>Button</code></td><td>5.1 kB</td><td>5.1 kB</td></tr><tr><td><code>Chip</code></td><td>3.2 kB</td><td>3.3 kB</td></tr><tr><td><code>LineChart</code></td><td>11.4 kB</td><td>11.6 kB</td></tr><tr><td><code>CodeBlock</code></td><td>5.0 kB</td><td>5.0 kB</td></tr><tr><td><code>Image</code></td><td>23.4 kB</td><td>6.5 kB</td></tr><tr><td><code>Gallery</code></td><td>—</td><td>10.2 kB</td></tr><tr><td>a whole page shell</td><td>28.5 kB</td><td>29.0 kB</td></tr><tr><td>12 components — a typical app</td><td>68.2 kB</td><td>70.5 kB</td></tr><tr><td>12 components, with Korean</td><td>70.7 kB</td><td>72.9 kB</td></tr><tr><td>25 components — a large one</td><td>112.6 kB</td><td>115.7 kB</td></tr><tr><td>all exports</td><td>248.1 kB</td><td>263.9 kB</td></tr></tbody></table><p><code>Image</code> is the row that moved on purpose, and it is a new row because nothing had ever measured it: a <code>Dialog</code> nobody had opened was 20 kB of it. <code>Chip</code> is the one worth explaining, because it is the only one that moved for a reason other than &quot;there is more library now&quot; — <code>transition</code> gained a seventh effect, and the entrance vocabulary is two <code>Record</code>s that an object literal cannot tree-shake per key, so every component offering a <code>transition</code> pays 0.1 kB for the row whether or not it names it. <code>AnimateFloat</code> and <code>AnimateShake</code> bring their own keyframe class instead of a row in that table, which is why it is only 0.1 kB.</p><p><code>all exports</code> went up rather than down because <code>npm run size</code> learned to count honestly while this was being measured. It called every chunk but the entry deferred, which stopped being true the moment a module was imported both statically and dynamically. It walks the static import graph now, so a number in the table is what a page needs before it draws.</p><p>The rest of the movement is one dependency. <code>@base-ui/react</code> is on 1.8.0, and pinning it back to 1.7.0 returns every row to the figure this table carried before it — so the 1.4 kB on a twelve-component app and the 2.5 kB on <code>all exports</code> are its rather than ours. Nothing that draws a single component moved by more than 0.1 kB.</p><h3 id="breaking-changes-1" tabindex="-1">Breaking changes <a class="header-anchor" href="#breaking-changes-1" aria-label="Permalink to &quot;Breaking changes&quot;">​</a></h3><ul><li><strong><code>AvatarGroup</code> is removed.</strong> <code>Stack</code> does everything it did and is not about avatars. What is lost is its context — <code>size</code>, <code>shape</code>, <code>variant</code>, <code>color</code> and <code>elevation</code> set once for the group. Set them on the avatars, or on a <code>NebaProvider</code> for the two it covers.</li></ul><h3 id="added-2" tabindex="-1">Added <a class="header-anchor" href="#added-2" aria-label="Permalink to &quot;Added&quot;">​</a></h3><ul><li><p><strong><code>Stack</code></strong> — a pile of things laid over each other. <code>direction</code> runs it along the inline axis, down the page, or diagonally as a fanned deck; <code>overlap</code> and <code>drop</code> say how far each item sits under the last; <code>max</code>, <code>total</code> and <code>overflow</code> turn the ones that did not fit into one more item at the back; <code>scaleStep</code> and <code>opacityStep</code> make the pile recede; <code>ring</code> draws the hairline that stops two overlapping shapes of similar tone reading as one smeared one.</p><p>The overlap is a <strong>margin</strong> rather than a <code>translate</code>, so the box is exactly as big as what is in it and the content after a Stack is laid out against the right width. Each item is drawn into a wrapper of its own rather than cloned onto, because a Tooltip around an avatar is under no obligation to accept a <code>className</code>.</p><div class="language-tsx vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">tsx</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Stack</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> ring</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> max</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">3</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">} </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">total</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">12</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">} </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">overflow</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{(</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">hidden</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &lt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Avatar</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> initials</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">\`+\${</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">hidden</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">}\`</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">} /&gt;}&gt;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  {team.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">map</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">((</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">name</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    &lt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Avatar</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> key</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{name} </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">name</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{name} /&gt;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  ))}</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;/</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Stack</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;</span></span></code></pre></div></li><li><p><strong><code>Gallery</code></strong> — a set of pictures, and the arrangement is a prop rather than four components. <code>grid</code> is a contact sheet; <code>masonry</code> keeps each picture&#39;s own proportion and stacks the columns; <code>justified</code> keeps the proportions <strong>and</strong> fills every row to the edge, which is the only arrangement where nothing is cropped and nothing is left over; <code>quilted</code> is a grid whose tiles may take more than one cell.</p><p><strong>None of them measures anything.</strong> A tile&#39;s shape is the item&#39;s own <code>ratio</code>, so the wall is right in the first frame the browser paints and does not move again as the files land, and <code>justified</code> is a wrapping flex row the browser does the arithmetic for. <code>masonry</code> is the one layout that needs a number in JavaScript, and it deals each item into the <em>shortest</em> column rather than filling the first one top to bottom.</p><p><code>columns</code> takes a breakpoint map and <code>gap</code> a step, a number or a length. <code>caption</code> puts an item&#39;s title and description below the picture, across the foot of it, or under the pointer; <code>hover</code> answers with depth, colour or the one scale the design language allows; <code>preview</code> opens the picture full size with the rest of the set on the arrow keys, in a chunk that is not fetched at all unless the prop is on. <code>filter</code>, <code>frame</code>, <code>watermark</code> and <code>protect</code> pass straight through to every tile&#39;s <code>Image</code>, and the last two follow the picture into the viewer. The <code>gallery</code> message namespace is new, in all eighteen languages.</p></li><li><p><strong><code>Show</code></strong> — its children at some widths and not at others. <code>above</code> is an inclusive floor and <code>below</code> an exclusive ceiling, so the same breakpoint in both covers every width exactly once, and the two together bound a range.</p><div class="language-tsx vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">tsx</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Show</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> above</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;md&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  &lt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Sidebar</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> /&gt;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;/</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Show</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Show</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> below</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;md&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  &lt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">SidebarTrigger</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> /&gt;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;/</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Show</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;</span></span></code></pre></div><p>The children are <strong>always rendered</strong> and what changes is <code>display</code>, which is what makes the answer right in the first frame, the same on a server, and free on a resize. What it deliberately cannot do is stop something running; <code>useBreakpoint</code> already could. The wrapper is <code>display: contents</code>, so a <code>Show</code> between a <code>GridContainer</code> and a <code>Grid</code> leaves the cell a cell.</p></li><li><p><strong><code>Flex</code></strong> — a row that becomes a column. &quot;Side by side on a desktop, stacked on a phone&quot; is half of what a responsive layout is, and the only way to say it was a <code>GridContainer</code> with a <code>Grid</code> around each child. <code>direction</code> takes <code>horizontal</code>/<code>vertical</code> rather than CSS&#39;s four values, so a Flex and a Stack say the same thing the same way, and it takes a breakpoint map — <code>direction={{ xs: &#39;vertical&#39;, md: &#39;horizontal&#39; }}</code>. <code>spacing</code>, <code>rowSpacing</code> and <code>columnSpacing</code> are a <code>GridContainer</code>&#39;s props on its scale, so a gutter is one number across the two. It draws nothing at all, and <code>wrap</code> is <strong>off</strong> by default.</p></li><li><p><strong>Six more <code>Animate*</code></strong> — <code>AnimateReveal</code>, an edge travelling across content that is already in place, as a <code>clip-path</code>, so nothing reflows and it is also a <code>transition</code> value; <code>AnimateFloat</code>, a slow drift with nowhere to get to; <code>AnimateShake</code>, the one effect that says <em>no</em>, which defaults to <code>trigger=&quot;manual&quot;</code> and is the single documented exception to the rule that a control is never transformed; <code>AnimateSplit</code>, a line arriving a word or a letter at a time; <code>AnimateCounter</code>, a number counted up to its value and formatted with <code>Intl.NumberFormat</code> options on every frame; <code>AnimateScramble</code>, text resolving out of noise in a box that is the finished length from the first frame.</p><p>The three that animate text put the whole string in the document once for a screen reader and hide the performance from it, which is <code>AnimateTyping</code>&#39;s arrangement, and all four now take their grapheme boundaries from <code>internal/text.ts</code>.</p></li><li><p><strong><code>stagger</code>, <code>durationStep</code> and <code>reverse</code></strong> on the nine <code>Animate*</code> whose motion is one <code>@keyframes</code> on the element itself. At <code>0</code> — the default — the box animates and the children are left alone, which is what they always did. Above it the effect moves onto each child in turn and nothing is written on the box, because a box fading in over eight children fading in is the same content faded twice. <code>AnimateAppear</code> runs on the same helper now, which is what stops the library having two staggers.</p></li><li><p><strong><code>timeline=&quot;view&quot;</code></strong> — any of the nine keyframe effects driven by how far the element has travelled through the viewport instead of by the clock. It is two declarations behind an <code>@supports</code>. It costs <code>duration</code>, <code>delay</code>, <code>repeat</code> and every <code>trigger</code>, and falls back to running once on mount where the browser has no <code>animation-timeline</code>.</p></li><li><p><strong><code>useBreakpointValue</code> and <code>useCurrentBreakpoint</code></strong> — a breakpoint map read in JavaScript exactly as the cascade reads it, for a caller working out a number for themselves. <code>undefined</code> is a real answer: a map that has said nothing yet at this width is an opinion declined, which is what the CSS fallback says there too.</p></li><li><p><strong>Four things an <code>Image</code> could not do</strong>, none of which costs anything until it is asked for. <code>filter</code> colours the picture — <code>grayscale</code>, <code>sepia</code>, <code>invert</code>, <code>saturate</code>, <code>mute</code>, <code>contrast</code>, or a CSS chain of your own — and rides the same transition as the picture&#39;s own fade. <code>frame</code> is how the picture is mounted, a silhouette on its own or the whole arrangement of <code>corner</code>, <code>border</code>, <code>mat</code>, <code>background</code>, <code>elevation</code> and <code>feather</code>; the line is an inset shadow on a layer over the picture rather than a <code>border</code>, which is what lets it follow a chamfered corner and keeps it out of the layout. <code>watermark</code> draws a mark in a corner or tiled across the whole picture, as one SVG background rather than a wall of elements. <code>protect</code> turns off the right-click menu, the drag, the iOS long press and the selection — a deterrent and not a lock, and the docs say so where the prop is described.</p></li><li><p><strong><code>overflow</code> and <code>lines</code> on <code>Tabs</code></strong> — <code>overflow=&quot;wrap&quot;</code> is for a bar whose tabs all have to be visible at once, and the rule under the chosen tab moves onto the line that tab is on rather than staying at the bottom of the list. <code>lines</code> caps a wrapping bar at that many tab-rows.</p></li><li><p><strong><code>maxWidth</code> takes a length and a breakpoint map</strong> on <code>Container</code>, <code>Header</code> and <code>Footer</code>, which had a copy each of the measure ladder and could only ever be one of its five values at one width. Anything that is not a step of the ladder goes straight to <code>max-width</code>, so <code>&#39;60ch&#39;</code> and <code>&#39;min(90vw, 72rem)&#39;</code> need no escape hatch and a number is pixels. The ladder is named <code>NebaMeasure</code> rather than left as <code>NebaSize</code>, because it shares five names with <code>NebaBreakpoint</code> and only four of its five values.</p></li><li><p><strong><code>Statistic</code>&#39;s <code>value</code> takes a node</strong>, which is where an <code>AnimateCounter</code> belongs.</p></li></ul><h3 id="changed-2" tabindex="-1">Changed <a class="header-anchor" href="#changed-2" aria-label="Permalink to &quot;Changed&quot;">​</a></h3><ul><li><p><strong>The library&#39;s media queries are <code>theme(--breakpoint-*)</code></strong>, so a Tailwind project moves Neba&#39;s widths by moving its own. <code>@media</code> cannot read a custom property, so a breakpoint stays a build-time decision and no provider prop could ever move one — but it can be a decision you take part in.</p><div class="language-css vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">css</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">@import</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;tailwindcss&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">@import</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;neba/tailwind.css&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">@theme</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  --breakpoint-md: 50rem;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>That moves the library&#39;s own rules, the <code>md:</code> variants its components spell out, and its JavaScript, which reads the resolved widths back off the document rather than holding a second copy. Before this, redeclaring <code>--breakpoint-md</code> moved your utilities and left Neba at 48rem in both places. A project on <code>neba/styles.css</code> gets the widths baked in, since that sheet is compiled here.</p></li><li><p><strong>A <code>Toggle</code> that is on now looks like it.</strong> Off was <code>accent 8%</code> and on was <code>accent 10%</code> — two names for one colour, with the ink carrying the whole state. Off leaves the colour family entirely now for the neutral <code>--neba-panel</code> ladder, and on takes the dyed <code>--n-panel-press</code> that draws a <code>SegmentedButton</code>&#39;s chosen segment. A control with two states cannot spend the family on the one that is false.</p></li><li><p><strong><code>Home</code>, <code>End</code>, <code>PageUp</code> and <code>PageDown</code> scroll a <code>DataTable</code> and nothing else.</strong> They used to move the selection with them, so ticking a row and then looking at the bottom of a thousand threw the tick away on the journey. The arrows still move and choose. A table with no <code>height</code> or <code>maxHeight</code> leaves all four to the browser rather than taking them and turning them into nothing.</p></li><li><p><strong>A <code>Spoiler</code> is the same height covered and uncovered.</strong> <code>reversible</code> grew the box by a whole button row at the moment of the press, and the cover — an <code>absolute inset-0</code> layer contributing no height — lost its own reveal button off the bottom edge whenever it was taller than what it covered. The row is drawn from the start and kept out of sight, and the cover is a grid item spanning every row that keeps its row on the press, giving up only the paint and the tab stop. <code>maxHeight</code> stays the deliberate exception, since releasing the clamp is the whole point of it.</p></li><li><p><strong>A tab bar says when it has more bar.</strong> It already scrolled; the scrollbar is an overlay on macOS and furniture on Windows, so it is hidden on both and the ends fade instead, through the same two masks <code>ScrollArea</code> already uses.</p></li><li><p><strong>A drawer comes in from its edge.</strong> It faded, which moves nothing and throws away the only thing that distinguishes it from a <code>Dialog</code> — and a <code>Sidebar</code> below its breakpoint <em>is</em> a drawer, so the same thing happened to every collapsed page shell. The panel travels on <code>translate</code> now and the scrim behind it still fades. It runs on <code>--neba-duration</code> rather than the window ladder&#39;s 240ms, because <code>--neba-duration-window</code> is not zeroed under <code>prefers-reduced-motion</code> and a drawer sliding across the screen is exactly what that setting is asking not to see.</p></li><li><p><strong>A <code>GaugeChart</code> sweeps to its reading.</strong> It drew the reading as a wedge, and a wedge is a closed shape, so moving the value rewrote its <code>d</code> — not a property CSS can travel along. It is a stroke along the middle of the groove now, whose drawn length is <code>stroke-dashoffset</code> with <code>pathLength=&quot;1&quot;</code>. The shape on screen is identical; only <code>fill</code> moved to <code>stroke</code>.</p></li><li><p><strong>An <code>Image</code> weighs 6.5 kB instead of 23.4.</strong> A <code>Dialog</code> was most of what it cost, and <code>preview</code>, the only thing that opens one, is off by default. It is fetched on demand now, the way <code>CodeBlock</code> fetches a grammar.</p></li><li><p><strong><code>npm test</code> runs the suite in sessions of at most fifty files</strong>, each its own browser, through <code>scripts/run-tests.mjs</code> — <code>ceil(files / 50)</code>, so adding tests does not need the number touched. Nothing is skipped and nothing is retried: a failing test still fails its shard and still fails the run.</p></li><li><p><strong><code>npm run build</code> empties <code>dist/</code> first.</strong> It never did, and <code>tsc</code> only ever writes, so a component deleted from <code>src/</code> stayed in <code>dist/</code> and shipped. Removing <code>AvatarGroup</code> is what surfaced it.</p></li><li><p><strong><code>ScrollZone</code>&#39;s <code>buttons=&quot;auto&quot;</code> disables an inline button that has nowhere to go</strong> rather than hiding it. The lane is held open either way, so an emptied one was not a lighter row — it was the same row reading as stray padding at the leading edge, which is the state every reader meets first.</p></li><li><p><strong><code>@base-ui/react</code> is on 1.8.0</strong>, which is the floor <code>dependencies</code> declares. It is the only runtime dependency that moved, and the multi-component rows of the table above are where it shows.</p></li></ul><h3 id="fixed-2" tabindex="-1">Fixed <a class="header-anchor" href="#fixed-2" aria-label="Permalink to &quot;Fixed&quot;">​</a></h3><ul><li><p><strong>A <code>Select</code>&#39;s and a <code>Combobox</code>&#39;s popup arrived in one frame</strong> while the calendar hanging off the <code>DatePicker</code> beside them took 160ms. The fade was fourteen identical copies of one declaration; it is <code>popupFadeClasses</code> in <code>internal/styles.ts</code> now, read by sixteen surfaces, and the two that were missing it got it by being written the same way as the rest. <code>NavigationMenu</code> keeps its own <code>transition</code>, because its panel also changes size and a second shorthand beside its own would win or lose by stylesheet order.</p></li><li><p><strong>Three fades were written down and never ran.</strong> <code>transitionClasses</code> lists the four properties a control answers a pointer with, and <code>opacity</code> is not one of them. An <code>Image</code> carried a comment saying the picture is faded in over a transition that could not fade it; a chart&#39;s legend dimming switched rather than faded; and a chart&#39;s other series dropped to 0.28 the frame the pointer crossed a legend row. <code>LineChart</code>, <code>AreaChart</code>, <code>BarChart</code> and <code>ScatterChart</code> now read the same <code>seriesDimClasses</code> that only <code>PieChart</code> had.</p></li><li><p><strong>A <code>Checkbox</code>&#39;s tick and a <code>Radio</code>&#39;s dot appear over time.</strong> The tick draws itself along its own length on <code>stroke-dashoffset</code>, over a path normalised with <code>pathLength=&quot;1&quot;</code> so one number covers both the tick and the indeterminate dash; the dot grows out of the centre of its ring on <code>width</code> and <code>height</code>. Neither scales, and the <a href="https://neba.cdget.com/design/design-language" target="_blank" rel="noreferrer">design language</a> now names the four things allowed to travel inside a control — a Switch&#39;s thumb, a Checkbox&#39;s tick, a Radio&#39;s dot, a Rating&#39;s fill — and says the list is closed.</p></li><li><p><strong>A chart&#39;s marks answer the pointer on one <code>transition</code> shorthand.</strong> A whole series at 0.28 and a single datum at 0.92 are the same sentence at two scales, and the datum half snapped everywhere the series half now fades; the mark under the crosshair also grows by a pixel, which a <code>&lt;circle&gt;</code> reaches through <code>r</code> and a scatter&#39;s arbitrary <code>&lt;path&gt;</code> reaches through <code>scale</code>. The scatter mark grows about the point it is pinned to rather than the middle of its bounding box. All three ride <code>markTransitionClasses</code>, because two shorthands on one element are decided by stylesheet order rather than by intent.</p></li><li><p><strong>Four more places where something changed and nothing moved.</strong> A <code>Rating</code>&#39;s fill travels on <code>width</code> — the same width on the same element, so no glyph is scaled. A <code>Tab</code> panel&#39;s arriving content fades up, and only the arriving one, since fading a leaving panel would put both in the layout and make the sheet twice as tall on the way past. An <code>Avatar</code>&#39;s picture fades up on its own clock instead of swapping in on one frame, which on a list of forty was forty separate flickers. A <code>FloatingActionButton</code>&#39;s dial no longer arrives in a single frame.</p></li><li><p><strong>A <code>TreeView</code> branch opens at a height.</strong> It was there on one frame and gone on the next, while <code>Accordion</code> and <code>Collapsible</code>, which do the same thing, both travel. It is a grid row going from <code>0fr</code> to <code>1fr</code>, so nothing is measured and a nested branch is carried by the same track. A branch on its way shut stays in the document but is marked <code>data-closing</code>, which keeps its rows out of the order the arrow keys walk and deregisters them on the same render that shut the branch.</p></li><li><p><strong><code>Transfer</code> says where the rows went.</strong> A press on the arrow took three rows off one list and put them in the other in a single frame, so the only way to find them was to read the whole panel again. The rows that landed fade up — keyed on the press and not on the list changing, because <code>rows</code> also changes on every keystroke in the search box and a filter that animates is a filter that feels slow.</p></li><li><p><strong>A <code>GaugeChart</code>&#39;s reading and range labels stay inside the dial.</strong> The reading was twice the tick type whatever it said, so <code>10,000%</code> was written straight across the band and out of the card; it is solved against a chord of the inner circle now and gives way to once the tick type before it is left to run. The two range labels were written from the arc&#39;s mid radius and lay over the band on a thick dial; they are set from the outer edge, in the arrangement the end calls for, and a dial closed past 330° writes none. The dial is also centred in the box rather than pinned under its top margin.</p></li><li><p><strong>A picture that had already decoded stayed invisible.</strong> An <code>&lt;img&gt;</code> fires <code>load</code> at whoever is listening at the time, and a data URI decodes inside the same task the element was inserted in — so the event went out before React had attached anything to catch it and the picture sat at <code>opacity: 0</code> behind its own placeholder for good. It is asked after the fact now: <code>complete</code> says whether it finished and <code>naturalWidth</code> says which way.</p></li><li><p><strong>A <code>Drawer</code> in <code>inline</code> mode and a collapsed <code>Sidebar</code> dropped the props they were handed.</strong> Only the <code>overlay</code> shape spread them, so an <code>id</code>, a <code>data-*</code> or an <code>aria-*</code> reached the panel in one mode and not the other — and on a <code>Sidebar</code> the mode is the window&#39;s choice rather than the caller&#39;s, so an attribute was there on the screen you developed against and gone on the screen you did not. A structural rule for this was tried and rejected: <code>resolution.test.ts</code> can ask whether one element both spreads and writes an attribute of its own, but &quot;every tree this component can return forwards what it was handed&quot; is a question about branches, and every regex shape of it produced false positives. It is three ordinary tests instead.</p></li><li><p><strong>A <code>HowToSteps</code> row said its title twice.</strong> The sentence naming the step sat in a visually hidden line beside the title rather than in place of it, so a screen reader announced the button as &quot;Use it Step 3: Use it&quot;. It is the button&#39;s <code>aria-label</code> now. A <code>title</code> that is a node leaves no string to build that sentence out of, and such a row is read as its contents.</p></li><li><p><strong><code>GridContainer</code>&#39;s axis gutters took their prop whole.</strong> <code>spacing={2} columnSpacing={{ md: 6 }}</code> left the row with no column gutter at all below 48rem, because the map says nothing there and the baseline it fell back to was the prop&#39;s own default rather than the <code>spacing</code> beside it. The two are walked together now, and <code>Flex</code> uses the same fold.</p></li><li><p><strong>The suite finishes.</strong> <code>run-test</code> had been red on <code>main</code> for weeks, always the same way: the chromium jobs failed on every runner while firefox and webkit passed on all three. It is not a test — every test that starts passes, and chromium loses the browser somewhere past the hundredth file of a hundred and forty-six, so the <em>file</em> count comes up short beside a full <em>test</em> count. Twelve full runs, none finished; the only lever that separated a run which finished from one which did not was how many files a single browser session was asked to hold. See the sharding above; eighteen sharded runs since, all green.</p></li><li><p><strong><code>npm run docs:build</code> needed more heap than Node gives by default.</strong> A hundred and thirty-nine pages with two hundred React demos behind them is past 4.3 GB, and it failed as a V8 <code>Abort trap: 6</code> inside Rollup that named no page at all.</p></li></ul><h3 id="documentation" tabindex="-1">Documentation <a class="header-anchor" href="#documentation" aria-label="Permalink to &quot;Documentation&quot;">​</a></h3><ul><li><p>A new <a href="https://neba.cdget.com/design/breakpoints" target="_blank" rel="noreferrer">breakpoints</a> page, which is where the rule that every entry is a floor, the table of which props are responsive, and the reason <code>size</code> and <code>variant</code> are not among them all live.</p></li><li><p>The <a href="https://neba.cdget.com/design/design-language" target="_blank" rel="noreferrer">design language</a> names its two exceptions rather than leaving them to be found: the closed list of four indicators allowed to travel inside a control, and the drawer that arrives on <code>translate</code> where every other floating surface only fades.</p></li></ul><h2 id="_1-11-0-2026-08-31" tabindex="-1">1.11.0 (2026-08-31) <a class="header-anchor" href="#_1-11-0-2026-08-31" aria-label="Permalink to &quot;1.11.0 (2026-08-31)&quot;">​</a></h2><p>The release that closes the gaps, rather than the one that adds a shelf of new components.</p><p>It started as an audit against the libraries people arrive here from — MUI, Ant Design, Chakra, Mantine, Radix, PrimeReact — and the answer was not the component list. Neba already ships charts, animations, a page shell and a data table that most of them charge for or split into a second package. What it was missing was smaller and felt harder: no way to say &quot;this application is compact&quot; once, no hooks, no imperative confirm, no standalone calendar, and a <code>TextField</code> you could hand an <code>onKeyDown</code> but could not actually make act on a key.</p><p>Nine of those are closed here. The one deliberately left open is a rich text editor, which is somebody else&#39;s package.</p><h3 id="where-the-bytes-went-3" tabindex="-1">Where the bytes went <a class="header-anchor" href="#where-the-bytes-went-3" aria-label="Permalink to &quot;Where the bytes went&quot;">​</a></h3><table tabindex="0"><thead><tr><th>What you import</th><th>1.10.0</th><th>1.11.0</th></tr></thead><tbody><tr><td><code>Divider</code></td><td>3.0 kB</td><td>3.2 kB</td></tr><tr><td><code>Button</code></td><td>5.0 kB</td><td>5.1 kB</td></tr><tr><td><code>Chip</code></td><td>3.0 kB</td><td>3.2 kB</td></tr><tr><td><code>LineChart</code></td><td>11.3 kB</td><td>11.4 kB</td></tr><tr><td><code>CodeBlock</code></td><td>4.9 kB</td><td>5.0 kB</td></tr><tr><td>a whole page shell</td><td>28.4 kB</td><td>28.5 kB</td></tr><tr><td>12 components — a typical app</td><td>67.3 kB</td><td>68.2 kB</td></tr><tr><td>12 components, with Korean</td><td>69.9 kB</td><td>70.7 kB</td></tr><tr><td>25 components — a large one</td><td>111.7 kB</td><td>112.6 kB</td></tr><tr><td>all exports</td><td>240.7 kB</td><td>248.1 kB</td></tr></tbody></table><p><strong>Every component grew by about 0.2 kB, and it is one thing.</strong> <code>NebaProvider</code> fills in the props a call site left out, which means every component that takes <code>size</code>, <code>density</code>, <code>variant</code> or <code>locale</code> now reads a context before its own destructuring — a hundred and thirty of them. <code>internal/defaults.ts</code> is 0.2 kB gzipped and it is a fixed cost even on a page that has no provider, which on a Chip is five per cent. It is stated here rather than left to be found, and <code>CLAUDE.md</code> now carries it as the sixth thing that holds the bundle numbers in place: whatever goes into that module goes into all of them.</p><p>The rest of the growth is the four new components and the hooks, and it lands where it should — on <code>all exports</code> and nowhere else.</p><p><code>neba/styles.css</code> moved 20.8 → 21.0 kB gzipped.</p><hr><p>A <code>DatePicker</code> has always drawn all three grids. The month name opened twelve months, the year opened twelve years, and both were only ever a way of reaching a day — so a product that wanted a billing period or a tax year got a control that showed it the twelve months and then insisted on a date inside one of them. <code>granularity</code> makes one of those grids the answer.</p><p>And a field has always accepted an <code>onKeyDown</code>, which is not the same as being able to act on a key. On a <code>Combobox</code> the keys worth acting on are the list&#39;s and never arrive; on a <code>NumberField</code> the handler lands on the column holding the label rather than on the <code>&lt;input&gt;</code>. <code>shortcuts</code> is <code>{ &#39;Mod+Enter&#39;: send }</code> bound to the control itself, written in the vocabulary <code>Shortcut</code> already draws — which turned out to be a vocabulary the library only half spoke.</p><p><code>internal/keys.ts</code> is 0.6 kB of the field rows above, and it is carried whether or not a <code>shortcuts</code> map was passed: the alias table and the predicate are reached from the control&#39;s own key handler, so there is nothing for a bundler to drop.</p><h3 id="added-3" tabindex="-1">Added <a class="header-anchor" href="#added-3" aria-label="Permalink to &quot;Added&quot;">​</a></h3><ul><li><p><strong><code>granularity</code> on <code>DatePicker</code></strong> — <code>&#39;day&#39;</code> (the default, unchanged), <code>&#39;month&#39;</code> or <code>&#39;year&#39;</code>. At the two coarser settings the calendar opens on that grid and a click there is the answer; there is no day view to fall into. Climbing is untouched, so a month picker still reaches any month of any year in two clicks.</p><p>The value stays a <code>Date</code>, normalised to the first day of what was chosen — 1 March, 1 January. A second value type would have meant a second set of props to compare it with, and <code>minDate</code>/<code>maxDate</code> already speak <code>Date</code>.</p><p>Four things follow the unit rather than being left to the caller to keep in step. The trigger&#39;s default <code>format</code> becomes <code>{ year: &#39;numeric&#39;, month: &#39;long&#39; }</code> or <code>{ year: &#39;numeric&#39; }</code> — <code>dateStyle: &#39;medium&#39;</code> on a month picker prints <code>Mar 1, 2026</code>, which names a day nobody chose in the one place a reader actually looks. The footer&#39;s shortcut says &quot;This month&quot; or &quot;This year&quot;. <code>name</code> submits <code>YYYY-MM</code> or <code>YYYY</code>, the shape a native <code>&lt;input type=&quot;month&quot;&gt;</code> already submits, rather than a day the server would have to know to ignore. And the bounds are read at the unit.</p></li><li><p><strong><code>thisMonth</code> and <code>thisYear</code> on <code>PickerLabels</code></strong> — the two strings that footer needs. Both have English defaults, like the eighteen already there.</p></li><li><p><strong><code>shortcuts</code> on <code>TextField</code>, <code>NumberField</code> and <code>Combobox</code></strong> — a map from a key combination to what it does, spelled the way <a href="https://neba.cdget.com/components/display/shortcut" target="_blank" rel="noreferrer"><code>Shortcut</code></a> draws it, so the key a form <em>shows</em> a reader and the key it <em>binds</em> are one string.</p><div class="language-tsx vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">tsx</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">TextField</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  label</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Message&quot;</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  multiline</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  shortcuts</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{{</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">    &#39;Mod+Enter&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: (</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">event</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      event.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">preventDefault</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">      send</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    Escape: clear</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  }}</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">/&gt;</span></span></code></pre></div><p>A map rather than an <code>onShortcut(combination, event)</code>, because a caller with three shortcuts wants three functions and not a <code>switch</code>. Modifiers are matched <strong>exactly</strong>, so <code>Enter</code> and <code>Mod+Enter</code> are two entries that never both fire and no entry is ambiguous.</p><p>It is bound to the control, which is the half <code>onKeyDown</code> could not do. On a <code>TextField</code> that only moves <code>currentTarget</code> onto the <code>&lt;input&gt;</code>; on a <code>NumberField</code> it is the difference between the field and the column the label sits in; on a <code>Combobox</code> it is the only way in at all, because the arrows, <code>Escape</code> and <code>Enter</code> belong to the list and are gone before anything on the root sees them. <code>onKeyDown</code> still receives every keystroke and runs after the map — neither prop replaces the other, and nothing is prevented on your behalf.</p><p>This is deliberately three components and not thirteen. It is for a control a reader <strong>types into</strong>, where a key is a thing the control already has an opinion about. Everything else already takes the handler it needs.</p></li><li><p><strong><code>NebaShortcuts</code> in <code>src/types.ts</code></strong> — the type behind that prop, generic in the element so <code>event.currentTarget.value</code> is typed without a cast.</p></li><li><p><strong><code>NebaProvider</code></strong> — one optional place to set what every component under it starts from, and the gap that costs the most on every project while showing up in no bundle number.</p><p><code>defaults</code> fills in <code>size</code>, <code>density</code>, <code>variant</code> and <code>locale</code> where a call site left them out, and the call site still wins: caller, then provider, then the component&#39;s own literal. The list is closed. <code>color</code> is out because a component&#39;s colour default is often semantic — an Alert is <code>info</code>, a Popconfirm is <code>danger</code> — and one global override would repaint those into something that means something else; <code>elevation</code> is out because a shadow is opt-in per surface, and an application-wide one is the moulded-plastic look the design language is against.</p><p>The colour scheme writes <code>data-theme</code> <strong>and</strong> <code>color-scheme</code> on <code>&lt;html&gt;</code>; the second is what turns the browser&#39;s own scrollbars and form controls over, and a page that changes only its own colours keeps a white scrollbar down the side of a dark one. <code>useColorScheme()</code> keeps <code>system</code> as its own answer and <code>resolvedColorScheme</code> never is, because a three-way switch has to show <code>system</code> as a position rather than as whichever of the two it resolves to. <code>colorSchemeScript()</code> is exported for the first-paint flash React cannot prevent, and shares the provider&#39;s key and attribute rather than being a snippet in a page nobody updates.</p><p><code>direction</code> sets <code>dir</code> and wraps Base UI&#39;s <code>DirectionProvider</code>, and is left alone when not given, so a document that already sets it server-side is not fought over.</p></li><li><p><strong>Seven hooks, from <code>neba/hooks</code> and the barrel</strong> — <code>useDisclosure</code>, <code>useMediaQuery</code>, <code>useBreakpoint</code>, <code>usePrefersReducedMotion</code>, <code>useElementSize</code>, <code>useOnScreen</code>, <code>useShortcut</code>.</p><p>Every one is machinery the library already runs on, which is the whole selection rule: there is no general-purpose hook collection here and there is not going to be one. <code>useDisclosure</code> is the caller&#39;s half of the <code>open</code>/<code>onOpenChange</code> pair every overlay takes; <code>useMediaQuery</code> is the store PageLayout subscribes to, one live <code>MediaQueryList</code> per query for the page; <code>useElementSize</code> and <code>useOnScreen</code> are the two shared observers; <code>useShortcut</code> is what CommandPalette binds its own opener with.</p></li><li><p><strong><code>Calendar</code></strong> — the pickers&#39; grid, inline. It has been in <code>internal/</code> since the first picker shipped, which meant a page wanting a month on it had to open a DatePicker and never close it. <code>mode</code> is <code>single</code>, <code>multiple</code> or <code>range</code>; a second click on a held day takes it out, and a range click below the start begins a new span rather than inverting the old one. <code>renderDay</code> puts a dot or a count under a number. It is <strong>not</strong> a scheduler, and the docs say so: the cells are the control ladder&#39;s heights.</p></li><li><p><strong><code>TreeSelect</code></strong> — the gap between <code>Select</code> and <code>TreeView</code>. <code>selectableBranches</code> is off by default because in most of these trees the branches are the taxonomy and the leaves are the answers, and <code>searchable</code> keeps every ancestor of a match and opens the branches it kept — a tree filtered to bare matches is a list, which is what the tree was chosen over.</p></li><li><p><strong><code>Image</code></strong> — the three things a bare <code>&lt;img&gt;</code> leaves to whoever wrote it: <code>ratio</code> reserves the box, a Skeleton stands in while the file arrives, and a box carrying the <code>alt</code> is drawn if it does not. <code>alt</code> is required by the type, because a missing one and an empty one mean different things and only the second is ever correct.</p></li><li><p><strong><code>ConfirmProvider</code> and <code>useConfirm</code></strong> — &quot;are you sure?&quot; as something you await. A promise rather than an <code>onConfirm</code>, because the code that asks is the code that acts. It never rejects; cancelling, <code>Escape</code> and the backdrop all resolve <code>false</code>. Questions queue rather than replacing each other: resolving an older one to make room reports an answer nobody gave, which at the call site reads as &quot;they said no&quot;.</p></li><li><p><strong><code>Popconfirm</code></strong> — the same question beside the control that raised it. The choice between the two is <strong>reach</strong>, not danger or size.</p></li><li><p><strong><code>VisuallyHidden</code> and <code>Portal</code></strong> — two things the library has needed everywhere and kept to itself. <code>VisuallyHidden</code> is the 1px clipped box behind a Chip&#39;s × and a chart&#39;s screen-reader table; <code>Portal</code> adds the <code>neba-portal</code> class a scoped stylesheet finds a portalled subtree by, which is the reason to reach for it over <code>createPortal</code>.</p></li><li><p><strong>Five things on <code>DataTable</code></strong> — column pinning, <code>columnOrder</code> and drag-to-reorder, in-place cell editing, <code>groupBy</code> with per-column <code>aggregate</code>, and CSV export. Each carries its own sharp edge in the docs: a pinned column moves to its edge, an order that does not name a key leaves it alone, editing needs both a column that allows it and a handler above it, grouping turns virtual scrolling off, and an export is every row the search and sort left rather than the page the reader is on.</p></li></ul><h3 id="fixed-3" tabindex="-1">Fixed <a class="header-anchor" href="#fixed-3" aria-label="Permalink to &quot;Fixed&quot;">​</a></h3><ul><li><p><strong>A folded group in <code>DataTable</code> kept its rows but lost its own heading</strong>, so there was no way to unfold it. Caught by its own test on the way in; the body now renders group by group rather than deriving headings from the rows it can see.</p></li><li><p><strong><code>shortcut</code> on <code>CommandPalette</code> now binds every spelling <code>Shortcut</code> draws.</strong> <code>Cmd+K</code>, <code>Command+K</code>, <code>Meta+K</code> and <code>Esc</code> all rendered a correct key cap and none of them fired; <code>Ctrl+K</code> was dead on Windows and Linux, because the matcher folded any <code>Ctrl</code> into the platform&#39;s <code>Mod</code> and then found no <code>Mod</code> in what it had been given. Only <code>Mod+…</code> and a bare key ever worked.</p><p>The cause was two spellings of one idea: the drawing side had an alias table and the binding side had a five-line predicate written separately. <code>src/internal/keys.ts</code> is now the one place that decides which key a token names, and both sides read it — the aliases, the platform, and the <code>Alt</code>-on-a-Mac case where <code>event.key</code> reports <code>˚</code> and the physical key has to be consulted instead. What a key looks like stays in <code>Shortcut</code>.</p></li></ul><h3 id="changed-3" tabindex="-1">Changed <a class="header-anchor" href="#changed-3" aria-label="Permalink to &quot;Changed&quot;">​</a></h3><ul><li><p><strong><code>minDate</code>, <code>maxDate</code> and <code>shouldDisableDate</code> on <code>DatePicker</code> are read at <code>granularity</code>.</strong> A minimum of 15 March leaves March pickable at <code>month</code>, since part of March is allowed, and <code>shouldDisableDate</code> is handed the value the cell would produce — the 1st, rather than a day inside the month it is being asked about.</p><p>Nothing changes at <code>day</code>, which is what every existing call is: <code>isUnitOutside</code> at day granularity is the <code>isDayOutside</code> it was before, character for character. The month grid was already making this comparison inline — it is the rule that keeps a month whose <code>minDate</code> falls inside it reachable — so the two coarser grids now call one function instead of restating it, and the footer&#39;s shortcut asks the same question the cells do.</p><p><code>shouldDisableDate</code> reaches a coarser grid only when that grid is the one being chosen from. At <code>day</code> a callback blocking weekends must not grey out every month whose 1st happens to be a Saturday.</p></li><li><p><strong>Every component reads its props through <code>useStyleDefaults</code> before its own destructuring.</strong> That is what makes <code>NebaProvider</code>&#39;s precedence come out as caller → provider → literal, and it is why every row of the table above moved.</p><p>The keys are passed in per component rather than worked out, and that is load-bearing: a key a component does not destructure stays in the props it spreads onto its root, so filling <code>density</code> into one that has none would put <code>density=&quot;compact&quot;</code> on a <code>&lt;div&gt;</code> — and <code>size</code> on an <code>&lt;input&gt;</code> is a real attribute that would quietly resize the field.</p></li><li><p><strong><code>internal/media.ts</code> owns the five breakpoint widths</strong>, which used to live in <code>page-layout.ts</code>. A layout asks &quot;narrower than this&quot; and a caller asks &quot;at least this&quot;; two tables would be two chances to disagree about what <code>md</code> is.</p></li></ul><h2 id="_1-10-0-2026-08-30" tabindex="-1">1.10.0 (2026-08-30) <a class="header-anchor" href="#_1-10-0-2026-08-30" aria-label="Permalink to &quot;1.10.0 (2026-08-30)&quot;">​</a></h2><p>No new components. This one is about the question that follows installing a component library and using it for a week: how do I change how this looks, when the design language and I disagree about one thing on one screen.</p><p><code>className</code> was never the missing half — every component already took one and merged it with its own. What it could never reach was everything <em>behind</em> the root. A <code>TextField</code>&#39;s <code>className</code> lands on the column holding the label, the shell and the two lines under it, which means the <code>&lt;input&gt;</code> itself had no name a caller could use; a <code>Table</code>&#39;s lands on the sheet the table scrolls inside, leaving the <code>&lt;table&gt;</code>, its header band, its rows and its cells unreachable; and a <code>Select</code>&#39;s popup, a <code>Dialog</code>&#39;s backdrop and a <code>Tour</code>&#39;s mask all render at the end of <code>&lt;body&gt;</code>, outside the element <code>className</code> reaches, so no selector written against the root would ever find them. Thirteen components now take a <code>classNames</code> map — seventy-six named parts between them.</p><p>The standing scenarios say what that cost the people who do not use it:</p><table tabindex="0"><thead><tr><th>What you import</th><th>1.9.0</th><th>1.10.0</th></tr></thead><tbody><tr><td><code>Button</code></td><td>5.0 kB</td><td>5.0 kB</td></tr><tr><td><code>Chip</code></td><td>3.0 kB</td><td>3.0 kB</td></tr><tr><td><code>LineChart</code></td><td>11.3 kB</td><td>11.3 kB</td></tr><tr><td><code>CodeBlock</code></td><td>4.9 kB</td><td>4.9 kB</td></tr><tr><td>a whole page shell</td><td>28.4 kB</td><td>28.4 kB</td></tr><tr><td>12 components — a typical app</td><td>67.2 kB</td><td>67.3 kB</td></tr><tr><td>12 components, with Korean</td><td>69.7 kB</td><td>69.9 kB</td></tr><tr><td>25 components — a large one</td><td>111.4 kB</td><td>111.7 kB</td></tr><tr><td>all exports</td><td>240.3 kB</td><td>240.7 kB</td></tr></tbody></table><p>A slot is one more argument into a <code>cx()</code> that was already being called, so nothing was added to the runtime and the five unchanged rows did not move. <code>neba/styles.css</code> does not move at all — there is no new CSS in this release. The 1.9.0 column is the budget as last recorded, which for two rows is a tenth or two above the table in those release notes — four commits of <code>perf</code> and <code>refactor</code> work landed after they were written.</p><h3 id="added-4" tabindex="-1">Added <a class="header-anchor" href="#added-4" aria-label="Permalink to &quot;Added&quot;">​</a></h3><ul><li><p><strong><code>classNames</code> on thirteen components</strong> — <code>TextField</code>, <code>NumberField</code>, <code>Select</code>, <code>Combobox</code>, <code>Checkbox</code>, <code>Switch</code>, <code>RadioGroup</code>, <code>Radio</code>, <code>Table</code>, <code>Dialog</code>, <code>ToastProvider</code>, <code>Tour</code> and <code>CommandPalette</code>. One class name per part, merged with the component&#39;s own rather than replacing it.</p><p><strong>There is never a <code>root</code> key.</strong> <code>className</code> is the root, on every component in the library, and a <code>classNames.root</code> beside it would be a second spelling of an idea that already has one — which is the rule <code>src/types.ts</code> exists to hold. <code>NebaSlots</code> and <code>NebaFieldSlot</code> are in that file for the same reason <code>NebaStyleProps</code> is: <code>label</code>, <code>control</code>, <code>description</code> and <code>error</code> mean the same four things on a <code>TextField</code>, a <code>Select</code>, a <code>Checkbox</code> and a <code>RadioGroup</code>, and what a component adds past them lives with the component.</p><p>The slots worth knowing about are the ones with no other way in. <code>Select</code>&#39;s and <code>Combobox</code>&#39;s <code>popup</code> and <code>item</code>, <code>Dialog</code>&#39;s and <code>CommandPalette</code>&#39;s <code>backdrop</code> and <code>viewport</code>, and <code>Tour</code>&#39;s <code>mask</code> are all portalled or siblings of the element <code>className</code> lands on. A descendant selector written against the root does not reach any of them, and before this there was nothing that did.</p></li><li><p><strong><code>className</code> on <code>Tour</code> and <code>CommandPalette</code></strong>, on the card and on the sheet — the same element <code>Dialog</code> puts one on, because in all three that is what a caller means when they name the component.</p><p><code>ToastProvider</code> still takes none, and that is the answer rather than an omission: it renders no element of its own — it wraps the application and puts a portalled stack on the page — so there is nothing for a root class name to land on. Its <code>viewport</code> and <code>toast</code> are slots instead.</p></li></ul><h3 id="changed-4" tabindex="-1">Changed <a class="header-anchor" href="#changed-4" aria-label="Permalink to &quot;Changed&quot;">​</a></h3><ul><li><p><strong>A <code>Table</code> cell&#39;s padding, alignment and background are still inline styles, and now say so.</strong> They have to be — <code>.vp-doc td</code> and <code>.prose td</code> outrank any one-class utility, which is why the styling moved inline in the first place — so a class handed to <code>cell</code>, <code>headCell</code> or <code>empty</code> can add anything the component does not already set inline and needs an important utility (<code>p-4!</code>) to change what it does. The slot type and the component page both carry that caveat rather than leaving it to be discovered.</p></li><li><p><strong><code>ToastProvider</code>&#39;s description no longer builds an empty class name out of a nested ternary.</strong> Same output, one condition instead of two.</p></li></ul><h3 id="fixed-4" tabindex="-1">Fixed <a class="header-anchor" href="#fixed-4" aria-label="Permalink to &quot;Fixed&quot;">​</a></h3><ul><li><strong><code>npm run size:update</code> no longer leaves the working tree failing <code>prettier --check</code>.</strong> It serialised with <code>JSON.stringify(…, 2)</code>, which puts <code>&quot;imports&quot;: [&quot;Button&quot;]</code> on three lines where Prettier wants one; the file stayed unformatted from the moment the script finished until something else rewrote it, and the only reason that was survivable is that <code>npm run build</code> runs <code>format:fix</code> first, so it was usually undone by accident before anyone looked. The formatter now does the layout, loaded on that path only so the check path — the one CI runs — does not pay for a formatter it never calls.</li></ul><h3 id="documentation-1" tabindex="-1">Documentation <a class="header-anchor" href="#documentation-1" aria-label="Permalink to &quot;Documentation&quot;">​</a></h3><ul><li><p><strong>The getting-started guide stopped promising the wrong thing.</strong> It told a Tailwind user that a <code>className</code> they pass &quot;sorts correctly against the component&#39;s own classes&quot;, which reads as a promise that theirs wins. Same-pass generation is only what lets the two be ordered against each other at all; the order is Tailwind&#39;s own, so a component&#39;s <code>h-10</code> beats a caller&#39;s <code>h-8</code> and its <code>rounded-lg</code> beats a caller&#39;s <code>rounded-full</code>, while <code>bg-red-500</code> wins — decided by the value rather than by who wrote it. The important modifier (<code>h-8!</code>) is the form that always wins, and it is now documented as such.</p></li><li><p><strong>A new section in <a href="https://neba.cdget.com/design/prop-conventions" target="_blank" rel="noreferrer">prop conventions</a></strong> covering all three channels: <code>className</code> on the root, <code>classNames</code> on the parts behind it, and <code>style</code> writing one of the hundred-odd <code>--n-*</code> custom properties a component reads its colour and depth out of. The last of those is the one override in the library that cannot lose — a caller&#39;s <code>style</code> is merged after the component&#39;s own, and an inline custom property has no cascade to compete in.</p></li><li><p>Twenty-four component pages — twelve components in both locales — gain a <code>classNames</code> section, with their rows in the props tables.</p></li><li><p><strong><code>test/package/resolution.test.ts</code> now holds the override contract</strong>, which fails the way everything else in that file fails: silently, and in someone else&#39;s project. Four invariants — that no component drops the <code>className</code> or <code>style</code> it was handed into a props spread, in JSX and in the <code>props</code> object <code>useRender</code> takes; that a slot union is declared beside the component that offers it; that it never names <code>root</code>; and that every slot offered is actually read. A slot that type-checks, reads as supported and does nothing is exactly what this catches.</p></li><li><p>Thirty-nine new test cases across thirteen files, taking the suite to 2,466.</p></li></ul><h2 id="_1-9-0-2026-08-30" tabindex="-1">1.9.0 (2026-08-30) <a class="header-anchor" href="#_1-9-0-2026-08-30" aria-label="Permalink to &quot;1.9.0 (2026-08-30)&quot;">​</a></h2><p>Sixteen components, and they come from one question asked properly: what does a large application still have to write by hand after installing this? The answer was in two places. Nine of them were Base UI primitives that had simply never been wrapped — a toggle and its group, a meter, a menu bar, a navigation menu, a hover card, a scroll area, a form and its fieldset. The other seven are the components a product team writes itself around the third month: a command palette, a guided tour, a two-list transfer, a table of contents that follows the scroll, a key-value panel, a stack of avatars, and a gauge.</p><p>Twenty new exports, and the standing scenarios say what they cost the people who do not import them:</p><table tabindex="0"><thead><tr><th>What you import</th><th>1.8.1</th><th>1.9.0</th></tr></thead><tbody><tr><td><code>Button</code></td><td>5.0 kB</td><td>5.0 kB</td></tr><tr><td><code>Chip</code></td><td>3.0 kB</td><td>3.0 kB</td></tr><tr><td><code>LineChart</code></td><td>11.0 kB</td><td>11.0 kB</td></tr><tr><td>a whole page shell</td><td>28.1 kB</td><td>28.1 kB</td></tr><tr><td>12 components — a typical app</td><td>67.0 kB</td><td>67.1 kB</td></tr><tr><td>25 components — a large one</td><td>110.8 kB</td><td>111.4 kB</td></tr><tr><td><code>DataList</code></td><td>—</td><td>1.4 kB</td></tr><tr><td><code>Anchor</code></td><td>—</td><td>2.0 kB</td></tr><tr><td><code>Meter</code></td><td>—</td><td>3.9 kB</td></tr><tr><td><code>Form</code> with <code>Fieldset</code></td><td>—</td><td>3.9 kB</td></tr><tr><td><code>GaugeChart</code></td><td>—</td><td>5.4 kB</td></tr><tr><td><code>CommandPalette</code></td><td>—</td><td>36.6 kB</td></tr><tr><td>all exports (126 → 146)</td><td>215.3 kB</td><td>239.8 kB</td></tr></tbody></table><p>The four unchanged rows are the ones worth reading: twenty new exports, and a bundle that did not ask for them is the same size to the byte. The two application-sized rows moved by 0.1 kB and 0.6 kB, and for a reason that is in <strong>Changed</strong> below rather than for anything that stopped being shaken out — every field component now renders the message its validity already had.</p><p>The spread in the new rows is Base UI rather than Neba. <code>DataList</code> is a <code>&lt;dl&gt;</code> and weighs what one weighs; <code>CommandPalette</code> is a modal dialog over an autocomplete, and the floating machinery underneath it is most of thirty-six kilobytes — shared with <code>Menu</code>, <code>Select</code>, <code>Popover</code> and <code>Dialog</code> the moment a page uses any of them. <code>neba/styles.css</code> moves 19.7 kB → 20.8 kB gzipped.</p><h3 id="added-5" tabindex="-1">Added <a class="header-anchor" href="#added-5" aria-label="Permalink to &quot;Added&quot;">​</a></h3><ul><li><p><strong><code>Toggle</code> and <code>ToggleGroup</code>.</strong> A button that stays down. The difference from a <code>Switch</code> is what the press <em>is</em> — a switch changes a setting and the change is the point, a toggle changes the state of the thing beside it: bold on the selected words, the grid on the canvas, the filter on the list. The difference from a <code>Checkbox</code> is that this one is a control rather than an answer, so it never goes in a form.</p><p><code>variant</code> says how the toggle looks while it is <strong>off</strong>, and on is always the colour family asserting itself. That is the whole design: the ink at rest is <code>--neba-muted-fg</code> in all three weights, because a Button at rest is an action waiting to be taken while a toggle at rest is a state that is currently <em>false</em>, and accent ink on an unpressed toggle says it is on. With no children it goes square around its icon, which is the shape a toolbar wants.</p><p><code>ToggleGroup</code> squares off the corners facing a neighbour exactly as <code>ButtonGroup</code> does, owns the value as an array in both the single and the multiple case, and sets <code>variant</code>, <code>size</code>, <code>color</code>, <code>density</code>, <code>elevation</code> and <code>disabled</code> once for the set. It provides the same context a <code>ButtonGroup</code> provides rather than a second one spelled identically, so a <code>Toggle</code> dropped into a <code>ButtonGroup</code> picks the set up too.</p></li><li><p><strong><code>Meter</code>.</strong> How much of something there is, on a scale known in advance — disk used, seats taken, quota spent, a password&#39;s strength. It looks exactly like a <code>ProgressLinear</code> and is not one: a progress bar is about <em>time</em>, so it may have no value at all and is expected to move on its own, while a meter is about <em>quantity</em> and does not move unless the thing it measures does. <code>value</code> is therefore required, and <code>role=&quot;meter&quot;</code> rather than <code>role=&quot;progressbar&quot;</code>.</p><p><code>thresholds</code> is the reason it earns a component. A meter&#39;s whole job is that where the value sits is what it means — 40% of a disk is fine, 95% is a page — and left to the caller that is a ternary at every call site, with the fourth one disagreeing about where amber starts.</p></li><li><p><strong><code>GaugeChart</code>.</strong> The same reading bent into an arc, and deliberately the same component in two shapes: <code>value</code>, <code>min</code>, <code>max</code> and <code>thresholds</code> mean exactly what they mean on a <code>Meter</code>, so a figure can move from a bar to a dial without changing what it says. Reach for the bar in a row of fields and for the dial in a tile of its own, where it reads from across a room and four pixels of bar does not.</p><p><code>sweep</code> opens the dial symmetrically about twelve o&#39;clock — <code>180</code> for a dashboard tile, <code>270</code> for the instrument shape, <code>360</code> for a ring — and the drawing is sized against the box for the sweep it was given, so a half-dial leaves no empty half above it. It is not a <code>PieChart</code> with <code>shape=&quot;semi&quot;</code>: a pie is parts of a whole and every slice is a category, while the unfilled part of a gauge is not a second category, it is the rest of the dial. The reading in the middle is real text rather than an SVG <code>&lt;text&gt;</code>, so it is selectable, findable and in the accessibility tree.</p></li><li><p><strong><code>Menubar</code>.</strong> The strip of words at the top of an application — File, Edit, View. What makes it a bar rather than a row of separate menus is what happens once one is open: moving along the strip walks through the others instead of closing the one you left, and the arrow keys move between the menus as well as inside them. <code>MenubarMenu</code> takes a <code>label</code> and the same <code>MenuItem</code>, <code>MenuGroup</code>, <code>MenuSeparator</code>, <code>MenuCheckboxItem</code>, <code>MenuRadioGroup</code> and <code>MenuSubmenu</code> a <code>Menu</code> takes, because it is the same menu.</p><p>It draws no surface, and the words sit on a ladder one rung below the control heights at every step. A menu bar is a strip of <em>words</em> rather than a row of buttons, and it is always on something that already has a height — a <code>Toolbar</code>, a <code>WindowPane</code>&#39;s title bar, a <code>Header</code>. Sized as controls, <code>File Edit View</code> would make the bar taller than the thing it is drawn on.</p></li><li><p><strong><code>NavigationMenu</code>.</strong> A site&#39;s navigation: a row of destinations, some of which open a panel of more of them. The difference from a <code>Menu</code> is what the rows <em>are</em>. A menu holds actions, so its rows are <code>menuitem</code>s and the whole thing is a widget. This holds links, so it is a <code>&lt;nav&gt;</code> full of real <code>&lt;a href&gt;</code>s — which is what puts them in the link list a screen reader pulls up, on the status bar, in a &quot;open in new tab&quot;, and in a crawler&#39;s index. An item with children is a trigger and a panel; an item with an <code>href</code> and nothing else is a link, and the two are announced differently.</p><p>One panel is open at a time and it resizes between items rather than closing and reopening, so crossing the row reads as one surface rather than three. <code>columns</code> lays a wide panel out in two or three.</p></li><li><p><strong><code>CommandPalette</code>.</strong> Everything an application can do, behind one field — the shape a keyboard-first product takes once it has more actions than a menu bar can hold. Not a <code>Menu</code>, which is a short list in one place where every row is visible before you look for it; not a <code>Combobox</code> either, because what comes back is not a value, it is something happening.</p><p><code>keywords</code> is the prop that decides whether anyone opens it twice: words the query matches and that are never drawn, so <code>Roll back</code> is found by typing <code>undo</code> and <code>Deploy production</code> by typing <code>ship</code>. <code>group</code> draws a heading each time it changes. <code>shortcut</code> binds the opening keystroke on the window and defaults to <code>Mod+K</code> — <code>Mod</code> being Command on a Mac and Control everywhere else, read from the same spelling <code>Shortcut</code> draws, so the label on the screen and the key that actually works cannot drift apart. <code>shortcut={false}</code> binds nothing, for an application that owns its own keyboard.</p></li><li><p><strong><code>Tour</code>.</strong> A guided walk over a page that already exists — the three things a new reader has to be shown once, pointed at where they actually are. It is <code>HowToSteps</code> turned inside out: that component puts the instructions <em>in</em> the page and the reader follows them, this one leaves the page alone and stands over it. Each step names its target with a CSS selector rather than restating it, because what a tour is about is already on screen and a second copy inside the card is a second copy to keep in step.</p><p>The dimming is one element with a hole in it — a box the size of the target carrying a shadow larger than any screen — rather than four rectangles around it, because the corners of a four-piece scrim never quite meet and the seams show the moment the dimming is anything but opaque. It never takes the pointer either, so the control being pointed at can still be used while the card is up, which is the whole difference between a tour and a sequence of dialogs. A step with no <code>target</code> is centred with nothing cut out, for a welcome card and a closing one.</p></li><li><p><strong><code>Transfer</code>.</strong> Two lists and the arrows between them: everything that could be chosen on one side, everything that has been on the other. The shape for a choice that is <em>long</em> — the columns in a report, the permissions on a role, the people on a channel — where a <code>Combobox</code> with forty chips in its field stops being readable and forty checkboxes give no answer to &quot;what did I actually pick&quot;. Below about a dozen options, one of those two is the smaller component.</p><p>Ticking a row is not choosing it: ticks say which rows the next press moves and <code>value</code> says which side they are on, and keeping the two apart is what lets a filter hide a row without silently moving it. The order of <code>items</code> is the order both lists show, so a row does not jump when it is sent across and back.</p></li><li><p><strong><code>Anchor</code>.</strong> The list of headings on the page being read, with the one the reader is in marked. Real <code>&lt;a href=&quot;#…&quot;&gt;</code>s in a real <code>&lt;nav&gt;</code>: they jump to their headings with JavaScript off and they are in the link list, and the scroll tracking is added on top rather than being the thing that makes it work.</p><p>The marked row is the last heading whose top has passed the line, which is the only rule that reads correctly going <em>up</em> as well as down, and the last heading wins once the scroll reaches the bottom — otherwise a final section with less content than a viewport is the one section that can never be marked. Nothing is marked while the reader is still above the first heading. <code>offset</code> clears a sticky header and <code>container</code> names what scrolls when it is not the document, which is what a <code>PageLayout</code> with <code>scroll=&quot;content&quot;</code> needs.</p></li><li><p><strong><code>DataList</code>.</strong> A list of things and what they are called — a details panel, a summary of a record, the metadata under a heading. Real <code>&lt;dt&gt;</code>/<code>&lt;dd&gt;</code> pairs rather than a two-column <code>Table</code>, and the difference is not cosmetic: a table is a grid of rows all of the same shape and a screen reader walks it as a grid, while this is a set of pairs and each one is read as &quot;label, value&quot;. The label column sizes itself to the widest label, so every value in the list starts at the same place without anybody measuring.</p></li><li><p><strong><code>AvatarGroup</code>.</strong> A stack of avatars, overlapping, with the ones that did not fit as a count. <code>max</code> is how many are drawn and <code>total</code> how many there are altogether, for a group handed only the first few. The first avatar is on top, because a stack read left to right is read front to back and the one the group is <em>about</em> should not be the one behind everything else.</p></li><li><p><strong><code>HoverCard</code>.</strong> A card that opens when the pointer rests on something, holding a preview of what is on the other side — a person behind a mention, a repository behind a link, a deploy behind an id. It sits between the library&#39;s other two popups and is close to both: a <code>Tooltip</code> is a label the pointer never reaches, a <code>Popover</code> was <em>asked for</em> by a press. This one is uninvited like the first and reachable like the second, so the pointer can cross into it and a link inside it can be followed. <code>closeDelay</code> is what makes the gap between the trigger and the card crossable.</p></li><li><p><strong><code>ScrollArea</code>.</strong> A box with a scrollbar of its own, because the browser&#39;s is drawn by the operating system: seventeen pixels wide on one machine, overlaid and invisible on the next, and a different colour from the sheet it is cut into. Not <code>ScrollZone</code>, which is a <em>rail</em> — a strip of items with buttons that step through them; this is the plain case of a box too small for what is in it, and underneath both are ordinary scroll containers, so the wheel, the trackpad, momentum and the keyboard are the browser&#39;s own.</p><p><code>fade</code> dims the content at each edge that has more beyond it and only there, so there is no fade at the top when you are at the top. It is a mask rather than a gradient painted over the content, because a gradient has to fade <em>to</em> a colour and a scroll area usually sits on a translucent acrylic sheet where there is no such colour.</p></li><li><p><strong><code>Form</code> and <code>Fieldset</code>.</strong> A <code>&lt;form&gt;</code> that knows which of its fields is wrong. On its own, a page of <code>TextField</code>s validates one field at a time and a failed submit leaves the reader to find the red one; what this adds is the part that has to be owned above the fields — a submit collects every field&#39;s validity at once and moves focus to the first that failed, so the reader is taken to the problem rather than told there is one. <code>errors</code>, keyed by each field&#39;s <code>name</code>, puts a server&#39;s answer back on the field it belongs to instead of in a banner, and clears it as soon as that field changes.</p><p>It is not a form <em>library</em>. There is no schema, no resolver and no field array here — a project that wants those keeps them and hands the result to <code>errors</code>, which is the seam this is built around. <code>validationMode</code> defaults to <code>onSubmit</code>, the only setting that does not tell somebody their email is wrong while they are still typing it.</p><p><code>Fieldset</code> is the grouping and draws no surface, because a group of fields is a grouping and not a sheet — the sheet already exists, and this goes inside a <code>Card</code>. What it owns is the legend, the gap the controls stand at, and the one thing only a real <code>&lt;fieldset&gt;</code> can do: <code>disabled</code> reaches every control inside it, including ones a component three levels down rendered and never heard of it.</p></li><li><p>Three new message namespaces in all eighteen languages — <code>anchor</code> for the table of contents&#39; <code>&lt;nav&gt;</code> name, <code>transfer</code> for the two headings, the two buttons, the filter and the select-all, and <code>command</code> for the palette&#39;s placeholder, its empty line and the name of a dialog that has no visible title — plus <code>steps.skip</code>, which <code>Tour</code> and <code>HowToSteps</code> share. All four together move a twelve-component app with Korean registered from 69.2 kB to 69.6 kB.</p></li><li><p><strong><code>ScrollZone</code> takes the wheel, where it is asked to.</strong> A mouse has one wheel and it points down the page, which is the one axis a horizontal strip does not run along, so <code>wheel</code> turns a wheel rolled over the strip into travel along it. It is off by default, because a wheel taken from the page is the page&#39;s: a reader who meant to scroll past the shelf would be held by it instead. What it does take it gives back — at either end of the strip the wheel is the page&#39;s again — and a trackpad swiping sideways is left alone, since that already scrolls the strip and answering it here would move it twice as far as it was asked to.</p></li></ul><h3 id="changed-5" tabindex="-1">Changed <a class="header-anchor" href="#changed-5" aria-label="Permalink to &quot;Changed&quot;">​</a></h3><ul><li><p><strong>A field with no <code>error</code> of its own now shows the message its validity already had.</strong> <code>TextField</code>, <code>NumberField</code>, <code>OtpField</code>, <code>Select</code>, <code>Combobox</code>, <code>Checkbox</code>, <code>RadioGroup</code> and <code>Switch</code> rendered a message only when the caller passed one, so a <code>required</code> field that failed the browser&#39;s own constraint went red and said nothing, and a <code>Form</code>&#39;s <code>errors</code> marked a field invalid without ever drawing the sentence. Each of the eight now falls through to a plain <code>Field.Error</code>, which renders the current validation message and nothing at all while the field is valid. An explicit <code>error</code> still wins and still shows whenever the field is invalid. This is the tenth of a kilobyte in the twelve- and twenty-five-component rows above.</p></li><li><p><strong><code>Menu</code> no longer passes <code>modal</code> when it was not given one.</strong> Base UI&#39;s default is the same <code>true</code>, and it warns when the prop is set on a menu that turns out to be nested — which is every menu on a <code>Menubar</code> and every submenu. Not passing it is how the default stays a default; nothing about how a menu behaves has changed.</p></li><li><p><strong><code>NebaThreshold</code> is in the shared vocabulary</strong>, in <code>src/types.ts</code> beside <code>NebaSize</code> and <code>NebaColor</code>, because <code>Meter</code> and <code>GaugeChart</code> are the same reading in two shapes and a page carrying both must not disagree about where amber starts. It is the one place in the library where a semantic colour is <em>computed</em>.</p></li><li><p><code>fill()</code> — the <code>{index} of {total}</code> interpolation — moved from <code>HowToSteps</code> into <code>internal/i18n.ts</code>, now that a second component counts steps. The placeholders are part of the message format, and a language that orders them the other way round is exactly the case a second copy of that function would eventually get wrong.</p></li><li><p><strong><code>ScrollZone</code>&#39;s <code>buttonPlacement</code> now defaults to <code>inline</code>.</strong> The buttons stand beside the strip rather than over its ends, so an item is cut off at the button&#39;s edge instead of sliding beneath it, and the button is legible over the page rather than over whatever it landed on. <code>overlay</code> is still there and is still what a shelf of pictures wants, where the thing under a button is a picture that carries on. The type lists <code>inline</code> first now, the way every other union in the file puts its default first.</p></li></ul><h3 id="documentation-2" tabindex="-1">Documentation <a class="header-anchor" href="#documentation-2" aria-label="Permalink to &quot;Documentation&quot;">​</a></h3><ul><li>Thirty-two component pages — sixteen components in both locales — with their props rows, sixty-one demos, sixteen cards in the component gallery and their blocks on the sample screen. <code>llms.txt</code> and the README&#39;s component lists carry all sixteen.</li><li>The sample screen gains the two components that are only really visible in a page that already exists: a <code>Tour</code> hung off the application header it points at, and a <code>CommandPalette</code> opened by the <code>Mod+K</code> row the Keyboard card was already advertising.</li><li>One hundred and eighty new test cases across sixteen files, taking the suite to 2,228.</li><li><code>CLAUDE.md</code> records the new group memberships, the two internal modules that gained a second reader (<code>button-group.ts</code> for <code>ToggleGroup</code>, <code>menu.ts</code> for <code>Menubar</code>), the new <code>internal/avatar-group.ts</code>, and the refreshed bundle table.</li></ul><h2 id="_1-8-1-2026-08-29" tabindex="-1">1.8.1 (2026-08-29) <a class="header-anchor" href="#_1-8-1-2026-08-29" aria-label="Permalink to &quot;1.8.1 (2026-08-29)&quot;">​</a></h2><p>Neba could not be imported into a React Server Component, and nothing in this repository could have said so. React&#39;s <code>react-server</code> build does not export <code>useState</code>, <code>useEffect</code>, <code>useRef</code>, <code>useContext</code> or <code>createContext</code> at all, so the fifty-three modules here that called one were a <code>TypeError</code> in someone&#39;s Next.js app rather than a component that rendered badly — and every check stayed green, because nothing here renders on a server.</p><h3 id="fixed-5" tabindex="-1">Fixed <a class="header-anchor" href="#fixed-5" aria-label="Permalink to &quot;Fixed&quot;">​</a></h3><ul><li><strong>Every component is a client component and now says so.</strong> <code>&#39;use client&#39;</code> on all ninety-nine component files, and on the seven modules under <code>internal/</code> that hold a context or an effect. Importing one into a Server Component works, with no wrapper of your own and no <code>transpilePackages</code> entry. The directive marks a boundary rather than a page: a Server Component that renders a <code>Button</code> stays a Server Component, and only the components it renders reach the browser. What has not changed is the ordinary rule about that boundary — an event handler defined in a Server Component still cannot be passed across it.</li><li>All of them, and not only the ones that hold state today. Thirteen components would technically have survived a server render; eight of those are Base UI form controls that go inside a client boundary anyway, and the other five are one prop from failing, since <code>transition</code> is a <code>useLayoutEffect</code> and <code>render</code> is Base UI&#39;s <code>useRender</code> and nearly every component in the library already takes one or the other. A per-component answer would be a table that rots. It costs nothing measurable either: every <code>npm run size</code> scenario is unchanged to the byte, because a bundler hoists the directive rather than shipping it a hundred and six times.</li><li><strong>The <code>neba</code> barrel, the component barrels and <code>neba/locales</code> are deliberately left unmarked</strong>, and each would break if they were not. A barrel only re-exports, so unmarked it belongs to whichever graph imports it and a Server Component importing <code>neba</code> reaches the client modules behind it rather than a boundary of its own. <code>registerMessages</code> stays a plain function for the same shape of reason: marked, it would arrive in a consumer&#39;s server module as a client reference instead of a function and throw when called.</li><li><strong><code>terser.config.json</code> sets <code>compress.directives: false</code></strong>, which is the half of this that would otherwise have failed silently. <code>directives</code> removes &quot;redundant or non-standard&quot; directives, and in a module — where <code>use strict</code> is implied — terser reads <code>use client</code> as both, stripping it out of all one hundred and six files without a word. The published package would have said nothing at all to Next.js while every check here still passed. It is <code>output.preserve_annotations</code>&#39; twin: two settings, each keeping one thing terser eats on the way out.</li></ul><h3 id="documentation-3" tabindex="-1">Documentation <a class="header-anchor" href="#documentation-3" aria-label="Permalink to &quot;Documentation&quot;">​</a></h3><ul><li><strong>Next.js and React Server Components</strong>, a new section in <em>Getting started</em> in both locales: what the directive does and does not do, why a handler written in a Server Component cannot be passed to a <code>Button</code>, where the stylesheet is imported in an App Router project, and how to register a language from a module that is in the client graph. <code>&#39;use client&#39;</code> is a string at the top of a file, so Vite, webpack, Remix, Astro and plain React ignore it and nothing above changes what the package does there. The README carries the short form.</li><li><code>test/package/resolution.test.ts</code> gains four checks, since this is exactly the kind of invariant that is invisible here and expensive in someone else&#39;s project: that every component starts with the directive, that every module calling a client-only React API does too, that the barrels and the locales do not, and that the terser setting holding it all up is still there. <code>CLAUDE.md</code> records the rule and its three deliberate exemptions.</li></ul><h2 id="_1-8-0-2026-08-29" tabindex="-1">1.8.0 (2026-08-29) <a class="header-anchor" href="#_1-8-0-2026-08-29" aria-label="Permalink to &quot;1.8.0 (2026-08-29)&quot;">​</a></h2><p>Everything Neba had until now went <em>inside</em> a page somebody else had already built. This release builds the page: a layout and the three regions it arranges, the mark that goes in the corner of it, and two components for the kind of page you would put in it — a code viewer and a step-by-step guide.</p><p>Eight components, nine exports, and not a byte on anyone who does not import them:</p><table tabindex="0"><thead><tr><th>What you import</th><th>1.7.0</th><th>1.8.0</th></tr></thead><tbody><tr><td><code>Button</code></td><td>5.0 kB</td><td>5.0 kB</td></tr><tr><td><code>Chip</code></td><td>3.0 kB</td><td>3.0 kB</td></tr><tr><td><code>LineChart</code></td><td>11.0 kB</td><td>11.0 kB</td></tr><tr><td>12 components — a typical app</td><td>67.0 kB</td><td>67.0 kB</td></tr><tr><td>25 components — a large one</td><td>110.8 kB</td><td>110.8 kB</td></tr><tr><td>a whole page shell</td><td>—</td><td>28.1 kB</td></tr><tr><td><code>CodeBlock</code></td><td>—</td><td>4.8 kB</td></tr><tr><td>all exports (117 → 126)</td><td>206.8 kB</td><td>215.3 kB</td></tr></tbody></table><p>The five unchanged rows are the interesting ones: they are what says the new modules are still shaken out of a bundle that did not ask for them. <code>neba/styles.css</code> moves 17.5 kB → 19.7 kB gzipped, and 0.8 kB of that is CodeBlock&#39;s ported themes — see below.</p><h3 id="added-6" tabindex="-1">Added <a class="header-anchor" href="#added-6" aria-label="Permalink to &quot;Added&quot;">​</a></h3><ul><li><p><strong><code>PageLayout</code>, with <code>Header</code>, <code>Footer</code> and <code>Sidebar</code>.</strong> The skeleton a page is hung on, and what it is really for is the landmarks: a page assembled out of divs is one undifferentiated region to a screen reader and one undifferentiated blob to a crawler, while the same page built out of <code>&lt;header&gt;</code>, <code>&lt;aside&gt;</code>, <code>&lt;main&gt;</code> and <code>&lt;footer&gt;</code> has a table of contents. <code>PageLayout</code> arranges the four, contributes the <code>&lt;main&gt;</code> and the skip link that jumps to it, and answers the questions the four cannot answer alone.</p><p><code>headerSpan</code> and <code>footerSpan</code> decide which of the bar and the rail takes the top corner — <code>full</code> is a website, <code>content</code> is an application — and they are asked separately, because a dashboard with a full-height rail still usually wants its copyright line under the content rather than under the rail. <code>scroll</code> decides whether the document scrolls or only the region between the bars. <code>sidebar</code> and <code>endSidebar</code> are two slots, for navigation down one side and a table of contents, an inspector or a filter panel down the other.</p><p>The arrangement is flexbox and media queries on purpose: everything that decides where a column goes is stated in CSS, so the layout is right in the first frame the browser paints and right with JavaScript off. The only measurement is the header&#39;s and the footer&#39;s height, and only because a sidebar that holds its place has to start below a bar whose height nobody but the bar knows.</p></li><li><p><strong>A <code>Sidebar</code> below <code>collapseBelow</code> is a <code>Drawer</code></strong>, not a second drawer written beside it — over a scrim, with a focus trap, an Escape and a way back to the trigger. Its children exist once either way, so nothing inside is in the document twice for a screen reader to read twice. <code>SidebarTrigger</code> is the hamburger that opens it and is <em>hidden</em> above the breakpoint by a class rather than being absent, or every phone would draw a header and then pop a button into it a moment later. A sidebar can also be <code>resizable</code>, dragged by its inner edge and reported through <code>onResizeEnd</code>.</p></li><li><p><strong><code>Header</code> and <code>Footer</code> are usable on their own</strong>, which is most of why they are separate components. A header takes three slots — <code>brand</code>, the middle, and <code>actions</code> — because the arrangement is fixed and what a caller wants to decide is what goes in each; <code>align=&quot;center&quot;</code> centres the middle on the bar&#39;s own midline rather than in the space left over, so a logo one character longer does not move the navigation. Both take <code>position</code>, <code>maxWidth</code> on Container&#39;s ladder, and a <code>divider</code>.</p></li><li><p><strong><code>AppLogo</code>.</strong> A product&#39;s mark, at a known size, that is never an empty box. Four things can be the mark and exactly one is at a time: markup handed to <code>children</code>, an image at <code>src</code>, the initials of <code>name</code> on a tile, or — with no tile to put them on — the name itself set as the logotype. That last one is the point: a product that has not drawn a logo yet still has a logo, and swapping it for the real file later is one prop.</p><p><code>shape</code> is the decision an <code>&lt;img&gt;</code> cannot make for you: a mark drawn as a bare glyph and one drawn with its own background need opposite treatment and the file cannot say which it is. <code>bare</code> keeps the artwork&#39;s proportions and draws nothing behind it; <code>app</code> and <code>circle</code> inset it into a tile. <code>bare</code> is the default because a logo file very often has the product&#39;s name set into it, which is also why <code>name</code> is read out rather than drawn a second time.</p></li><li><p><strong><code>CodeBlock</code>.</strong> A viewer for one line of code or a thousand. Everything it draws above the code is optional and off one prop each — <code>toolbar</code>, <code>showLanguage</code>, <code>copyable</code>, <code>rawToggle</code>, <code>lineNumbers</code>, <code>startLine</code>, <code>prompt</code> — because the same component has to be a bare snippet inside a sentence and the full transcript at the top of a README, and those are the same block with different things turned on rather than two components.</p><p>Syntax highlighting is highlight.js, and <strong>every specifier that reaches it is behind an <code>import()</code></strong>: the core in one chunk, one chunk per grammar. So the block is 4.8 kB in the bundle a page downloads to draw its first frame, <code>highlight={false}</code> fetches none of the rest, and a block colouring TypeScript fetches about 11 kB more after the paint. Thirty-four languages come with it; <code>registerLanguage</code> is <code>registerMessages</code>&#39; arrangement for the other hundred and fifty.</p><p><code>prompt</code> draws a shell symbol — <code>$</code>, <code>#</code>, <code>C:\\&gt;</code> — that is never actually there: it is generated content, so it cannot be selected, cannot be found by find-in-page and never reaches the clipboard. A transcript stays a transcript and still pastes into a shell. <code>highlightLines</code> marks lines with a tinted row and a rule down the leading edge, taking a number, a string of lines and ranges (<code>&#39;1,4-9,12&#39;</code>) or an array of either, counted the way the gutter counts. <kbd>Ctrl</kbd>/<kbd>⌘</kbd> + <kbd>A</kbd> inside the focused block selects the code and nothing else.</p><p><strong><code>theme</code> is the one colour decision in the library that does not follow the page.</strong> Code is read against a background chosen for code. Four themes are the library&#39;s own — <code>dark</code> (the default), <code>light</code>, <code>auto</code> and <code>mono</code> — and eight are ports kept at their published values: <code>one-dark</code>, <code>dracula</code>, <code>monokai</code>, <code>nord</code>, <code>night-owl</code>, <code>gruvbox</code>, <code>github</code> and <code>solarized-light</code>. A theme is a set of <code>--n-code-*</code> custom properties under a <code>[data-code-theme]</code> selector and nothing else, so the prop takes any string and a project that writes one in its own CSS has a theme with nothing to import and nothing to register.</p></li><li><p><strong><code>HowToSteps</code>.</strong> A guide the reader walks through: numbered steps down one side, one step&#39;s instructions beside them, a way forward under those, and an end that says so. It is <code>Timeline</code>&#39;s interactive sibling and the two are deliberately not one component — a Timeline <em>reports</em> and nothing in it is pressed, while a HowToSteps <em>asks</em>.</p><p>Every step&#39;s body is rendered into the same grid cell, with the ones not showing left in the document, <code>invisible</code> and <code>inert</code>, so the panel is as tall as the tallest step at every moment: moving from a one-line step to one with a code block in it does not resize the card the guide sits in, which on a page the reader has already scrolled moves everything under it. Nothing is remounted either, so a form halfway through a guide still holds what was typed into it.</p><p><code>orientation</code> runs the numbers down a column or across the top; <code>maxHeight</code> scrolls a long list and keeps the current row in view; <code>divider</code> puts a hairline between the list and the body; a step&#39;s <code>icon</code> is drawn before its title over the body and never in the list, where the numbered disc already says which step it is. <code>transition</code> is the entrance a step arrives with, from the library&#39;s usual motion vocabulary plus <code>&#39;none&#39;</code> — the one place that prop runs on something other than a mount, and inside the rule against moving a control for the same reason the rule exists: the effect is on the panel, and the buttons and rows that changed it hold still.</p></li><li><p>Three new message namespaces in all eighteen languages — <code>layout</code> for the skip link, the sidebar&#39;s name and the button that opens it, <code>code</code> for the copy button and the raw toggle, and <code>steps</code> for the four buttons and the sentence at the end. All three together move a twelve-component app with Korean registered from 68.7 kB to 69.2 kB, which is the whole of what a new set of words costs a project that has registered a language.</p></li><li><p><code>npm run size</code> gains a <strong>page shell</strong> scenario — <code>PageLayout</code> with <code>Header</code>, <code>Footer</code>, <code>Sidebar</code>, <code>SidebarTrigger</code> and <code>AppLogo</code> — and prints each scenario&#39;s on-demand chunk total beside its entry. That second number is unbudgeted and exists so CodeBlock&#39;s grammars cannot quietly become the entry&#39;s problem: the day that <code>import()</code> turns static, 4.8 kB becomes 68.</p></li></ul><h3 id="changed-6" tabindex="-1">Changed <a class="header-anchor" href="#changed-6" aria-label="Permalink to &quot;Changed&quot;">​</a></h3><ul><li><strong><code>highlight.js</code> is a second runtime dependency</strong>, alongside Base UI. Only <code>CodeBlock</code> reaches it, only through a dynamic import, and it therefore never lands in a bundle that did not ask for it. It is a real dependency rather than an optional peer because a specifier a bundler cannot resolve fails the <em>whole</em> build — Rollup walks and resolves <code>CodeBlock.js</code> while it is still deciding whether to keep it — so an optional peer would break <code>import { Button } from &#39;neba&#39;</code> for anyone who had not installed a highlighter.</li><li>A <code>Header</code>&#39;s three slots are held apart by their own gap ladder, about twice the gap <em>inside</em> a slot. One ladder was doing both jobs, which put the first navigation link exactly as far from the logo as the logo sits from its own name, so the eye grouped the wrong things and the bar read as one undifferentiated row.</li></ul><h3 id="fixed-6" tabindex="-1">Fixed <a class="header-anchor" href="#fixed-6" aria-label="Permalink to &quot;Fixed&quot;">​</a></h3><ul><li><strong>A press on a <code>FloatingBottomNavigation</code> no longer moves the bar&#39;s height.</strong> The name on a floating destination collapsed in both axes, so an item without one was shorter than an item with one — and since a press animates two items at once, the tallest item in the row dipped to somewhere between the two heights and came back, taking the sheet with it. On a lozenge floating over the page that read as the whole bar wobbling. Only the column track travels now.</li></ul><h3 id="documentation-4" tabindex="-1">Documentation <a class="header-anchor" href="#documentation-4" aria-label="Permalink to &quot;Documentation&quot;">​</a></h3><ul><li>Seven component pages in both locales — <code>PageLayout</code>, <code>Header</code>, <code>Footer</code> and <code>Sidebar</code> under <strong>Layout</strong>, <code>AppLogo</code> and <code>CodeBlock</code> under <strong>Display</strong>, <code>HowToSteps</code> under <strong>Surfaces</strong> — with their props rows, thirty-five demos, seven cards in the component gallery, and their blocks on the sample screen.</li><li>The sidebar and page-layout demos draw their navigation as plain text links rather than as a bordered <code>List</code>, which is what a navigation rail is; the header demos&#39; links lose their underline, which is the case <code>TextLink</code>&#39;s three-way <code>underline</code> prop exists for.</li><li><code>CLAUDE.md</code> records the one deliberate exception to the stylesheet&#39;s marginal cost: CodeBlock&#39;s eight ported themes are 0.8 kB gzipped that everybody carries and only a CodeBlock user sees. Shipping them as tree-shakeable JS token objects was the alternative and was rejected, because it costs both the string prop and the consumer&#39;s own theme.</li></ul><h2 id="_1-7-0-2026-08-27" tabindex="-1">1.7.0 (2026-08-27) <a class="header-anchor" href="#_1-7-0-2026-08-27" aria-label="Permalink to &quot;1.7.0 (2026-08-27)&quot;">​</a></h2><p>A release about what lands in your bundle. Nothing about how a component looks or behaves has changed; what changed is how much of the library you have to take to get one of them.</p><p>Measured with a real bundler rather than reported from an <code>unpackedSize</code> — rollup and terser, <code>react</code> external, judged on gzip:</p><table tabindex="0"><thead><tr><th>What you import</th><th>1.6.0</th><th>1.7.0</th></tr></thead><tbody><tr><td><code>Chip</code></td><td>14.5 kB</td><td>3.0 kB</td></tr><tr><td><code>LineChart</code></td><td>22.8 kB</td><td>11.0 kB</td></tr><tr><td>12 components — a typical app</td><td>79.8 kB</td><td>67.0 kB</td></tr><tr><td>25 components — a large one</td><td>124.4 kB</td><td>110.8 kB</td></tr><tr><td>all 117 exports</td><td>218.7 kB</td><td>206.8 kB</td></tr></tbody></table><p>The last row is the least interesting one, and that is the point: taking the whole library is barely cheaper, because nothing was removed. What changed is the price of taking a <em>part</em> — and the reason <code>Chip</code> used to cost fourteen kilobytes was never <code>Chip</code>.</p><h3 id="changed-7" tabindex="-1">Changed <a class="header-anchor" href="#changed-7" aria-label="Permalink to &quot;Changed&quot;">​</a></h3><ul><li><p><strong>Languages are registered rather than shipped.</strong> Neba speaks English out of the box and the other eighteen languages are now modules you turn on:</p><div class="language-tsx vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">tsx</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { registerMessages, ko } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;neba/locales&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">registerMessages</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;ko&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, ko);</span></span></code></pre></div><p>Call it once at module scope, before your first render, for each language you support; then a <code>locale</code> prop translates exactly as it did.</p><p><strong>This is a breaking change in a minor release</strong>, which is deliberate given how few projects are on Neba today, and it is the only one here. A project that passes <code>locale</code> and registers nothing will see English where it used to see a translation — the strings still resolve, they just resolve to the fallback. If you pass a <code>locale</code> anywhere, add the two lines above and you are done. Everything else is unchanged: tags are still matched by script, then region, then language, so registering <code>ko</code> answers <code>ko-KR</code> and <code>zhHans</code> registered as <code>zh-hans</code> answers <code>zh-CN</code> and a bare <code>zh</code>; a language still fills in only what it has and English answers for the rest; and every string a component invents still has a prop that overrides it.</p><p>The reason is arithmetic. Eighteen languages of sixteen namespaces was one object literal, and a bundler cannot drop a key out of an object literal — so a <code>Chip</code> that wanted the word &quot;Remove&quot; carried the ColorPicker&#39;s colour names and the Table&#39;s column labels in eighteen languages. Registered, a language costs about 1.7 kB gzipped and you pay for the ones you name.</p></li><li><p><code>severityIcons</code> is now <code>severityIcon(color)</code> internally, so a component that draws one severity mark carries one, and no React elements are built at import time for a page that may draw none.</p></li></ul><h3 id="added-7" tabindex="-1">Added <a class="header-anchor" href="#added-7" aria-label="Permalink to &quot;Added&quot;">​</a></h3><ul><li><strong>Every component is its own entry point</strong>, named after its folder: <code>import { Button } from &#39;neba/button&#39;</code>, <code>import { TextField } from &#39;neba/text-field&#39;</code>. The bundle is the same as the barrel&#39;s — <code>import { Button } from &#39;neba&#39;</code> already tree-shook correctly — but the barrel makes a bundler parse two hundred modules to keep five, and the subpath makes it parse five. It is also the escape hatch if your bundler ignores <code>sideEffects</code>.</li><li><code>neba/locales</code> for the barrel and <code>neba/locales/ko</code> for one language, plus the <code>NebaLocale</code> type for a translation of your own.</li><li><code>neba/package.json</code> is exported, which some tooling asks for.</li><li><code>npm run size</code> — a bundle-size budget. Seven scenarios, from one component to all of them, bundled against <code>dist/</code> and checked against the numbers committed in <code>scripts/bundle-budget.json</code>. It runs as its own CI job, so a change that quietly stops something being tree-shakeable fails a pull request instead of shipping. <code>npm run size:update</code> records a new budget when the growth is real and wanted.</li></ul><h3 id="fixed-7" tabindex="-1">Fixed <a class="header-anchor" href="#fixed-7" aria-label="Permalink to &quot;Fixed&quot;">​</a></h3><ul><li><strong>The package can be imported by Node, and by TypeScript on <code>moduleResolution: node16</code>.</strong> Emitted ESM used extensionless relative imports (<code>export * from &#39;./types&#39;</code>), which Node&#39;s resolver rejects outright and which TypeScript on <code>node16</code> or <code>nodenext</code> reported as <em>&quot;Module &#39;neba&#39; has no exported member&quot;</em> — for every component at once. Anything using a bundler was unaffected, which is why it went unnoticed. Every relative specifier now carries its <code>.js</code>.</li><li>The published files now carry <code>@__PURE__</code> annotations, so a bundler can drop the parts of a multi-component module you did not import. They are written into <code>dist/</code> during the build and terser is told to keep them, which it does not do by default — until now the annotations reached the consumer&#39;s bundler stripped, and it kept <code>Tab</code> and <code>TabPanel</code> for anyone who imported only <code>Tabs</code>. <code>Tabs</code> on its own is 23% smaller.</li></ul><h3 id="documentation-5" tabindex="-1">Documentation <a class="header-anchor" href="#documentation-5" aria-label="Permalink to &quot;Documentation&quot;">​</a></h3><ul><li>The README gains a <strong>Languages</strong> section and the subpath import form; <code>CLAUDE.md</code> gains a <strong>Packaging, bundle size and tree-shaking</strong> section with the measurements, the five invariants that hold them in place, and the list of things measured and rejected so they are not tried again — minifier tuning, per-component stylesheets, and dropping Tailwind&#39;s <code>@property</code> fallback.</li><li><code>test/package/resolution.test.ts</code> and <code>test/locales/register.test.tsx</code> are new. Neither renders a component: the first checks the wiring between <code>src/</code>, <code>dist/</code> and <code>package.json</code> that no render test or <code>tsc --noEmit</code> can see, and the second checks the registration contract.</li></ul><h2 id="_1-6-0-2026-08-24" tabindex="-1">1.6.0 (2026-08-24) <a class="header-anchor" href="#_1-6-0-2026-08-24" aria-label="Permalink to &quot;1.6.0 (2026-08-24)&quot;">​</a></h2><h3 id="added-8" tabindex="-1">Added <a class="header-anchor" href="#added-8" aria-label="Permalink to &quot;Added&quot;">​</a></h3><ul><li><strong><code>ScrollZone</code>.</strong> A strip of anything laid out in one direction and scrolled in it — a rail of cards, a row of chips, a column of rows. <code>orientation</code> decides which way it runs, <code>lines</code> how many rows it fills before starting a new column, so one scroll can hold twice as much in the same width, and <code>spacing</code> is the gutter on the same Tailwind scale <code>GridContainer</code> uses. The mechanism underneath is an ordinary scroll container, which is the whole design: touch, the wheel, the scrollbar and the arrow keys are the browser&#39;s own, nothing is transformed, and the strip runs the other way under RTL without being told.</li><li><strong>What a press of its buttons does is a prop.</strong> <code>mode</code> is <code>item</code> (with <code>step</code> for more than one at a time), <code>page</code>, or <code>hold</code> — which scrolls at <code>speed</code> pixels a second for as long as the button is down, and falls back to one item on a press too short to be a hold, so a quick tap is never a dead press. <code>buttons</code> draws them only when there is somewhere to go, always (with the useless one disabled), or never. <code>buttonPlacement</code> decides whether they sit over the strip or beside it: <code>inline</code> stops the scroller where the button starts, so an item is cut off at its edge rather than sliding under it, and the lane is kept even while that button has nowhere to go. <code>drag</code> adds a mouse and pen drag to the finger&#39;s; <code>snap</code> brings the nearest child to the leading edge whenever the scrolling stops.</li><li><strong><code>FloatingBottomNavigation</code>.</strong> <code>BottomNavigation</code> lifted off the page: the same <code>&lt;nav&gt;</code>, the same <code>aria-current</code>, and the same <code>BottomNavigationItem</code> children, which is why the item&#39;s context moved to <code>internal/</code> — two bars provide it and one item reads it. What differs is everything that follows from <code>offset</code>, the gap it floats above the bottom edge: the sheet is a stadium rather than a bar with two corners, it is only as wide as its destinations, it carries a shadow, <code>position</code> gains <code>absolute</code> for a bar that belongs to a region rather than to the window, and <code>labels</code> defaults to <code>selected</code>, because five drawn names would stretch it back into a bar.</li><li><strong>Its highlight belongs to the bar rather than to the destination that is current</strong>, which is what gives it a position to travel: it is measured off whichever item carries <code>aria-current</code> and animates its <code>left</code>, <code>top</code>, <code>width</code> and <code>height</code> to the next one, transforming nothing. A name the bar is not drawing is <em>collapsed</em> rather than clipped — the box it sits in runs between <code>0fr</code> and <code>1fr</code> in both axes — so pressing a destination re-shapes the bar around it: the name grows, its neighbours move over, and the highlight slides under it on one clock.</li><li><strong><code>WindowPane</code>.</strong> Anything at all, drawn the way an operating system draws a window. It is not a real window and does not pretend to be one — there is no desktop and no z-order — but the frame <em>behaves</em>: the title bar drags, all four edges and all four corners resize, and the three buttons are real buttons with real names, so a screenshot, a feature demo or a piece of a landing page can be shown as the thing it will be rather than as a picture of it.</li><li><strong><code>os</code> is eight systems, a version being its own entry wherever the title bar is what changed.</strong> <code>macos</code> and <code>macosx</code> (Aqua — a short striped bar, glossy lights, a bold embossed title); <code>windows11</code> (rounded corners, bar and body one Mica sheet), <code>windows10</code> (square, white, ruled off from the body), <code>windows8</code> (flat, with a band of colour around the whole window), <code>windows7</code> (Aero — a sheet of glass with the content sunk into it and the page blurred through the band as well as the caption) and <code>windowsxp</code> (Luna — the glossy blue caption curve and a band of the same blue down the sides and along the bottom); and <code>linux</code>, a GNOME header bar. The older systems paint their own chrome rather than the page&#39;s, so Luna stays blue and Aqua stays grey on a page switched to dark — the choice <code>Mockup</code>&#39;s finishes already make. Nothing here is a copy of anything: a minimize is a line, a maximize is a box, a close is a cross, and no mark, wordmark or icon belonging to anyone else is drawn.</li><li><code>controls</code> is <code>true</code>, <code>false</code> or exactly the buttons named, in the system&#39;s order rather than the array&#39;s. <code>open</code>, <code>minimized</code> and <code>maximized</code> are each a controlled/uncontrolled pair: closing an uncontrolled window renders nothing, minimizing rolls it up to its title bar — a page has no dock to send it to — and maximizing fills whatever is holding the window. <code>draggable</code> and <code>resizable</code> report through <code>onOffsetChange</code> and <code>onResize</code>, with <code>minWidth</code>/<code>minHeight</code> bounding them.</li><li><strong><code>active</code> looks after itself.</strong> Left out, a window is in front until another <code>WindowPane</code> on the page is pressed or takes the focus; a press on the page <em>around</em> the windows changes nothing, because a paragraph is not a desktop. Being in front is drawn the way each system draws it — coloured traffic lights, an accent title bar and border on Windows 10, a tinted header bar on GNOME — and on all of them it is one step more shadow than the windows behind. <code>accent</code> dyes the title bar (and, on the systems with one, the band) with the colour family; <code>transparency</code> lets the page through the chrome and turns the acrylic on with it, never touching the content on top.</li><li><strong>Maximizing, restoring and rolling up are journeys rather than cuts.</strong> <code>left</code>, <code>top</code>, <code>width</code> and <code>height</code> are what move — never a transform, so no glyph in the window is resampled on the way — and a window that was never given a <code>height</code> is measured and pinned for one frame, because <code>auto</code> is not a length a transition can start from. A rolled-up window keeps its body in the tree, <code>inert</code> and clipped; a closed one fades before it goes; a reader who has asked for reduced motion gets every one of them instantly.</li><li>Two new namespaces in <code>src/internal/i18n.ts</code>, translated into all eighteen locales: <code>scroll</code> for the two buttons that are an arrow and nothing else, and <code>window</code> for minimize, maximize, restore and the corner a keyboard resizes with. The × is <code>action.close</code>, which every other close button in the library already reads.</li><li><code>--neba-duration-window</code> — 240ms, longer than a control&#39;s 160ms because what is travelling is the whole sheet rather than a colour on one. Nothing but a window may use it.</li></ul><h3 id="documentation-6" tabindex="-1">Documentation <a class="header-anchor" href="#documentation-6" aria-label="Permalink to &quot;Documentation&quot;">​</a></h3><ul><li>Three component pages in both locales — <code>ScrollZone</code> under <strong>Layout</strong>, <code>FloatingBottomNavigation</code> under <strong>Inputs</strong>, <code>WindowPane</code> under <strong>Surfaces</strong> — with their props rows, fifteen demos, three cards in the component gallery, three new blocks on the sample screen, and their entries in <code>llms.txt</code> and the README.</li><li>The props table&#39;s name column wraps at spaces and never inside a word, which is the arrangement the type column already made with the pipes in its unions. One row of five names had been setting the width of a forty-row table; the five are their own rows now, so a reader looking for <code>closeLabel</code> finds a row called <code>closeLabel</code>.</li></ul><h2 id="_1-5-0-2026-08-12" tabindex="-1">1.5.0 (2026-08-12) <a class="header-anchor" href="#_1-5-0-2026-08-12" aria-label="Permalink to &quot;1.5.0 (2026-08-12)&quot;">​</a></h2><h3 id="added-9" tabindex="-1">Added <a class="header-anchor" href="#added-9" aria-label="Permalink to &quot;Added&quot;">​</a></h3><ul><li><strong>Charts.</strong> Five components on one engine: <code>Sparkline</code>, <code>LineChart</code>, <code>AreaChart</code>, <code>BarChart</code> and <code>PieChart</code>. They take their data the way <code>Table</code> does — <code>series</code> (or, for a pie, <code>data</code>) as a prop rather than markup — and they take it in the <em>same</em> shape as each other, so a dashboard tile can be switched from one chart to another without rewriting what feeds it. <code>Statistic</code> joins them in a new <strong>Charts</strong> group in the docs.</li><li><strong>A <code>null</code> is a gap, not a zero,</strong> on every one of them. A line breaks at it, an area breaks with it, a bar is not drawn and the table cell is empty; <code>connectNulls</code> bridges it for the case where the gap is an artefact of collection rather than a month in which nothing happened. A chart that renders missing data as zero reports an outage as a collapse.</li><li><strong>A series&#39; colour follows its place in the array it was passed, never its position among the visible ones</strong> — so filtering a legend cannot repaint the survivors, and a reader who learned that Europe is blue keeps that. <code>series.color</code> overrides the slot with a <code>NebaColor</code> family or any CSS colour, and a point&#39;s own <code>color</code> overrides that for one mark.</li><li><code>LineChart</code> takes <code>curve</code> (<code>linear</code>, a monotone <code>smooth</code> that will not dip below a value both its neighbours are above, or <code>step</code>), <code>markers</code>, <code>gradient</code>, <code>connectNulls</code>, <code>valueLabels</code> and <code>stacked</code>. Its value axis crops to the data, because a line encodes a <em>position</em> and cropping moves every point by the same amount.</li><li><code>AreaChart</code> takes the same three curves plus <code>stacked</code> — <code>true</code> for totals, <code>&#39;full&#39;</code> for a chart about the mix rather than the size, where the axis becomes a percentage and the tooltip and the table keep the caller&#39;s own number. Its axis keeps zero, because there the fill&#39;s thickness <em>is</em> the magnitude. Unstacked bands are a wash that fades downward so two of them overlapping stay readable; stacked bands are a flat tint parted by two pixels of the surface rather than by a stroke.</li><li><code>BarChart</code> takes <code>orientation</code> — <code>horizontal</code> is the right answer whenever the category names are words — plus <code>stacked</code> (<code>true</code> / <code>&#39;full&#39;</code>), <code>rounded</code>, <code>barSize</code> and <code>valueLabels</code>. Corners are cut off the <strong>data end</strong> only; a rounded foot makes the axis look scalloped. Bars grow from where zero is rather than from the bottom of the plot, so a negative bar starts on the same line as its neighbours.</li><li><code>PieChart</code> takes <code>shape</code> — <code>pie</code>, <code>donut</code> or a <code>semi</code> circle — plus <code>center</code> for what goes in the hole, and <code>valueLabels</code> for each slice&#39;s share, drawn only where the text fits with room on both sides and dropped rather than clipped where it does not.</li><li><code>Sparkline</code> is not a small chart: no axes, no grid, no legend, and no numbers, because every number it could label is one the sentence around it already has. <code>shape</code> is a line, an area or bars; <code>endDot</code>, <code>baseline</code>, <code>min</code> and <code>max</code> are the rest of it. It fills itself with its own range, which is what makes it legible at twenty pixels tall — and why two of them are only comparable when both are given the same <code>min</code> and <code>max</code>.</li><li><strong>A hidden table under every chart, always.</strong> The drawing is <code>aria-hidden</code> and the data is rendered as a visually clipped <code>&lt;table&gt;</code> captioned with <code>label</code>, so no value is reachable only by pointer. The plot itself is focusable: <code>←</code>/<code>→</code> step the crosshair between categories, <code>Home</code>/<code>End</code> jump to the ends, <code>Escape</code> clears it, and the tooltip is a live region. The legend is a list of <code>aria-pressed</code> buttons, so which series are drawn is stated rather than implied by colour.</li><li><strong>Eight new colour tokens, <code>--neba-chart-1</code> … <code>--neba-chart-8</code>, per theme</strong> — and the one place in the library where a colour is not a semantic role, because a series is an <em>entity</em> and nothing about it means success or danger. They were solved rather than picked: every step inside the OKLCH lightness band, chroma above the floor where a hue stops carrying identity, every adjacent pair at least ΔE 8 apart in OKLab under simulated protanopia and deuteranopia (measured at 13.6 light and 14.5 dark), and every slot above 4:1 on white and 5.3:1 on the dark sheet. Dark is re-solved against the dark surface rather than lightened from the light values. Slots are handed out in order and never cycled: a ninth series is an &quot;Other&quot; row or a second chart, not a ninth hue.</li><li><code>--neba-chart-grid</code>, <code>--neba-chart-axis</code>, <code>--neba-chart-baseline</code> and <code>--neba-chart-gap</code> — the chart&#39;s chrome, derived from <code>--neba-border</code> and <code>--neba-surface</code> so a chart&#39;s rules and a Card&#39;s dividers are one family of line. Gridlines are solid hairlines: a dashed grid says &quot;projection&quot; when all it is is a grid.</li><li><code>src/types.ts</code> gains the vocabulary the charts share — <code>NebaChartSeries</code>, <code>NebaChartDatum</code>, <code>NebaChartPoint</code>, <code>NebaChartCategory</code>, <code>NebaChartAxis</code>, <code>NebaChartLegend</code>, <code>NebaChartTooltip</code>, <code>NebaChartCurve</code>, <code>NebaChartValueLabels</code> — for the reason <code>NebaSize</code> is there: a <code>series</code> handed to a LineChart has to be the one a BarChart takes.</li><li><strong><code>Collapsible</code>.</strong> One section that folds, standing on its own — what an <code>Accordion</code> is a set of, for a &quot;Show more&quot; on a form or the detail under a row. <code>title</code>, <code>subtitle</code>, <code>startIcon</code> and <code>action</code> build the header the way <code>AccordionItem</code> does, with <code>action</code> outside the trigger so a header that both folds and holds a switch has two things to press rather than one nested in the other; <code>trigger</code> replaces the header entirely and the element passed becomes the trigger, handed the click, <code>aria-expanded</code> and the <code>aria-controls</code>. <code>keepMounted</code> and <code>hiddenUntilFound</code> decide whether a closed panel is in the DOM, the second of them so the browser&#39;s own page search can open it.</li><li><strong><code>Rating</code>.</strong> A score as a row of stars, and two components in one skin. Choosable, it is a <code>role=&quot;radiogroup&quot;</code> of real <code>&lt;input type=&quot;radio&quot;&gt;</code>s hidden under the half-stars they stand for — one tab stop for the row, arrow keys within it, <code>aria-checked</code> on the one that is taken and a value in a form submission, none of which a row of <code>&lt;button&gt;</code>s would have. <code>readOnly</code> renders no inputs at all and leaves one <code>role=&quot;img&quot;</code> carrying the score as a sentence, which is why it is the one read-only in the library that does not drain the saturation: it is not a control being held still, it is the number itself. <code>precision</code> bounds what can be <strong>picked</strong> and never what is <strong>drawn</strong>, so a <code>value</code> of <code>4.3</code> stays 4.3 — an average is not a choice, and rounding it would report a different number from the one it was handed. The fraction is a filled star laid over an empty one and clipped from the inline start, so nothing is scaled and a half star fills from the right under RTL on its own.</li><li><strong><code>BottomNavigation</code></strong> and <strong><code>BottomNavigationItem</code>.</strong> An app&#39;s main destinations, held against the bottom edge of the window — <code>position</code> defaults to <code>fixed</code> here, against the <code>static</code> everything else defaults to, because that is what the component is. It is a <code>&lt;nav&gt;</code> of ordinary buttons (or <code>&lt;a&gt;</code>s, given an <code>href</code>) with <code>aria-current=&quot;page&quot;</code> on the one you are on, and deliberately <strong>not</strong> a <code>role=&quot;tablist&quot;</code>: a tab list promises one tab stop for the set and arrow keys within it, and a bottom navigation changes the page rather than which panel of one is showing. <code>labels</code> draws every name, only the current one, or none — and a name it does not draw stays in the document, because a glyph on its own has no accessible name at all. <code>safeArea</code> holds the row clear of a phone&#39;s home indicator while the sheet still reaches the bottom of the screen.</li><li><strong><code>FloatingActionButton</code></strong> and <strong><code>FloatingAction</code>.</strong> The one action a screen is about, floating over it. The button is a <code>Button</code> unchanged — same variants, same elevation ladder, same pointer light — started one step up the size ladder at <code>lg</code>, because this is the control that has to be found and hit with a thumb without being looked at. <code>extended</code> writes <code>label</code> beside the glyph, which is also its accessible name, so the word drawn and the word read can never differ. <code>position</code> adds <code>absolute</code> to the three CSS values <code>NebaPosition</code> has, which is what pins one inside a card or a <code>Mockup</code> rather than to the window; <code>corner</code> and <code>offset</code> place it. Given <code>FloatingAction</code> children it becomes a dial: <code>aria-expanded</code> and <code>aria-controls</code> rather than a <code>role=&quot;menu&quot;</code> it would not honour, the actions as ordinary buttons next in the tab order, Escape closing it and handing the focus back, and a press outside putting it away.</li></ul><h3 id="documentation-7" tabindex="-1">Documentation <a class="header-anchor" href="#documentation-7" aria-label="Permalink to &quot;Documentation&quot;">​</a></h3><ul><li>Five component pages in both locales — <code>Sparkline</code>, <code>LineChart</code>, <code>AreaChart</code>, <code>BarChart</code>, <code>PieChart</code> — with the data model written out once on the LineChart page and cross-referenced from the rest, their props rows (including tables for <code>NebaChartAxis</code>, <code>NebaChartLegend</code> and <code>NebaChartTooltip</code>), sixteen demos, a <strong>Charts</strong> group in the component gallery, three charts and a row of sparklines on the sample screen, and their entries in <code>llms.txt</code>.</li><li><code>Statistic</code> moved from <strong>Display</strong> to <strong>Charts</strong>, in both locales and in every index that names it.</li><li>Four component pages in both locales — <code>Collapsible</code> under <strong>Surfaces</strong>, <code>Rating</code>, <code>BottomNavigation</code> and <code>FloatingActionButton</code> under <strong>Inputs</strong> — with their props rows, twenty-one demos, four cards in the component gallery, two new blocks on the sample screen, and their entries in <code>llms.txt</code> and the README.</li><li>A <code>rating</code> namespace in <code>src/internal/i18n.ts</code>, translated into all eighteen locales: the group&#39;s name, the sentence one star is read out as, and what an unrated control says. The sentence carries both numbers rather than counting stars, because a count of stars is a plural in most languages and a fraction in none of them.</li></ul><h2 id="_1-4-0-2026-08-06" tabindex="-1">1.4.0 (2026-08-06) <a class="header-anchor" href="#_1-4-0-2026-08-06" aria-label="Permalink to &quot;1.4.0 (2026-08-06)&quot;">​</a></h2><h3 id="added-10" tabindex="-1">Added <a class="header-anchor" href="#added-10" aria-label="Permalink to &quot;Added&quot;">​</a></h3><ul><li><code>Empty</code> — what stands where content would have been, in four slots: <code>icon</code>, <code>title</code>, <code>children</code> and <code>action</code>. It is the other half of <code>Skeleton</code>: one is the shape of something on its way, the other is the shape of something that is not coming, and a list showing neither has a blank rectangle where its answer should be.</li><li><strong>The headline is defaulted rather than required</strong>, because the version that says nothing useful is the version that gets shipped. <code>title</code> falls back to the <code>locale</code>&#39;s way of saying that there is nothing here — a new <code>empty</code> namespace in the message table, translated into all eighteen languages the library already speaks — and <code>title={false}</code> drops it for a state that is a glyph and a sentence. <code>icon</code> defaults to an empty tray and takes any node, so an illustration or another set&#39;s glyph goes in without a wrapper; <code>action</code> holds the way out, and several of them wrap together in one row.</li><li><code>variant</code> defaults to <code>text</code> on this component and on no other: an empty state is nearly always already inside a Card, a Table or a panel, and a second rectangle inside the first is one rectangle too many. <code>outline</code> and <code>solid</code> are the undyed container sheet, so <code>color</code> reaches the hairline and the ring and stops — an empty state arriving in the accent colour is making a claim about content that does not exist. The root is a <code>role=&quot;status&quot;</code> live region, which is what lets a list that empties under the reader say so; <code>role={undefined}</code> turns that off for a state that is part of the page on arrival.</li><li><code>Mockup</code> — a device with a screen you can put anything on. <code>device</code> is the one prop with no default and picks the machine: a <code>mobile</code>, a <code>tablet</code>, or a <code>desktop</code> that is either a <code>monitor</code> on a stand or a <code>laptop</code> on a base. <code>os</code> draws that system&#39;s own chrome — a menu bar and a floating dock, a centred taskbar, a top bar with a dock down the leading edge, a status bar with a home indicator or three navigation glyphs — and <code>systemUi={false}</code> takes all of it away. <code>bezel</code> runs from <code>none</code> (no hardware at all, just the glass) through <code>thin</code> and <code>standard</code> to <code>thick</code>, which is an older device with a forehead and a chin rather than a wider frame; <code>finish</code> is <code>graphite</code>, <code>silver</code> or <code>white</code>; <code>notch</code> is a <code>dynamic-island</code>, a <code>notch</code>, a <code>punch-hole</code> or <code>none</code>, defaulting to whatever the device would really have. <code>orientation</code> turns a handheld, and the screen, the bezel and the cut-out turn with it.</li><li><strong>The screen is a viewport at the device&#39;s real resolution, not a picture scaled down.</strong> <code>size</code> is a five-step ladder of genuine resolutions per device — a phone from 320 to 430 CSS pixels wide, a desktop from 1024 to 1920 — and <code>resolution</code> takes a <code>{ width, height }</code> pair for anything else. The whole device is then scaled once to whatever <code>width</code>/<code>height</code> come to on the page, so the same component laid out inside a phone and inside a desktop wraps differently in each. The screen is also a container named <code>neba-screen</code>, so content can answer to the device with a container query rather than to the window.</li><li><code>scroll</code> lets content taller than the screen scroll instead of being clipped; <code>wallpaper</code> takes any CSS <code>background</code> for what sits behind it; <code>time</code> is the clock, and the only text the chrome draws — every menu title, dock icon and tray glyph is an abstract shape, so there is nothing to translate and no other party&#39;s marks in the library. Every part of the device is <code>aria-hidden</code>, which leaves a screen reader with <code>children</code> and nothing else.</li><li><code>DataTable</code> — a table for a lot of rows. <code>Table</code> draws a grid; this one is a place to work, and it takes the same two props to get there: <code>headers</code> and <code>items</code>. It is compact by default (<code>sm</code>, <code>compact</code>) with its own row ladder a step below the rest of the library, <code>striped</code> takes <code>true</code>, <code>&#39;odd&#39;</code> or <code>&#39;even&#39;</code>, and the parity is counted over the whole set so it does not change as the rows are scrolled or sorted.</li><li><strong>Only the rows on screen are in the DOM, and <code>height</code> is what turns that on.</strong> Every row is <code>rowHeight</code> tall — defaulted off the <code>size</code>/<code>density</code> ladder — so the offset of a row is its index times a constant and nothing has to be measured on a scroll frame; the rows that are left out stand up as two spacer rows, which is what keeps the scrollbar honest. Without a <code>height</code> or a <code>maxHeight</code> there is nothing to measure against and every row is rendered, whatever <code>virtual</code> says; <code>virtual={false}</code> asks for that on purpose, for a table small enough that find-in-page matters more than the DOM count. Cells truncate rather than wrap, which is the price of the arithmetic.</li><li><strong>Selecting is the file manager&#39;s, not the form&#39;s.</strong> A click chooses a row and drops the rest, <kbd>Ctrl</kbd>/<kbd>⌘</kbd> adds one, <kbd>Shift</kbd> takes the run between, a drag takes the run under the pointer and scrolls when it reaches an edge, and the arrow keys do all three with the same modifiers — plus <kbd>Home</kbd>, <kbd>End</kbd>, <kbd>PageUp</kbd>, <kbd>PageDown</kbd>, <kbd>Space</kbd>, <kbd>Ctrl</kbd>+<kbd>A</kbd>, <kbd>Esc</kbd> and <kbd>Enter</kbd>. <code>selectionMode</code> is <code>none</code>, <code>single</code> or <code>multiple</code>; <code>checkboxes</code> adds a tick column and is deliberately not the default, because a column of checkboxes says the task is choosing and on most tables it is not. With a selection mode the table is a <code>grid</code> with one tab stop and <code>aria-activedescendant</code>, since a virtual row cannot hold the focus.</li><li><code>sortable</code> makes the headings pressable and they cycle ascending → descending → <strong>unsorted</strong>, because the order the rows arrived in is a state nothing else can get back to. <code>sortMode=&quot;multiple&quot;</code> lets a Shift-click add a second key rather than replace the first. <code>resizable</code> puts a handle on each boundary; the first drag freezes every column at the width the browser had given it, so pulling one moves one, and a double-click restores it. Adjacent columns sharing a <code>group</code> string merge under one heading in a second header row.</li><li><code>paging=&quot;pages&quot;</code> swaps the scroll for a footer holding the range, the number of chosen rows, a page-size <code>Select</code> and a <code>Pagination</code>; <code>footer</code> shows that bar on its own, so a scrolling table can have the count without the pages. <code>search</code> matches every column that has not set <code>searchable: false</code>, case- and accent-insensitively, and <code>searchable</code> draws the field with <code>toolbar</code> filling the rest of its bar. <code>manual</code> hands any of <code>&#39;sort&#39;</code>, <code>&#39;filter&#39;</code> and <code>&#39;pages&#39;</code> back to the caller — with <code>rowCount</code> for the total — so a table whose rows come from a server is the same component rather than a second one.</li><li>The i18n table gains a <code>table</code> namespace — the search field&#39;s placeholder, the two ticks&#39; labels, the page-size label, and the footer&#39;s count and selection sentences — in all eighteen languages. The two that carry numbers are whole templates with <code>{start}</code>, <code>{end}</code>, <code>{total}</code> and <code>{count}</code> in them rather than fragments, because what differs between languages is the order the numbers appear in.</li></ul><h3 id="fixed-8" tabindex="-1">Fixed <a class="header-anchor" href="#fixed-8" aria-label="Permalink to &quot;Fixed&quot;">​</a></h3><ul><li><strong>Forcing a theme on a nested element now works in both directions.</strong> <code>.dark</code> / <code>[data-theme=&#39;dark&#39;]</code> on an element that is not the document root was already documented, but the light values were declared on <code>:root</code> alone, so <code>.light</code> / <code>[data-theme=&#39;light&#39;]</code> inside a dark page had nothing to switch back to. The light block now carries all three selectors.</li><li><code>--neba-plate-solid</code>, <code>--neba-plate-glass</code> and the <code>--neba-shadow-1</code> … <code>--neba-shadow-4</code> ladder moved into the derived block. Each is a base token spread into a shadow list, and declared only on <code>:root</code> they froze to the light hairline and the light ambient inside any theme root that was not the document root.</li></ul><h3 id="documentation-8" tabindex="-1">Documentation <a class="header-anchor" href="#documentation-8" aria-label="Permalink to &quot;Documentation&quot;">​</a></h3><ul><li>Every live preview carries a theme switch in its top corner, so a component can be read in the theme the page is not in without taking the whole site with it. Untouched previews still follow the site switch, and a preview flipped back to the page&#39;s own theme rejoins it.</li><li>An <code>Empty</code> page in both locales with eight examples, its props rows, its demos, a card in the component gallery, a place on the sample screen beside the placeholder it is the other half of, and its entry in <code>llms.txt</code>.</li><li>A <code>Mockup</code> page in both locales with eleven examples, its props rows, its demos, a card in the component gallery, a place on the sample screen and its entry in <code>llms.txt</code>.</li><li>A <code>DataTable</code> page in both locales with seven examples, two props tables, its demos, a card in the component gallery, twelve thousand rows on the sample screen and its entry in <code>llms.txt</code>.</li><li><strong>The nav bar and the sidebar no longer hang off the edges between 1440px and 1600px.</strong> The site raises <code>--vp-layout-max-width</code> to 1600px, and the default theme centres the shell on the article by taking half of what the page has left over — an expression it writes out five times without a floor, because at its own 1440px it cannot come out below zero. At ours it can: the social icons sat 8px outside the viewport and the sidebar climbed over the article&#39;s left margin. All five now read one floored <code>--neba-gutter</code>, which reduces every one of them to the gutter plus nothing, the page margin, or the sidebar&#39;s width.</li><li><code>npm run docs:dev</code> starts in about four seconds instead of fourteen. All three <code>docs:*</code> scripts ran <code>npm run build</code> first — ten seconds of <code>format:fix</code>, <code>tsc</code> and terser that nothing then looked at, since the docs resolve <code>neba</code> to <code>src/index.ts</code> and import <code>src/styles.css</code> directly. Prettier now runs where it always did as part of <code>npm run build</code>, and CI still checks it.</li></ul><h2 id="_1-3-0-2026-08-05" tabindex="-1">1.3.0 (2026-08-05) <a class="header-anchor" href="#_1-3-0-2026-08-05" aria-label="Permalink to &quot;1.3.0 (2026-08-05)&quot;">​</a></h2><h3 id="added-11" tabindex="-1">Added <a class="header-anchor" href="#added-11" aria-label="Permalink to &quot;Added&quot;">​</a></h3><ul><li><strong>A sixth group, Transitions: eleven <code>Animate*</code> wrappers that make anything move.</strong> <code>AnimateFade</code>, <code>AnimateGrow</code>, <code>AnimateZoom</code>, <code>AnimateSlide</code>, <code>AnimateRotate</code> and <code>AnimateBlink</code> are the six named effects; <code>AnimateAppear</code>, <code>AnimateTyping</code>, <code>AnimateLighting</code>, <code>AnimateMarquee</code> and <code>AnimateHeadline</code> are the five that have to understand what their children are. All eleven take the same settings — <code>duration</code> and <code>delay</code> in milliseconds, <code>easing</code>, <code>repeat</code> (a count or <code>&#39;infinite&#39;</code>), <code>alternate</code>, <code>paused</code>, and <code>trigger</code>: <code>mount</code>, <code>visible</code> (with <code>once</code> and <code>threshold</code>), <code>hover</code>, or <code>manual</code> driven by <code>play</code>. Every one of them is switched off entirely by a <code>prefers-reduced-motion</code> preference, so none of them is ever the only thing carrying a message.</li><li><strong>A <code>transition</code> prop on the components that display something</strong> — <code>Box</code>, <code>Card</code>, <code>Statistic</code>, <code>Alert</code>, <code>Chip</code>, <code>Avatar</code>, <code>Icon</code>, <code>Typography</code> and <code>Blockquote</code>. <code>transition=&quot;fade&quot;</code> is an entrance run once on mount, and the object form takes the details: <code>{ type: &#39;slide&#39;, from: &#39;left&#39;, duration: 500 }</code>. It is offered on no component that is pressed, because a control that moves under the pointer aiming at it is the one thing the design language rules out. Anything past a mount — a replay, a scroll trigger, a hover — is an <code>Animate*</code> component, and any component can be wrapped in one.</li><li><code>ColorPicker</code> — a colour chosen by eye: a saturation square, a hue rail, an optional opacity rail, a field for typing a value in, and a grid of swatches. <code>format</code> decides whether the value comes back as hex, <code>rgb()</code> or <code>hsl()</code>; <code>alpha</code> adds the fourth channel; <code>swatches</code> replaces the built-in set with the colours a product actually uses; <code>inline</code> draws the panel into the page instead of into a popup. It reads hex in all four lengths, <code>rgb()</code>/<code>rgba()</code> and <code>hsl()</code>/<code>hsla()</code> in both syntaxes, and it adds no dependency — the conversions are a hundred lines of arithmetic in <code>internal/color.ts</code>.</li><li>The i18n table gains a <code>color</code> namespace, so the picker&#39;s square, rails, field and swatch grid — none of which have any text on them — are named in all nineteen languages. <code>locale</code> picks the language and <code>labels</code> overrides any one of them.</li></ul><h3 id="documentation-9" tabindex="-1">Documentation <a class="header-anchor" href="#documentation-9" aria-label="Permalink to &quot;Documentation&quot;">​</a></h3><ul><li>Pages for <code>ColorPicker</code> and the eleven <code>Animate*</code> components in both locales, their props rows, their demos, cards in the component gallery, a place on the sample screen, and their entries in <code>llms.txt</code>.</li><li><strong>Prop conventions gains a Motion section</strong>, which is where the shared animation vocabulary and the rule about which components take <code>transition</code> are written down.</li></ul><h2 id="_1-2-0-2026-08-01" tabindex="-1">1.2.0 (2026-08-01) <a class="header-anchor" href="#_1-2-0-2026-08-01" aria-label="Permalink to &quot;1.2.0 (2026-08-01)&quot;">​</a></h2><h3 id="added-12" tabindex="-1">Added <a class="header-anchor" href="#added-12" aria-label="Permalink to &quot;Added&quot;">​</a></h3><ul><li>Eight components, bringing the library to fifty-seven: <code>Avatar</code>, <code>Breadcrumb</code> (with <code>BreadcrumbItem</code>), <code>ChatBubble</code>, <code>OtpField</code>, <code>Panes</code> (with <code>Pane</code>), <code>Spoiler</code>, <code>TextLink</code> and <code>TreeView</code> (with <code>TreeItem</code>). <code>Panes</code> opens a fifth group, <strong>Layout</strong>, alongside <code>Container</code> and <code>Grid</code>.</li><li><strong>The library speaks nineteen languages on its own behalf.</strong> Almost nothing in Neba writes text a reader sees — a Button says whatever it was handed — but a few components have to invent a string because there is nowhere else for it to come from: the sentence read out after a link that opens a new tab, the label on the button that uncovers a <code>Spoiler</code>, the word under a chat message that says it was read. Those are now one table rather than eight English defaults. <code>TextLink</code>, <code>Spoiler</code> and <code>ChatBubble</code> take a <code>locale</code>, and every string still has an override prop of its own, so an unsupported language is never a dead end. A tag resolves by script, then by region, then by language (<code>zh-Hant-TW</code> → <code>zh-hant</code>, <code>pt-BR</code> → <code>pt</code>), and a translation that fills in part of the table falls back to English one namespace at a time rather than leaving blanks.</li><li><code>Avatar</code> — an image that falls back to initials, to a glyph, or to a silhouette, so the slot is never an empty box. <code>shape</code> is <code>circle</code> or <code>square</code>, <code>delay</code> holds the fallback back long enough for a cached image not to flash it, and <code>onLoadingStatusChange</code> reports <code>idle</code> / <code>loading</code> / <code>loaded</code> / <code>error</code>.</li><li><code>Breadcrumb</code> — a trail that collapses in the middle when it is too long: <code>maxItems</code>, <code>itemsBeforeCollapse</code> and <code>itemsAfterCollapse</code> decide where, and <code>expandable</code> makes the ellipsis a button that opens the rest in place. <code>separator</code> takes <code>chevron</code>, <code>arrow</code>, <code>slash</code>, <code>dot</code> or a node of your own.</li><li><code>ChatBubble</code> — one message in a conversation. The avatar, the name, the time, the delivery mark, the media and the link card are each drawn only when given something, so the same component is a bare bubble or a full row. <code>status</code> runs <code>sending</code> → <code>sent</code> → <code>delivered</code> → <code>read</code>, plus <code>failed</code>; <code>typing</code> draws three dots that change colour rather than bounce.</li><li><code>OtpField</code> — a one-time code as one field per character. <code>length</code>, <code>charset</code> (<code>numeric</code>, <code>alpha</code>, <code>alphanumeric</code>, <code>any</code>), <code>mask</code>, and <code>groupSize</code> with a <code>separator</code> for codes written in blocks. <code>onComplete</code> fires when the last box is filled, <code>autoSubmit</code> submits the form it is in, and <code>onValueInvalid</code> reports what was rejected.</li><li><code>Panes</code> — regions with a draggable bar between each pair, horizontal or vertical. Each <code>Pane</code> takes <code>defaultSize</code>, <code>minSize</code> and <code>maxSize</code> as a number of pixels or any CSS length; <code>resizable</code> turns the bars off, and <code>onResize</code> / <code>onResizeEnd</code> report the split.</li><li><code>Spoiler</code> — content covered by a blur rather than removed, so a reader can see that something is there without reading it by accident. Controlled with <code>revealed</code> / <code>defaultRevealed</code> / <code>onRevealedChange</code>; <code>reversible</code> allows covering it again, <code>maxHeight</code> clamps it to a fade-out instead, and <code>blur</code> is the radius.</li><li><code>TextLink</code> — a link with no surface of its own. <code>underline</code> is <code>always</code>, <code>hover</code> or <code>none</code>, <code>newTab</code> adds the mark and the sentence a screen reader needs, <code>icon</code> overrides or drops that mark, and <code>render</code> swaps in a router&#39;s own link component.</li><li><code>TreeView</code> — a tree with roving focus, controlled <code>expanded</code> and <code>selected</code>, and <code>multiple</code> for checkbox-style selection. <code>lines</code> draws the guides as <code>none</code>, <code>simple</code> or <code>folder</code> — the rail runs past an expanded subtree to the next sibling and stops halfway down the last child, the way a file manager draws it, and mirrors under RTL without a second rule.</li></ul><h3 id="changed-8" tabindex="-1">Changed <a class="header-anchor" href="#changed-8" aria-label="Permalink to &quot;Changed&quot;">​</a></h3><ul><li><strong>The Examples section is four pages, not one.</strong> <code>/examples/</code> no longer resolves to a page — the overview moved to <a href="https://neba.cdget.com/examples/overview" target="_blank" rel="noreferrer"><code>/examples/overview</code></a> and three concept screens joined it. Any link you have to <code>/examples/</code> needs updating.</li><li>The stylesheet carries a <code>.neba-link</code> rule, written as <code>.neba-link.neba-link</code> so that a host&#39;s <code>.prose a</code> or <code>.vp-doc a</code> cannot outrank the link&#39;s underline thickness, offset and colour. Like <code>neba-portal</code>, the class is a hook you can exempt from your own typography rules.</li></ul><h3 id="fixed-9" tabindex="-1">Fixed <a class="header-anchor" href="#fixed-9" aria-label="Permalink to &quot;Fixed&quot;">​</a></h3><ul><li>The date formatter cache joined a locale and its options with a space, which is a character both halves of the key can contain. Two different requests could land on one entry and the second would be formatted with the first&#39;s options. The separator is now one that cannot appear in either.</li></ul><h3 id="documentation-10" tabindex="-1">Documentation <a class="header-anchor" href="#documentation-10" aria-label="Permalink to &quot;Documentation&quot;">​</a></h3><ul><li><strong>Three concept screens</strong>, each a whole fictional product page built only out of the library: a <a href="https://neba.cdget.com/examples/concept-landing" target="_blank" rel="noreferrer">landing page</a>, an <a href="https://neba.cdget.com/examples/concept-dashboard" target="_blank" rel="noreferrer">admin dashboard</a> and a <a href="https://neba.cdget.com/examples/concept-signup" target="_blank" rel="noreferrer">sign-up flow</a>.</li><li>The home page now says what the library is rather than how it is drawn — what ships, what is tested, and what an install actually gets you.</li><li><strong>Every page carries its own description.</strong> A page&#39;s lede is read out of the source and becomes its <code>&lt;meta name=&quot;description&quot;&gt;</code>, so pages no longer share one sentence between them. Canonical URLs, an <code>hreflang</code> pair per locale with an <code>x-default</code>, Open Graph and Twitter cards, JSON-LD on the home page, and a <code>robots.txt</code> naming the sitemap are generated in the build.</li><li><strong>A preview mounts when it is scrolled into view</strong>, not when the page loads. A component page holds a dozen of them, and mounting all at once put the one being read behind chunks for previews far below the fold. The empty box reserves its height before and after, so nothing jumps.</li><li>New icons and social image at 128 and 256 px, an <code>apple-touch-icon</code>, and a <code>theme-color</code>.</li><li>Pages for the eight new components in both locales, their props rows, their demos, and their entries in <code>llms.txt</code>.</li></ul><h2 id="_1-1-0-2026-07-29" tabindex="-1">1.1.0 (2026-07-29) <a class="header-anchor" href="#_1-1-0-2026-07-29" aria-label="Permalink to &quot;1.1.0 (2026-07-29)&quot;">​</a></h2><h3 id="setup" tabindex="-1">Setup <a class="header-anchor" href="#setup" aria-label="Permalink to &quot;Setup&quot;">​</a></h3><ul><li><p><strong><code>neba/styles.css</code> now ships compiled.</strong> It carries the design tokens, the real rules for every utility class the components use, and a small reset — so the whole setup is <code>npm install neba</code> and one <code>@import</code>. Tailwind is no longer a requirement on your side; it builds this package and stays a devDependency of it. About 13 kB gzipped.</p></li><li><p><strong>New export: <code>neba/tailwind.css</code>.</strong> The token sheet, for a project that already runs Tailwind v4 — it registers the package as a Tailwind source, so your own build generates the components&#39; utilities in the same pass as your app&#39;s and a <code>className</code> you pass to a component sorts correctly against the component&#39;s own classes. If you were on the previous two-line setup, this is the line to switch to:</p><div class="language-css vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">css</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">@import</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;tailwindcss&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">@import</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;neba/tailwind.css&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">/* was: neba/styles.css */</span></span></code></pre></div></li><li><p><strong>The bundled reset</strong> is Tailwind&#39;s Preflight cut down to what the components actually need — <code>box-sizing</code>, font inheritance on form controls, list markers off. It does not touch the typography of your paragraphs, headings or links, and every rule is wrapped in <code>:where()</code>, so a single type selector of your own beats it whatever the import order. It is in <code>neba/styles.css</code> only; the Tailwind path has Preflight already.</p></li><li><p><strong><code>react</code> and <code>react-dom</code> are now declared as <code>peerDependencies</code></strong> (<code>^18.0.0 || ^19.0.0</code>) rather than only as devDependencies, so the requirement is stated where a package manager can check it. <code>@types/react</code> is an optional peer for TypeScript consumers.</p></li></ul><h3 id="added-13" tabindex="-1">Added <a class="header-anchor" href="#added-13" aria-label="Permalink to &quot;Added&quot;">​</a></h3><ul><li>Eighteen components: <code>Blockquote</code>, <code>Carousel</code>, <code>Container</code>, <code>DatePicker</code>, <code>DateRangePicker</code>, <code>DateTimePicker</code>, <code>Grid</code>, <code>GridContainer</code>, <code>Highlight</code>, <code>Icon</code>, <code>IconButton</code>, <code>Pill</code>, <code>SegmentedButton</code> (with <code>Segment</code>), <code>Shortcut</code>, <code>Statistic</code>, <code>TimePicker</code>, <code>Timeline</code> (with <code>TimelineItem</code>) and <code>Toolbar</code>.</li><li>Shared types for the new layout and date work: <code>NebaPosition</code>, <code>NebaBreakpoint</code>, <code>NebaResponsive&lt;T&gt;</code>, <code>NebaWeekday</code>, <code>NebaJustifyContent</code>, <code>NebaAlignItems</code> and <code>NebaAlignSelf</code>.</li><li><code>Divider</code> takes a <code>thickness</code>, as a number of pixels or any CSS length.</li><li><code>Pill</code> takes <code>elevation</code>, <code>startIcon</code> / <code>endIcon</code>, <code>title</code> / <code>description</code> / <code>details</code>, <code>expanded</code> and <code>position</code>.</li><li><code>llms.txt</code>, for agents reading the documentation site.</li></ul><h3 id="changed-9" tabindex="-1">Changed <a class="header-anchor" href="#changed-9" aria-label="Permalink to &quot;Changed&quot;">​</a></h3><ul><li><code>Select</code>, <code>DatePicker</code> and <code>DateRangePicker</code> hold their trigger at the width of the longest thing it could say. Choosing a shorter option no longer shrinks the field out from under the pointer that just used it.</li><li><code>Checkbox</code> and <code>RadioGroup</code> drop the white plate highlight. A 1px hairline is light on a cut edge at 32px and a bevel at 18px; the acrylic surface stays, only the highlight goes.</li><li>A highlighted row in a <code>Select</code> or <code>Combobox</code> popup now takes the accent text colour as well as the soft background.</li></ul><h3 id="fixed-10" tabindex="-1">Fixed <a class="header-anchor" href="#fixed-10" aria-label="Permalink to &quot;Fixed&quot;">​</a></h3><ul><li><code>Select</code> and <code>Combobox</code> popups render through a portal, outside the element their <code>--n-*</code> slots were declared on, which left every <code>var()</code> in them with nothing to resolve to: a <code>currentColor</code> hairline instead of the family&#39;s, a transparent surface, and a highlighted row that did not light at all. The slots are now set on the popup itself.</li><li>A field at the top or bottom of a scrolling <code>Dialog</code> body had its focus ring sliced off by the scroll container. The body without <code>dividers</code> now reserves the 4px the ring is drawn in and gives the space straight back, so nothing on the sheet moves.</li><li><code>Pagination</code> keyed its buttons by page number, so recentring the window moved the DOM nodes and the button under the pointer became a different element — its hover bloom faded out while a fresh neighbour&#39;s faded in from a centre it had no pointer position for. The row is keyed by slot now.</li><li><code>Combobox</code>&#39;s input stretches to the height of the row it sits on rather than to a fixed <code>1lh</code>, which put the placeholder a pixel or two above the chips beside it.</li></ul><h3 id="documentation-11" tabindex="-1">Documentation <a class="header-anchor" href="#documentation-11" aria-label="Permalink to &quot;Documentation&quot;">​</a></h3><ul><li>The sidebar is four sections — Guide, Components, Design and Discover more — with the component groups kept as headings inside Components, and the index page as an entry of its own rather than as the heading&#39;s link.</li><li>The design pages moved from <code>guide/</code> to <code>design/</code>.</li><li>This changelog is published at <a href="https://neba.cdget.com/changelog" target="_blank" rel="noreferrer">/changelog</a> in both locales.</li></ul><h2 id="_1-0-0-2026-07-26" tabindex="-1">1.0.0 (2026-07-26) <a class="header-anchor" href="#_1-0-0-2026-07-26" aria-label="Permalink to &quot;1.0.0 (2026-07-26)&quot;">​</a></h2><ul><li>First release</li></ul><h2 id="_0-0-1-2025-12-08-alpha" tabindex="-1">0.0.1 (2025-12-08 / Alpha) <a class="header-anchor" href="#_0-0-1-2025-12-08-alpha" aria-label="Permalink to &quot;0.0.1 (2025-12-08 / Alpha)&quot;">​</a></h2><ul><li>Alpha release (Not tested. Do not use production.)</li></ul>`,176)])])}const u=o(n,[["render",s]]);export{g as __pageData,u as default};
