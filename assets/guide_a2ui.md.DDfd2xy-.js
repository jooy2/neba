import{_ as a,o as t,c as s,a4 as i}from"./chunks/framework.BoUsC9QI.js";const g=JSON.parse('{"title":"A2UI catalog","description":"The package ships an A2UI catalog and a renderer to go with it. The catalog tells an agent which Neba components it may describe; the adapter turns what the…","frontmatter":{"title":"A2UI catalog","order":4},"headers":[],"relativePath":"guide/a2ui.md","filePath":"en/guide/a2ui.md","lastUpdated":1789784681000}'),o={name:"guide/a2ui.md"};function n(h,e,r,d,c,l){return t(),s("div",null,[...e[0]||(e[0]=[i(`<h1 id="a2ui-catalog" tabindex="-1">A2UI catalog <a class="header-anchor" href="#a2ui-catalog" aria-label="Permalink to &quot;A2UI catalog&quot;">​</a></h1><p class="neba-lede">The package ships an A2UI catalog and a renderer to go with it. The catalog tells an agent which Neba components it may describe; the adapter turns what the agent writes back into those components. An agent writes a surface as JSON and it comes out as your design system.</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// What the agent is handed.</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> catalog </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;neba/a2ui/catalog.json&#39;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> with</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { type: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;json&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> };</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// What draws what it writes back.</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { createNebaCatalog } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;neba/a2ui&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span></code></pre></div><p>The catalog is also served from the URL it names itself by:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>https://neba.cdget.com/a2ui/catalog.json</span></span></code></pre></div><h2 id="what-a2ui-is" tabindex="-1">What A2UI is <a class="header-anchor" href="#what-a2ui-is" aria-label="Permalink to &quot;What A2UI is&quot;">​</a></h2><p><a href="https://a2ui.org" target="_blank" rel="noreferrer">A2UI</a> is a protocol for an agent describing an interface as JSON that the <strong>host</strong> renders with the host&#39;s own design system. The agent never writes markup and never writes CSS: it writes a flat map of components with IDs, and the renderer turns each one into whatever that design system calls it.</p><p>A catalog is how the two sides agree on the vocabulary. It is a single JSON Schema file with a <code>catalogId</code>, a set of <code>components</code>, a set of <code>functions</code> the renderer will execute, and <code>instructions</code> written for the model rather than for a reader. There is no registry: the <code>catalogId</code> is an identifier that only looks like a URL, and the spec says it does not have to point at anything. This one does anyway.</p><h2 id="two-halves-and-you-can-take-one" tabindex="-1">Two halves, and you can take one <a class="header-anchor" href="#two-halves-and-you-can-take-one" aria-label="Permalink to &quot;Two halves, and you can take one&quot;">​</a></h2><p><strong><code>neba/a2ui/catalog.json</code> costs nothing.</strong> It is JSON, so it never enters a bundle, and whether you render it with this library&#39;s adapter or with a renderer of your own is your decision. Take it alone and the mapping is yours.</p><p><strong><code>neba/a2ui</code> is the adapter</strong>, and it needs three packages this library does not install for you:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> install</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> @a2ui/react</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> @a2ui/web_core</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> zod</span></span></code></pre></div><p>They are <strong>optional peer dependencies</strong>, which is safe here because <code>neba/a2ui</code> is not re-exported from <code>neba</code>: a bundler walking the package never reaches it, so a project that imports <code>Button</code> and has never heard of A2UI resolves nothing new. Only the import below pulls them in.</p><div class="language-tsx vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">tsx</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { MessageProcessor } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@a2ui/web_core/v0_9&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { A2uiSurface } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@a2ui/react/v0_9&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { createNebaCatalog } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;neba/a2ui&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> processor</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> new</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> MessageProcessor</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">([</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">createNebaCatalog</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()]);</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">processor.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">processMessages</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(whateverTheAgentSent);</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// …then render each surface the agent created.</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">A2uiSurface</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> surface</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{surface} /&gt;;</span></span></code></pre></div><p>That is the whole of it. The adapter registers the eighteen components and the fourteen functions, and the <code>catalogId</code> a surface names is the one in the file the agent was given — so the two halves cannot drift apart.</p><h3 id="what-the-adapter-does-and-does-not" tabindex="-1">What the adapter does, and does not <a class="header-anchor" href="#what-the-adapter-does-and-does-not" aria-label="Permalink to &quot;What the adapter does, and does not&quot;">​</a></h3><p>The renderer does the work: a prop the agent wrote as a literal, as a path into the data model or as a function call arrives at the component already resolved; a field&#39;s edits are written back to the data model; an action arrives as a function with its context gathered; a <code>checks</code> rule is evaluated as the data changes. What this package adds is the rename — <code>Alert</code>&#39;s <code>child</code> is its children, <code>Select</code>&#39;s <code>options</code> are its <code>items</code> — and putting a failed check&#39;s message where the field draws its own error.</p><p>The Zod schemas the renderer binds against are <strong>derived from <code>catalog.json</code> at load time</strong> rather than written a second time. That is the point: a hand-written mirror is a mirror that drifts, and the copy that drifts is the one the agent was never told about — a model writing exactly what the JSON allows, and a renderer rejecting it.</p><h2 id="the-eighteen" tabindex="-1">The eighteen <a class="header-anchor" href="#the-eighteen" aria-label="Permalink to &quot;The eighteen&quot;">​</a></h2><p>The standard Basic Catalog is eighteen components and calls itself &quot;intentionally sparse&quot;. This one is eighteen too, and the number is the decision rather than an accident: a model chooses badly from a list of a hundred and thirty-eight, and the components left out are the ones whose useful props are functions, React nodes or render props — things a JSON schema cannot describe at all.</p><table tabindex="0"><thead><tr><th>Group</th><th>Components</th></tr></thead><tbody><tr><td>Layout and surface</td><td><code>Flex</code> <code>Card</code> <code>Divider</code> <code>Alert</code></td></tr><tr><td>Display</td><td><code>Typography</code> <code>Image</code> <code>Chip</code> <code>Avatar</code> <code>Statistic</code> <code>DataList</code></td></tr><tr><td>Inputs</td><td><code>Button</code> <code>TextField</code> <code>NumberField</code> <code>Checkbox</code> <code>Switch</code> <code>RadioGroup</code> <code>Select</code> <code>Slider</code></td></tr></tbody></table><p><code>Table</code> is the most obvious absence and the clearest example of the rule: its columns are objects carrying render functions, so a catalog entry for it would be a schema the adapter had to invent rather than one the component already has.</p><p>The names are Neba&#39;s own rather than the Basic Catalog&#39;s, which is what makes the mapping on your side a lookup instead of a translation.</p><h2 id="what-the-model-is-told" tabindex="-1">What the model is told <a class="header-anchor" href="#what-the-model-is-told" aria-label="Permalink to &quot;What the model is told&quot;">​</a></h2><p><code>instructions</code> is the field the specification sets aside for design guidance, written for the model. This catalog&#39;s says the four things a screen gets wrong without them:</p><ul><li><strong>Nothing has a margin.</strong> Two components sit beside each other only inside a <code>Flex</code>, and the gutter is that <code>Flex</code>&#39;s <code>spacing</code>.</li><li><strong>Colour is semantic.</strong> Six families, each meaning something; <code>danger</code> is for what failed or what destroys, and none of them is a way of making a screen more varied. One <code>solid</code> control in a view.</li><li><strong>One ladder.</strong> <code>size</code> is the same five steps everywhere, so a row of controls lines up.</li><li><strong>Fields carry their own words.</strong> A <code>TextField</code> draws its own label and its own error; a <code>Typography</code> above it is a second label, and one below it is an error the field does not know about.</li></ul><h2 id="functions" tabindex="-1">Functions <a class="header-anchor" href="#functions" aria-label="Permalink to &quot;Functions&quot;">​</a></h2><p>Fourteen, with the specification&#39;s own names and call signatures: <code>required</code>, <code>length</code>, <code>regex</code>, <code>numeric</code>, <code>email</code>, <code>formatString</code>, <code>formatNumber</code>, <code>formatCurrency</code>, <code>formatDate</code>, <code>pluralize</code>, <code>openUrl</code>, <code>and</code>, <code>or</code> and <code>not</code>.</p><p>Declaring one is a claim that your renderer implements it. Ten of the fourteen are one <code>Intl</code> call, three are boolean arithmetic, and <code>openUrl</code> is the only one that does anything to the page — which is why it is declared <code>rendererOnly</code> and needing a user activation.</p><p><code>formatString</code> is the one to know about: A2UI has no operators, so it is the only way to put a value into a sentence.</p><h2 id="versions" tabindex="-1">Versions <a class="header-anchor" href="#versions" aria-label="Permalink to &quot;Versions&quot;">​</a></h2><p>The catalog is written against <strong>A2UI v1.0</strong>, which the file states in its own <code>protocolVersion</code>. The v0.9 catalog had a <code>theme</code> key and wrapped every component in a <code>ComponentCommon</code>; v1.0 has neither, and adds <code>instructions</code> and a <code>$defs</code> holding <code>anyComponent</code> and <code>anyFunction</code>.</p><p><strong>The adapter registers with <code>@a2ui/react/v0_9</code></strong>, because 0.11 has no v1.0 renderer — its root export is still v0.8. The eighteen components only use constructs the two versions share, which is what makes the bridge a rename rather than a translation, and the two differences that exist are both harmless: v1.0 moved <code>accessibility</code> out of the catalog entry and into the envelope, which the adapter puts back, and v1.0&#39;s <code>Action</code> gained a <code>userMessage</code> that the v0.9 schema strips rather than rejects.</p><p>The <code>@a2ui/*</code> packages are on 0.11.x while the specification is at 1.0, so expect the tooling to move before the format does. When there is a v1.0 React renderer, what changes is one import inside this package and nothing in <code>catalog.json</code>.</p><h2 id="next" tabindex="-1">Next <a class="header-anchor" href="#next" aria-label="Permalink to &quot;Next&quot;">​</a></h2><ul><li>The protocol: <a href="https://a2ui.org" target="_blank" rel="noreferrer">a2ui.org</a>, and the <a href="https://github.com/google/A2UI/blob/main/specification/v1_0/catalogs/basic/catalog.json" target="_blank" rel="noreferrer">Basic Catalog</a> it is modelled on.</li><li>What the props mean on the components themselves is under <a href="./../components/">Components</a>.</li></ul>`,36)])])}const k=a(o,[["render",n]]);export{g as __pageData,k as default};
