import { AnimateTyping } from 'neba';

export default function AnimateTypingHero() {
  return (
    <AnimateTyping className="text-xl font-semibold text-(--neba-fg)" speed={18}>
      Ship the interface, not the design system.
    </AnimateTyping>
  );
}
